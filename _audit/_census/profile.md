# CENSUS SLICE: PROFILE, IDENTITY, SETTINGS AND PRIVACY

Written 2026-09-03. **REVISION 2** -- widened after the lead reported that Help
topic pages can render `0 articles` for products that exist, and after a fifth
state was added to the schema. What changed is recorded in section 11.

Read-only. No LinkedIn account was touched: no browser, no session, no page
load, no MCP tool call. The taxonomy was imported by walking LinkedIn's PUBLIC
Help Center and then, in a second pass, by SEARCHING FOR PRODUCT NAMES where a
topic index looked thin. The coverage verdicts come from the repository at
``. Nothing was committed and
no tracked file was edited.

---

## 1. COUNTS

**CORRECTED BY:** `_audit/2026-09-20-the-reopener-triggers.md` -- rows B2 and M1 called the upload ban package-wide and the verb unsanctioned; one call site has been sanctioned since 2026-09-04, and no state moved.

**CORRECTED BY:** `_audit/2026-09-21-the-read-triage.md` -- row `K8`'s reason cell read "no tool, no reason", and the second half is FALSE: the capability was measured live on 2026-09-05 at allowlist +0, on an instrument shown able to disagree with itself on the same page. The "no tool" half is true and is kept. **NO STATE MOVED** -- this slice's GAP count is 55 before and after. The same document triages all 17 of this slice's read-direction GAP rows by remaining cost and finds 16 of the 17 gated on an address, a decision or a press.
**CORRECTED BY:** `_audit/2026-09-23-readers-four-rows.md` -- row `O3` said the 2026-09-21 witness showed *"the panel opens"* and left one name-free shaper as the whole blocker. The press behind that sentence was measured to land on the header nav; the row is blocked by "Show more analytics" carrying no sanctioned attribute and by filter application being a submission. State unchanged.

**CORRECTED BY:** `_audit/2026-09-21-what-is-reachable-now.md` -- row `O3`'s reason cell read *"no tool, no reason"*. There is now a reason and it is not a ruling: the disclosing press on `/analytics/profile-views/` is permitted, three of its four conditions settle offline against the shipped gate, and the press was TAKEN with a witness returning `disclosed: true`. **NO STATE MOVED** -- this slice's GAP is 55 before and after -- and the row is now blocked on ONE unbuilt artifact, a name-free shaper for a panel made of other people, priced against a `# readonly-ok` waiver budget that is AT its cap of 22 with zero remaining.

    CAPABILITIES ENUMERATED, IN SCOPE, MAPPED       260

      COVERED-PROVEN                                 19
      COVERED-UNFIRED                                 7
      COVERED-CANNOT-DELIVER                          5
      EXCLUDED-RULED                                150
      GAP                                            79

**DELTA SINCE THE FREEZE, 2026-09-04. The block above is UNCHANGED and still
reads as it did when this census was written**, so every document citing those
numbers still resolves against them. What moved is recorded here instead,
because a count that silently rewrites itself cannot be cited:

    CAPABILITIES ENUMERATED      260  ->  261   L2 SPLIT into L2 and L2b
      MEASURED-ABSENT              0  ->    1   L2, the follower COUNT
      GAP                         79  ->   79   L2 left it, L2b entered it

    COVERED-PROVEN                19  ->   20   N2, dark mode CHANGE, fired
    COVERED-UNFIRED                7  ->    6   same row

**Why N2 moved, 2026-09-19.** It is the only state change on this slice from the
first writes this server has ever fired. The row was never short of a tool --
it was short of a fire, and it had NAMED the verification that would settle it:
*"a fresh navigation and a re-read of all three radios."* That is exactly what
was done, so this is the row's own criterion being met rather than a new and
easier one being found. **The verification read a DIFFERENT surface from the one
clicked**, which is the standard this campaign applies to every write.
**Nothing moved out of GAP and the GAP count is untouched.**

**NOT MOVED, and this is the larger half of the pass.** `N1` (dark mode current
state) was already COVERED-PROVEN on six agreeing readings and does not move --
re-reading a surface that was already proven adds a reading, not a state. Every
`EXCLUDED-RULED` settings row in sections K, M and N stays exactly where it is:
firing the ONE setting admitted by name is the ruling *working*, not a reason to
revisit the family. **A fire on an admitted page is not an argument for
admitting its neighbours.**

**Why the split, since a split is the unusual move.** `L2` read "Own follower
count and follower list" -- two capabilities on one row. A live reading on
2026-09-04 retired one half and never touched the other, and no single state is
honest about both: MEASURED-ABSENT claims evidence the LIST does not have, GAP
throws away evidence the COUNT does have. Merging them deletes the vocabulary
needed to describe what is actually known, which is how a row comes to be more
confident than its evidence -- and section L is where this census already
carries one such row's consequences.

**`MEASURED-ABSENT` is defined in section 2**, added the same day: no tool, and
a LIVE READING of the surface says LinkedIn does not draw the thing. It is not
a weaker GAP; it is a stronger reason than most EXCLUDED-RULED rows carry,
because nobody decided against the capability -- the page does not have it.

Set aside BEFORE mapping, and counted so none of them reads as a zero:

    Help Center rows that DOCUMENT behaviour rather than name a
      control he can exercise (troubleshooting, FAQ, "how X works")   101
    DUPLICATES across the three primary walks                         102
    RECOVERY-PASS rows that confirmed or enriched a row already
      counted, rather than adding a capability                         40
    NOT-ENTITLED -- his tier or his country cannot do it either        13
    RETIRED OR PAUSED BY LINKEDIN -- no longer a capability              7
    OWNED BY A SIBLING CENSUS SLICE (jobs, network, messaging)         14
    ------------------------------------------------------------------
    RAW HELP CENTER ROWS HARVESTED                                    537
      three primary walks               477
      two product-name recovery passes   60

**Three numbers carry the slice.**

**COVERED-PROVEN is 19 of 260, and every one of the 19 is a READ.** Not one
write in this slice has ever completed against live LinkedIn.

**123 of the 150 EXCLUDED-RULED come from THREE address-family rulings**, not from
123 decisions: the settings family (93), the `/edit/` family (23) and
`/jobs/application` (7). Section 6 splits that out. The shape it reveals is the
real one: this server is bounded by three family refusals plus a handful of named
acts, and **not one of those 123 capabilities was weighed on its own merits.**

**The reported hazard did NOT reduce this denominator.** Every topic index this
slice relied on rendered a full article list -- `a64` 55, `a51` 26, `a153003` 49,
`a149001` 18, `a65` 40. The `0 articles` failure appears to be specific to Events
and LinkedIn Live rather than a general property of topic pages. The recovery
passes still added 20 capabilities, but they came from products with no topic
home at all, not from an index that rendered empty. Detail in section 10.

**CORRECTED BY:** `_audit/2026-09-23-exclusion-returns.md` -- lane R returned 98 of this slice's 113 exclusions to GAP with each blocker named -- the 22 profile editors, 19 Open To Work rows, the unnamed settings pages and the rest -- and kept 15 on the census's written grounds; GAP 55 -> 153.

**LANE R DELTA, 2026-09-23 -- THE EXCLUSION RETURNS.** The blocks above are UNCHANGED;
this records the movement. Every moved row carries a note in its own cell naming its
blocker, and the decision behind each is in the lane R audit the line above cites. The
call was the orchestrator's, delegated under OUTWARD-ACTS-NEED-THE-OPERATOR.

    GAP               55  ->  153   98 returned
    EXCLUDED-RULED   113  ->   15   kept, see below

**Kept, each on a written ground:** N29 (ground one); N28 (grounds one and two); E5, F5, K7, M3, O2, P1, I11, N27 (ground two); L5, N30, N31 (ground four); A23 (grounds four and two); E4 (the operator's 2026-09-04 ruling).
**THE THREE FAMILY RULINGS SECTION 6 COUNTS NOW HOLD NO ROW OF THIS SLICE.** The
settings family's unnamed pages are awaiting admission by name -- his ruling admits a
setting by name and rules none out; the "/edit/" family is blocked on an exemption his
2026-08-31 ruling allows; and "/jobs/application" was a read-only fence that ruling (b)
withdrew. Section 6 is left as written, as this slice leaves its frozen blocks.
**`O6-O20` IS ONE ROW STANDING FOR FIFTEEN CAPABILITIES**, so the capability count moves
by fourteen more than the row count shows; `scripts/census_completion.py` says so under
its collapse check.

---

## 2. HOW A CAPABILITY WAS ASSIGNED A STATE

    COVERED-PROVEN          a tool exists AND an audit records it firing live
                            and returning what it claims. Cited per row.
    COVERED-UNFIRED         a tool exists and would not refuse, and nothing
                            records it running live.
    COVERED-CANNOT-DELIVER  a tool exists, it HAS fired live, and it cannot do
                            this thing. Filing these as UNFIRED would say
                            "nobody has tried", which inverts a measurement.
    MEASURED-ABSENT         no tool, and a LIVE READING of the surface says
                            LinkedIn does not draw the thing. Added 2026-09-04
                            by team-lead ruling, for the same reason
                            COVERED-CANNOT-DELIVER exists above it: filing this
                            as GAP would say "no tool and no reason", which
                            INVERTS A MEASUREMENT. The reason exists and is
                            stronger than a ruling -- nobody decided against
                            it, the page simply does not carry it. THE ROW
                            SURVIVES rather than being deleted, with its
                            evidence quoted in it, because a deleted row is
                            indistinguishable from a row nobody thought of.
    EXCLUDED-RULED          no tool, and a written passage in this repo gives a
                            reason that BEARS ON THIS CAPABILITY -- naming it,
                            or naming an address family or act-class containing
                            it.
    GAP                     no tool and no such passage.

**The line between EXCLUDED-RULED and GAP, which decides 229 rows.**
`readonly._ALLOWED_URL_PATTERNS` is closed by default: every LinkedIn address not
on it is already refused. So "the allowlist refuses it" is NOT a reason -- it is
the silence this census exists to measure. A row is EXCLUDED-RULED only where
something was WRITTEN: an entry on `readonly._FORBIDDEN_URL_SUBSTRINGS` (each
entry carries an argument), a key in `writes.PERMANENTLY_FORBIDDEN`, a
`WriteSpec` refusing in its own words, or an audit passage measuring the
capability unreachable. Everything a general mechanism merely happens to block is
a **GAP with a NAMED BLOCKER** -- recorded so nobody reads "GAP" as "cheap", but
not laundered into a decision.

**The settings-family ruling is capability-level, not path-level.** It says a
setting is admitted BY NAME or not at all. That excludes every page below the
settings index whatever its URL spelling -- which is why finding 7.1, where six
of those URLs slip past the denylist, **changes no state in this census.** The
capability-level ruling does the excluding; the denylist is defence in depth, and
it is the defence that has holes.

**Read and write are separate rows where their states differ.** Where they agree,
one row carries both.

---

## 3. THE SERVER SURFACE THIS SLICE IS MEASURED AGAINST

35 tools in `linkedin_server/server.py`. **Nine touch this slice.**
`writes.PERFORMABLE` holds 12 actions; **two are here**.

| tool | what it does here | live-fire |
|---|---|---|
| `linkedin_my_profile` | name, headline, location, About, public identifier, Open To Work + audience, which sections rendered, and on a second page load the skills list | FIRED-SUCCEEDED. Errored on the first live run 2026-08-21 (`mcp-servers/_audit/2026-08-21-linkedin-parse-fix.md:1`), fixed, read live since; skills re-measured 2026-09-03 -- 20 skill cards, all carrying text |
| `linkedin_profile_editor_fields` | control LABELS inside `/in/me/edit/intro/`, never values | FIRED-SUCCEEDED twice: 23 controls 2026-08-31 (`_audit/2026-08-31-linkedin-perform.md:370`), 17 controls 2026-09-02 (`:2925`) |
| `linkedin_profile_editor_values` | what those controls HOLD -- the restore path for an edit | FIRED-SUCCEEDED 2026-09-02 (`:2925`) |
| `linkedin_update_profile_field` | change ONE intro-editor field, behind the two-call gate | **FIRED-FAILED.** Shipped `a540461` 2026-09-02, could not navigate once -- three independent fatal defects, `NAVIGATIONS ATTEMPTED: []` -- while minting a live `confirm_token` off a real preview he confirmed. Repaired `ea5354d` the same day. **No successful edit exists, before or after the repair. "It fired" is not "it worked", and no prose calling it well-verified counts as coverage.** |
| `linkedin_update_setting` | dark mode: read the three-state radio group, then change it | READ FIRED-SUCCEEDED -- six readings, two days, three builds, agreeing on every count: 20 controls, ZERO forms, 16 links, no dialogs, no redirect, exactly one of three radios checked. **WRITE FIRED AND VERIFIED 2026-09-19** -- off -> on -> off, each state read back after a fresh navigation (`_audit/2026-09-19-tier1-fires.md`); this superseded a standing **WRITE NEVER FIRED**. For any setting but dark mode the tool refuses in Python and loads nothing: `READABLE_SETTINGS` has exactly one key |
| `linkedin_surface_census` | control counts on `profile`, `profile_edit_intro`, `settings`, `settings_dark_mode`, `premium` (plus six keys outside this slice) | FIRED-SUCCEEDED on all five of this slice's keys: profile 4, profile_edit_intro 4, settings 3, settings_dark_mode 2 (`_audit/2026-08-31-linkedin-finish.md:275-279`); premium 3 (`_audit/2026-08-31-linkedin-perform.md:2035`) |
| `linkedin_who_viewed_me` | Who's Viewed Your Profile; 365 days on his Premium Career account | FIRED-SUCCEEDED -- "Now 10 rows, 10 distinct names, verified live" (`mcp-servers/_audit/2026-08-21-linkedin-parse-fix.md:5`) |
| `linkedin_my_activity_items` | his own activity rail -- the profile Activity section | FIRED-SUCCEEDED, and measured UNRELIABLE: 233 controls on one reading, 67 on another, same session, minutes apart |
| `linkedin_server_info` | reports which actions are performable | not a LinkedIn surface |

**There is no `linkedin_set_open_to_work` tool.** The spec exists in `writes.py`;
nothing on the tool surface calls it. `server.py:82`:

> *"`set_open_to_work` has no tool registered for it at all, so it is not part of
> the thirty-five: it is a spec behind the gate with nothing on the surface to
> call it. `writes.mint` refuses it a grant at issue, so no confirm token for it
> can exist for anyone."*

That is why every Open To Work write below is EXCLUDED-RULED and **not**
COVERED-CANNOT-DELIVER: the fifth state needs a tool that exists and has fired,
and here nothing exists to fire.

---

## 4. THE TABLE

R = read, W = write.

### A. Intro / top card -- the ONE profile editor this server can address (29)

`/in/me/edit/intro/` is admitted by an EXACT-url exemption past the `/edit/`
forbidden entry. Its live control list was 23 on 2026-08-31 and 17 on 2026-09-02
-- **a different set, not a subset**. Six of the 17 can be aimed at by name;
three cannot, for two different measured reasons, and those three are the fifth
state's clearest instances.

Help Center `a547248` names the desktop editor's fields as: Name, Profile photo,
Background photo, Headline, Current position, Education, Location, Industry,
Contact info, "Open to". **It names no pronouns field and no postal code**, both
of which appear below on other evidence.

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| A1 | Headline | R | COVERED-PROVEN | `my_profile` returns it; `profile_editor_values` reads it verbatim |
| A2 | Headline | W | COVERED-CANNOT-DELIVER | `update_profile_field` exists; `profile_editor_fields` fired live 2026-09-02 and measured the control unaimable -- *"`headline` because its name IS its content"*. A `div[role=textbox]` with no aria-label, no label-for and no title, so its accessible name resolves to its own text; the reader returns `<content>` and withholds it |
| A3 | First name | R | COVERED-PROVEN | *"Their values read perfectly -- the value reader returned both"* |
| A4 | First name | W | COVERED-CANNOT-DELIVER | *"The first-name and last-name inputs have NO ACCESSIBLE NAME AND ARE `required: true` ... they cannot be addressed by name. That is the exact inverse of the problem the ruling was written to solve"* (`:2960`) |
| A5 | Last name | R | COVERED-PROVEN | same live reading |
| A6 | Last name | W | COVERED-CANNOT-DELIVER | same measurement |
| A7 | Additional name (former / maiden / nickname) | R | COVERED-PROVEN | read live; value is the empty string |
| A8 | Additional name | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. aimable by `label-for`; the gate would not refuse it; never fired. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A9 | Additional-name visibility | W | GAP | four options, named by `a545784`: Only you / Your connections / Your network / All LinkedIn members. **EXCLUDED-RULED 2026-09-19: NO ROUTE REMAINS, which is why this is a refusal and not a deferral.** Measured live on the intro editor: its only candidates are two unnamed switches, and across all 11 fields **0 carry `aria-expanded` or `aria-haspopup`, every `id` shapes to `<opaque>`, and no field carries a `name`** -- so the control is unidentifiable by every non-press route this server can read. The press route is closed too and twice over: it matches no shape on the disclosing-press ruling's enumerated list (`_audit/2026-09-19-the-disclosing-press-ruling.md` condition 2), and toggling a switch is a WRITE, not a disclosure. Evidence: `_audit/2026-09-19-profile-modals-measured.md` D3 **ZERO RE-TAKEN 2026-09-19 11:45-11:50 AFTER THE `/in/me/` SHELL REPORT, AND IT STANDS.** `content-tail` measured `/in/me/` serving 2,146 chars (0.197% of the recorded prior), so every profile zero taken today was suspect. Three fresh readings: `/in/me/` html **398,365 / 398,234 / 398,424**, controls **236 / 235 / 236** -- a full page, stable, 36.5% of the prior and not 0.2%. The intro editor rendered in all three (**10-11 fields, 8 labelled**), and **a shell cannot draw a labelled form**. The load-bearing zeros reproduced in all three: `aria-expanded` 0, `aria-haspopup` 0, `name` attribute 0. **The field count drifted 11/10/11 and that is NOT what this row rests on.** No theory is offered for the shell `content-tail` saw; it was not reproduced here and nothing separates a transient hydration failure from a session or product change. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the control is one of two unnamed switches on the intro editor (0 aria-expanded, 0 aria-haspopup, 0 name attribute, measured 2026-09-19 and re-taken three times), so no route this server reads can aim at it; a write with no WriteSpec. The A2 and A4 precedent would file it COVERED-CANNOT-DELIVER once linkedin_update_profile_field has fired. |
| A10 | Country / Region | R | COVERED-PROVEN | read live |
| A11 | Country / Region | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. aimable by `aria-label`. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A12 | City | R | COVERED-PROVEN | read live |
| A13 | City | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. aimable by `aria-label`. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A14 | Postal code | W | GAP | not among the 17 controls, and `a547248` does not name it either -- see NOT REACHED #12  **THE BLOCKER'S PREMISE IS NOW WRONG: THE CONTROLS ARE READ.** `INTRO-EDITOR-UNREAD-CONTROLS` says they never have been. They were, 2026-09-19, by RUNNING THE SHIPPED TOOL `linkedin_profile_editor_fields` -- which already existed, is wired, and whose first docstring line is *"Name the controls inside the intro editor on HIS OWN profile"*. No reader was missing; a run was. Self-ownership established, 2 page loads, nothing pressed. **WHAT THE CONTAINER HOLDS:** 16-17 controls across two runs (the count is NOT stable), of which **10 are form fields** (6 text inputs, 2 checkboxes, 2 selects), 2 carry `role=switch`, and **4 HAVE NO ACCESSIBLE NAME AT ALL** (`name_source: none`). **THE EDITOR ITSELF HAS BEEN MEASURED AT 67 CONTROLS, so this container is a SUBSET and an absence here is an absence FROM THIS CONTAINER, never from the editor.** **FOR THIS ROW THE ANSWER IS `unknown`, WHICH IS NOT `absent`.** `linkedin_server.intro_fields.signature` returns a TRI-STATE and postal_code reads `unknown`: no label matched, AND four labels could not be compared, so the field may be behind one of them. **A present/absent reader would have reported `absent` here and been believed** -- that is the tidy-zero this repository keeps paying for, and the tri-state exists to refuse it. **SO THE PRECONDITION IS NOT SETTLED, AND THE BLOCKER IS NOW SPECIFIC: this container cannot answer the question while 4 of its controls are unnameable.** NEXT ARTIFACT: a reading that can name those four, or the other ~50 controls of the editor. **STILL A WRITE EITHER WAY** -- it needs a WriteSpec, a gate and a ruling, and no read banks it. Instrument `linkedin_server/intro_fields.py` (12 tests, shown failing); run `scripts/_probe_intro_editor_controls.py` **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** No new code is owed: `linkedin_update_profile_field` aims at any control the intro editor names, and since this lane it presses the editor's `Save` and verifies on a fresh render (before, it did neither). What is missing is the CONTROL: the render-gated reading of 2026-09-19 found no postal-code field among eleven -- its two unnamed text inputs are the required first- and last-name fields (`A4`) -- while the container signature still reads `unknown` over four unnamed controls. Lifted by capture C1 (one navigation to the admitted intro editor, no press, no fill: every control's label, tag and state, and for each UNNAMED control the shaped text of its row). LinkedIn draws a postal code for some countries only, and changing Country/Region to see one is an edit the capture does not make; if C1 finds none, this row's candidate state is MEASURED-ABSENT for this account. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A15 | Location display choice | W | GAP | not among the 17 controls  **THE BLOCKER'S PREMISE IS NOW WRONG: THE CONTROLS ARE READ.** `INTRO-EDITOR-UNREAD-CONTROLS` says they never have been. They were, 2026-09-19, by RUNNING THE SHIPPED TOOL `linkedin_profile_editor_fields` -- which already existed, is wired, and whose first docstring line is *"Name the controls inside the intro editor on HIS OWN profile"*. No reader was missing; a run was. Self-ownership established, 2 page loads, nothing pressed. **WHAT THE CONTAINER HOLDS:** 16-17 controls across two runs (the count is NOT stable), of which **10 are form fields** (6 text inputs, 2 checkboxes, 2 selects), 2 carry `role=switch`, and **4 HAVE NO ACCESSIBLE NAME AT ALL** (`name_source: none`). **THE EDITOR ITSELF HAS BEEN MEASURED AT 67 CONTROLS, so this container is a SUBSET and an absence here is an absence FROM THIS CONTAINER, never from the editor.** **FOR THIS ROW THE PRECONDITION IS PARTLY SETTLED AND IT MOVED FORWARD:** the signature reads `city` **present** and `country_region` **present** in this container -- so location-choice fields ARE drawn where the row said they were not among the seventeen. What is not established is which of them is the *display choice* specifically. **STILL A WRITE** -- needs a WriteSpec, a gate and a ruling, and no read banks it. Instrument `linkedin_server/intro_fields.py` **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** Same family and same gap as `A14`: the tool needs no new code, the control has not been named. LinkedIn's location display choice follows a postal code, which this account's editor did not draw on 2026-09-19. Lifted by capture C1 (one navigation to the admitted intro editor, no press, no fill: every control's label, tag and state, and for each UNNAMED control the shaped text of its row). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A16 | Industry | R | COVERED-PROVEN | read live |
| A17 | Industry | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. aimable by `aria-label`. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A18 | Pronouns | R | COVERED-PROVEN | read live; option text returned. **Enumerated from the server's own live control list, not from LinkedIn's documentation, which has no pronouns article** |
| A19 | Pronouns | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. a `select`; `select_option` is the 4th entry on `SANCTIONED_MUTATIONS`. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A20 | Education shown in intro | R | COVERED-PROVEN | read live; option text returned |
| A21 | Education shown in intro | W | COVERED-UNFIRED | **NOT SAVED 2026-09-23 22:46 (live lane, session 1); ZERO WRITES.** The profile-edit ruling's first condition is notify-network CONFIRMED OFF IN THE EDIT DIALOG. The shipped `linkedin_profile_editor_fields` read 17 controls in the intro editor and none is a notify control: the only two switches are 'Open Profile' and 'Profile Premium Badge' (identified offline from the capture). A condition that cannot be proven means no save, by the brief's rule. What would make it provable: a reading of the account-level share-profile-updates setting plus a ruling that it satisfies condition 1, and the write path asserting it at save time. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. a `select`, same route. **RELEASED BY `SELF-PROFILE-EDITS-NOT-OUTWARD`** (2026-09-23): an edit to his own profile field targets no other person and reverses, so it is not an outward act and needs no named target. Held by no ruling; what remains is a session. A live proof runs with notify-network off, restores the field in the same session, proves both by a before/after reading, and is never taken on a field that broadcasts by nature **LANE L7, 2026-09-24 -- THE BASIS OF THIS STATE, REPLACED; THE STATE STANDS.** Until 2026-09-24 `linkedin_update_profile_field` never pressed the editor's `Save`: it entered the change, pressed nothing, and verified by re-reading the unsaved dialog, so a live run of this row would have reported `performed: true` for an edit LinkedIn never stored. COVERED-UNFIRED now rests on the repaired commit: `profile_editor.read_save_gate` presses `Save` only once condition 1 of the ruling is established -- a notify control in the dialog read off, or, when the amended condition's account-level reader is wired, his 'Share profile updates with your network' setting read off before the edit; NEEDS-OPERATOR until one of them is -- and the verification reads the stored value on a fresh render of the editor. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 3 and the integration section. |
| A22 | Primary Position(s) -- which current role shows in the intro | W | GAP | real control, named verbatim in `a550169`: *"If you've already added Primary Position(s) select the company you want to show in your intro"*. Distinct from reordering (`a786867`). No position control among the 17  **THE BLOCKER'S PREMISE IS NOW WRONG: THE CONTROLS ARE READ.** `INTRO-EDITOR-UNREAD-CONTROLS` says they never have been. They were, 2026-09-19, by RUNNING THE SHIPPED TOOL `linkedin_profile_editor_fields` -- which already existed, is wired, and whose first docstring line is *"Name the controls inside the intro editor on HIS OWN profile"*. No reader was missing; a run was. Self-ownership established, 2 page loads, nothing pressed. **WHAT THE CONTAINER HOLDS:** 16-17 controls across two runs (the count is NOT stable), of which **10 are form fields** (6 text inputs, 2 checkboxes, 2 selects), 2 carry `role=switch`, and **4 HAVE NO ACCESSIBLE NAME AT ALL** (`name_source: none`). **THE EDITOR ITSELF HAS BEEN MEASURED AT 67 CONTROLS, so this container is a SUBSET and an absence here is an absence FROM THIS CONTAINER, never from the editor.** **FOR THIS ROW THE ANSWER IS `unknown`.** `current_position` matched no comparable label while four labels could not be compared. The row cites LinkedIn's help article naming the control verbatim, so the likeliest reading is that it sits behind one of the four unnamed controls or in the ~50 outside this container -- but **likeliest is not measured**, and `unknown` is what the instrument will say until one of those is read. **STILL A WRITE.** Instrument `linkedin_server/intro_fields.py` **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** Choosing which current role the intro shows is a display choice on his own intro, not adding a position. The family delivers it with no new code once the control is named -- a select, chosen by its own option text. Not drawn on 2026-09-19; Help `a550169` conditions it on primary positions already added (UNVERIFIED against this account). Lifted by capture C1 (one navigation to the admitted intro editor, no press, no fill: every control's label, tag and state, and for each UNNAMED control the shaped text of its row). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A23 | Name pronunciation audio | W | EXCLUDED-RULED | `a550527`: max 10 seconds, **mobile iOS/Android app only** -- cannot be recorded or edited on desktop, only deleted. A browser-driven server structurally cannot record it **RETIRED 2026-09-05, `MOBILE-APP-ONLY` (3.9).** Refused from two independent directions, neither of them the allowlist: `a550527`, fetched this pass, says it cannot be recorded or edited on the desktop site at all; and the one verb desktop does offer, delete, meets `writes.PERMANENTLY_FORBIDDEN['delete_or_withdraw_anything']`. REOPENER: LinkedIn shipping desktop recording. See `_audit/2026-09-05-decide-retire-rulings.md` |
| A24 | ID name as additional name | W | GAP | `a7153330`; requires an identity verification first (block K) **LANE L7, 2026-09-24 -- CREDENTIAL, BLOCKED.** It presupposes a completed identity verification (`K2`, settings family), and the ruling CREDENTIAL-SETTINGS-NEED-THE-OPERATOR bars any live proof of an identity-verification control unless he names it. No control exists on this account to capture, and no build is appropriate before he has a verified name to show. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A25 | Contact info panel | R | GAP | **CAPTURED 2026-09-23 22:46 (live lane, session 1); STATE UNCHANGED.** `/in/me/overlay/contact-info/` REDIRECTED to his profile (path depth 4 -> 2) and drew 1 dialog; lane L1's anchor reading: 58 anchors, 30 of them member anchors. The capture is `_state/l1-contact.html` (gitignored) for the offline reader this row still owes. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. the control `Edit contact info` WAS among the 17 read on 2026-09-02; the panel behind it has never been opened. **STAYS GAP 2026-09-19 with the reason NARROWED, deliberately not EXCLUDED-RULED:** the control is **wired and undeclared** -- `aria-haspopup` 0, `aria-expanded` 0, `aria-controls` 0, yet `DOMDebugger.getEventListeners` shows a `click` listener registered on the node (negative control: three static nodes on the same live page report NONE). So the PRESS route is refused -- it is off the disclosing-press ruling's shape list, and the ruling separately refuses anything that opens an editor -- **but the CAPABILITY is not proven closed**: a route from the profile surface that nobody has tried would not be excluded by any of this. Evidence: `_audit/2026-09-19-profile-modals-measured.md` amendments A and D **ADDRESS ADMITTED 2026-09-23 (lane L1); STATE UNCHANGED.** The route nobody had tried is the overlay's OWN ADDRESS: `/in/me/overlay/contact-info/` is now on the read allowlist, one anchored line, the `/in/me/` form only, under `PROFILE-EDITOR-ADDRESSES-ALLOWED` -- a NAVIGATION, not the press the disclosing-press ruling refused. Nothing has loaded it: it is NEEDS-CAPTURE for the live lane (`scripts/_probe_l1_admitted_reads_live.py` key `contact`, one load), and any reader owes closed-vocabulary presence only -- no email, phone, website or birthday may cross back. See `_audit/2026-09-23-lane-l1-refused-reads.md` **DRAWN, 2026-09-24 (lane Y2 re-harvest):** the intro editor's dialog draws 'Edit contact info' at `/in/<me>/edit/forms/contact-info/new/` (captured 2026-09-23). |
| A26 | Website on profile | W | GAP | inside that unopened panel **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** A field of his own contact-info editor, visible to whom his settings allow; nobody is targeted. The panel has never been opened. Lifted by capture C2 (stage 1: one navigation to the admitted contact-info overlay, shapes only, to learn whether its edit affordance is an ANCHOR and to which route; stage 2, only if it is: one navigation to that editor once its exact address is admitted under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED, labels and states, never a value). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A27 | Phone number on profile | W | GAP | same. **There is no address field** -- `a565128` enumerates exactly six: LinkedIn profile link, email, phone, website, instant messenger, birthday **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** As `A26`. Lifted by capture C2 (stage 1: one navigation to the admitted contact-info overlay, shapes only, to learn whether its edit affordance is an ANCHOR and to which route; stage 2, only if it is: one navigation to that editor once its exact address is admitted under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED, labels and states, never a value). A live proof types a value HE picks and puts the previous one back in the same session. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A28 | Instant messenger accounts on profile | W | GAP | same panel **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** As `A26`. Lifted by capture C2 (stage 1: one navigation to the admitted contact-info overlay, shapes only, to learn whether its edit affordance is an ANCHOR and to which route; stage 2, only if it is: one navigation to that editor once its exact address is admitted under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED, labels and states, never a value). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| A29 | Birthday on profile, and its visibility | W | GAP | same panel **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** As `A26`, plus the visibility choice. A birthday visible to connections drives LinkedIn's birthday reminders to them, so a live proof restores both value and audience in the same session. Lifted by capture C2 (stage 1: one navigation to the admitted contact-info overlay, shapes only, to learn whether its edit affordance is an ANCHOR and to which route; stage 2, only if it is: one navigation to that editor once its exact address is admitted under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED, labels and states, never a value). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |

### B. Photo, banner, frames, badges (10)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| B1 | Whether a profile photo exists | R | COVERED-PROVEN | `my_profile.completeness.has_photo`, off the topcard images |
| B2 | Profile photo add / change / delete | W | GAP | **REASON REPAIRED 2026-09-20. THE STATE IS UNCHANGED AND CORRECT; THE REASON HAD GONE FALSE, WHICH IS THE HARDER DEFECT** -- right answer, wrong reason is indistinguishable to a later reader from wrong answer, wrong reason. This cell said *"a test-enforced **package-wide** ban on `set_input_files`"*. **There is no package-wide ban and has not been since 2026-09-04.** Measured by importing, not by grep: `readonly.SANCTIONED_MUTATIONS` holds **7** entries and the seventh is `('linkedin_server/writes.py', 'perform', 'set_input_files')`. The test does not assert the kind ABSENT -- it asserts it PRESENT, exactly once, and its own docstring calls that *"the literal inverse of the line this test used to end on"*. **WHY THE ROW STILL DOES NOT MOVE:** `writes.PERFORMABLE` holds **12** actions and the upload verb is NOT one of them, so no caller can name it, and `writes.writes_enabled()` is **False**, so every write path is off. **The verb is permitted at exactly ONE switched-off call site while the capability stays closed.** **THE OPEN QUESTION IS STILL OPEN AND IS NOT RESOLVED HERE:** the docstring's ground was *"Nothing in this package should be one edit away from that, and the operator has never been asked about it"* -- and **no operator answer is recorded anywhere in this corpus**, so the sanction is a dated code fact and not an answer to it. **REOPENER, NAMED, and both halves are checkable by import rather than by reading prose: the upload verb entering `writes.PERFORMABLE`, OR `writes.writes_enabled()` returning True by default.** WHO: either is one import. **CITATION REPAIRED TOO:** the old `tests/test_readonly.py:306-341` now lands inside `test_the_partition_conserves_every_hit`, a different test -- a stale line range does not dangle, it gives a plausible wrong answer. The ban lives in `test_exactly_one_place_in_this_package_can_reach_a_file_input`, cited by SYMBOL so it cannot drift again. Full reading in `_audit/2026-09-20-the-sanctioned-seventh.md`. Prior framing kept: an OPEN QUESTION, not silence -- finding 7.11 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: no WriteSpec for the photo: the upload verb, set_input_files, is sanctioned at one call site and is not in writes.PERFORMABLE. The operator was asked on 2026-09-04 and opened uploads FULLY -- profile photo, post media and message attachments (readonly.py). Owed: a WriteSpec, a gate and a consent line. Its delete half stays refused by delete_or_withdraw_anything. |
| B3 | Profile photo crop / filter / adjust | W | GAP | same ruling. `a541850` names the pop-up controls -- Edit, Add photo, Frames, Delete **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.1), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the photo editor's controls (Edit, Frames) have never been opened and no WriteSpec exists; uploads were opened on 2026-09-04. |
| B4 | Profile photo visibility audience | W | GAP | `/public-profile/settings`, which no forbidden substring catches and no allowlist pattern admits  **THAT CLAUSE IS FALSE AND THE ROW MOVES ON THE CORRECTION, 2026-09-20. EXCLUDED-RULED.** The bare word `settings` entered the forbidden substring tuple in `readonly.py` on 2026-09-03, and `/public-profile/settings` is the address that entry's own comment names as the member which exposed the hole: *"A SECOND SPELLING. `/public-profile/settings` has no trailing slash, so `/settings/` -- slashes on both sides, present since the beginning -- does not match it. A gate that turns on a trailing slash is a spelling filter, not a ruling."* That tuple is checked BEFORE the allowlist, so the address cannot be opened however the allowlist is edited later. `C7` is already EXCLUDED-RULED on exactly this shape (`/psettings/guest-controls`). **CO-LOCATION UNDER A RULED ADDRESS, which is how all 123 of this slice's family-ruling rows are filed -- nobody has weighed this capability on its merits and this row does not claim anybody has.** **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| B5 | Background / banner image add / change / delete | W | GAP | same ruling **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.1), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: no WriteSpec for a banner upload; uploads were opened on 2026-09-04. Its 'same ruling' pointed by position at P B4, a different address. Its delete half stays refused by delete_or_withdraw_anything. |
| B6 | #OpenToWork photo frame apply / remove | W | GAP | the residue clause of the `set_open_to_work` spec: *"Switching to All LinkedIn members draws a green #OpenToWork frame on the photo that a current employer and his colleagues can see. Taking it down later removes the frame; it does not un-see it."* **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). The All-members audience draws a frame his employer can see: a fire is his to name. |
| B7 | #Hiring photo frame apply / remove | W | GAP | implied by `a519730` and never enumerated -- **no Help Center article lists the frame set at all**, so the full frame inventory is unknown on both sides **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** The same capability as `J2`. The frame belongs to Open to Hiring (`J1`) and tells everyone who sees his photo that he is hiring; `J1` is blocked, so this is. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| B8 | Top Voice badge show / hide | W | GAP | `a1577365`; no tool, no reason **LANE L7, 2026-09-24 -- SELF-PRIVATE, BLOCKED.** The same capability as `K9` (`a1577365` both). No Top Voice badge exists on this account -- `K8` read zero in text AND in accessible names, on an instrument shown able to disagree with itself -- and the show/hide control exists only for a badge holder. Reopened by `K8` reading non-zero. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| B9 | Premium profile badge show / hide | W | GAP | `a569234`; no tool, no reason **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** He is Premium: his own profile draws the Premium icon. Where its show/hide control lives is not on record; the intro editor's two UNNAMED switches are the candidates. Lifted by capture C1 (one navigation to the admitted intro editor, no press, no fill: every control's label, tag and state, and for each UNNAMED control the shaped text of its row). If one is it, the build is a toggle kind in the same family, aimed by that row's text -- a naming route designed once the capture shows the structure, not before. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **FOLLOW-UP 2026-09-24 -- CAPTURED; SELF-PRIVATE; NOT BUILT, BLOCKED ON THE AIM.** The live lane's capture of the intro editor (C1, done) puts this row's control on record: the SECOND of the two unnamed switches in the editor's settings row, in the block whose title reads 'Profile Premium Badge'; the first is the Open Profile switch (row B10). SELF-PRIVATE, as section 6 classed it: showing or hiding the badge changes what viewers of his profile see, as a city or a headline does, but the class turns on whether the act's point is other people, not on whether they can see it -- a membership badge carries no message he authors, targets no one, notifies no one, broadcasts nothing and reverses, as B8's Top Voice badge does. The blocker is exact: NO ATTRIBUTE tells this switch from the Open Profile switch beside it (the two blocks differ only in a key of random-UUID shape and a generated input id, and both wrappers carry the same accessible name), so the only things that do are each block's visible title, which the family does not aim by, and the ORDER of the two blocks. Order is enough for the save gate to know that neither is a notify control (Follow-up F.1); it is not enough to aim a press, because a render that drew the two the other way round would turn an order-aimed press into a change to Open Profile -- row B10, a permission setting nobody has admitted. Lifted by any one of: LinkedIn giving the switch an accessible name of its own (the family aims by name); a render showing a stable attribute that tells the two apart; or a ruling admitting the block's title -- a LinkedIn UI string, not his data -- as the aim, compared inside the page. With any of them the build is offline: the fixture, its builder and the recognition already carry the structure. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, Follow-up F.2. |
| B10 | Open Profile setting (who may message without connecting) | W | GAP | R11 (`network.md:814`) -- twin `N159` *Enable or disable Open Profile on your own profile* is EXCLUDED-RULED under it. R11's premise re-verified 2026-09-19: `/mypreferences/d/categories/` is on `_FORBIDDEN_URL_SUBSTRINGS` and `categories/privacy` + `categories/profile-visibility` both return `is_read_url=False`. Corroborated independently by `_audit/2026-09-19-settings-tail-addresses.md`: the settings index draws 20 addresses and this toggle is not among them, so it sits behind a denylisted category page. Was GAP on *no tool, no reason* while a shipped ruling covered it **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him. |

### C. Public profile and vanity URL (8)

`/public-profile/settings` is not below the settings index, so the settings-family
ruling does not reach it. Nothing else in this repo names it either.

**THE SECOND SENTENCE WAS MEASURED FALSE ON 2026-09-20 AND FIVE ROWS OF THIS
TABLE MOVE ON THE CORRECTION.** It is kept standing rather than rewritten,
because a reader who arrives from a document citing it needs to find what they
were told. What changed is not this table and not LinkedIn: it is
`linkedin_server/readonly.py`, edited on 2026-09-03 -- the day after this slice
was written -- to add ten entries to `_FORBIDDEN_URL_SUBSTRINGS`, and
**`/public-profile/settings` is named VERBATIM in that block's own comment as
the address which exposed the hole**:

```
* A SECOND SPELLING. ``/public-profile/settings`` has no trailing
  slash, so ``"/settings/"`` -- slashes on both sides, present since
  the beginning -- does not match it. The SAME url with a trailing
  slash was already refused. A gate that turns on a trailing slash is
  a spelling filter, not a ruling.
```

The bare word `settings` is the entry, it is checked BEFORE the allowlist, and
`tests/test_the_second_gate_covers_the_class.py` pins the class. So `C2`, `C3`,
`C4`, `C5`, `C6` -- and `B4`, which sits on the same address in the previous
table -- are EXCLUDED-RULED by co-location under a ruled address, exactly as
`C7` already is one row below on `/psettings/guest-controls`.

**WHAT THIS IS NOT.** It is not a finding that anybody weighed the public-profile
capabilities and declined them; section 6's arithmetic applies here unchanged.
And it is not a new ruling -- the ruling is seventeen days old and this table
simply had not been re-read against it. **The general defect is worth more than
the five rows: a blocker cell that asserts what the boundary does NOT catch is a
claim with a timestamp, and nothing in this census re-takes it.** Three more
were found by the same sweep and corrected the same day -- `N12` (`/uas/`),
`O5` (`/create`), and this pair of sentences.

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| C1 | Own public profile URL / slug | R | COVERED-PROVEN | `my_profile.public_identifier`, parsed from the landed url |
| C2 | Customise the public profile URL | W | GAP | `/public-profile/settings`; no tool, no reason  **THE `no reason` HALF IS FALSE AND THE ROW MOVES ON THE CORRECTION, 2026-09-20. EXCLUDED-RULED.** The bare word `settings` entered the forbidden substring tuple in `readonly.py` on 2026-09-03, and `/public-profile/settings` is the address that entry's own comment names as the member which exposed the hole: *"A SECOND SPELLING. `/public-profile/settings` has no trailing slash, so `/settings/` -- slashes on both sides, present since the beginning -- does not match it. A gate that turns on a trailing slash is a spelling filter, not a ruling."* That tuple is checked BEFORE the allowlist, so the address cannot be opened however the allowlist is edited later. `C7` on this same table is already EXCLUDED-RULED on exactly this shape (`/psettings/guest-controls`). **CO-LOCATION UNDER A RULED ADDRESS, which is how all 123 of this slice's family-ruling rows are filed -- nobody has weighed this capability on its merits and this row does not claim anybody has.** **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| C3 | Public profile visibility master switch | W | GAP | `a528138`: *"Your profile's public visibility"* Off hides it from public view **EXCLUDED-RULED 2026-09-20 BY CO-LOCATION, not a new decision.** `/public-profile/settings` carries the bare `settings` forbidden substring added to `readonly.py` on 2026-09-03 and is named verbatim in that entry's own comment; the section header's *"Nothing else in this repo names it either"* was measured FALSE on 2026-09-20. The full argument is on `C2` and on the corrected section header. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| C4 | Per-section public visibility toggles | W | GAP | `a518980`; the default photo setting is Public **EXCLUDED-RULED 2026-09-20 BY CO-LOCATION, not a new decision.** `/public-profile/settings` carries the bare `settings` forbidden substring added to `readonly.py` on 2026-09-03 and is named verbatim in that entry's own comment; the section header's *"Nothing else in this repo names it either"* was measured FALSE on 2026-09-20. The full argument is on `C2` and on the corrected section header. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| C5 | Search-engine visibility of the public profile | W | GAP | same page **EXCLUDED-RULED 2026-09-20 BY CO-LOCATION, not a new decision.** `/public-profile/settings` carries the bare `settings` forbidden substring added to `readonly.py` on 2026-09-03 and is named verbatim in that entry's own comment; the section header's *"Nothing else in this repo names it either"* was measured FALSE on 2026-09-20. The full argument is on `C2` and on the corrected section header. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| C6 | Unlink a prior public URL | W | GAP | same page **EXCLUDED-RULED 2026-09-20 BY CO-LOCATION, not a new decision.** `/public-profile/settings` carries the bare `settings` forbidden substring added to `readonly.py` on 2026-09-03 and is named verbatim in that entry's own comment; the section header's *"Nothing else in this repo names it either"* was measured FALSE on 2026-09-20. The full argument is on `C2` and on the corrected section header. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: the bare "settings" word on readonly._FORBIDDEN_URL_SUBSTRINGS, added 2026-09-03 for the settings family, catches /public-profile/settings. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its rows GAP; a narrowing needs a measured blast radius. |
| C7 | Guest controls / cookie consent | W | GAP | `/psettings/guest-controls`; `"/psettings/"` is a forbidden substring **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (his cookie consent). |
| C8 | Save profile as a PDF | R | GAP | `a541960`: desktop only, **200 downloads per month**, English-only, and it works on **another member's profile too** -- which would collide with `PERMANENTLY_FORBIDDEN["load_a_third_partys_profile_to_measure_a_control"]` if ever built for anyone but him |

### D. Profile body sections (29)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| D1 | About text | R | COVERED-PROVEN | `my_profile.about`, trimmed to 1200 chars |
| D2 | About text | W | GAP | its editor is `/in/<member>/edit/forms/summary/new/`, named in the `update_profile_field` docstring as measured to exist, and refused by `/edit/`. The exemption table admits ONE url past that entry and this is not it **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D3 | Which profile sections rendered | R | COVERED-PROVEN | `completeness.sections_present` / `sections_not_rendered`, with the tool's own rule that absent means UNKNOWN, never zero |
| D4 | Experience entries | R | COVERED-CANNOT-DELIVER | `my_profile` DECLARES the field and always returns `experience_entries: None`, because it does not scroll and the section is deferred. It fires live and structurally cannot fill the field it promises. **`/in/<member>/details/experience/` IS on the read allowlist and nothing navigates to it** -- the tool hands the caller the url instead |
| D5 | Add / edit / delete a position | W | GAP | `/edit/` family ruling. `a593695` covers jobs, volunteering, military, board service; it does not enumerate field names **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. Its delete half stays refused by delete_or_withdraw_anything. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the add-an-Experience form `/in/<me>/edit/forms/position/new/`. |
| D6 | Employment type, skills, media on a position | W | GAP | `/edit/` family ruling PLUS the upload ban -- two independent written refusals **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. Uploads were opened on 2026-09-04. |
| D7 | Notify network of a job change | W | GAP | the standing broadcast setting is O-block; `a547248` names `/psettings/activity-broadcast` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (broadcasts to his network). |
| D8 | Reorder current positions | W | GAP | `/edit/` family ruling. `a786867`: Sort then Reorder, click-hold-drag; `drag_to` is also an unsanctioned mutation class **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. drag_to is also an unsanctioned mutation class. |
| D9 | Career break | W | GAP | `/edit/` family ruling. Confirmed to EXIST as a **Core** item in the Add-profile-section menu (`a540837`) and **documented nowhere** -- absent from a 55-article Profile index **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D10 | Education entries | R | COVERED-CANNOT-DELIVER | same shape as D4: `education_entries: None` declared and never filled; `/details/education/` allowlisted and unvisited |
| D11 | Add / edit / delete education | W | GAP | `/edit/` family ruling **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. Its delete half stays refused by delete_or_withdraw_anything. |
| D12 | Reorder education | W | GAP | `/edit/` family ruling **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D13 | Licenses and certifications | W | GAP | `/edit/` family ruling; and its Help article `a567169` is **HTTP 404 on all three verticals tried**. Its content survives only as a cached search snippet, so its form fields are UNVERIFIED. **REOPENER, NAMED 2026-09-20: the operator admitting this editor's address BY NAME** -- the mechanism that has already let exactly three addresses out of the closed families (dark mode, the intro editor, the skills details page; section 6). WHO: the operator. **AND THE HELP-ARTICLE HALF OF THIS CELL REOPENS NOTHING, flagged because it is the half that LOOKS live:** `a567169` resolving would supply the field names this row records as UNVERIFIED and would not move the row an inch, because the `/edit/` ruling excludes it either way. **A world-fact sitting beside a ruling is colour, not the load-bearing reason** -- and a reader who takes the 404 for the trigger will re-litigate a durable exclusion on a fact that cannot decide it **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D14 | Courses | W | GAP | `/edit/` family ruling; **name-searched, no dedicated Help article exists**. **REOPENER, NAMED 2026-09-20: the operator admitting this section's editor address BY NAME** (section 6 -- three addresses have ever left these families, each on an operator ruling). WHO: the operator. **THIS REOPENER IS INHERITED BY `D15`, `D16` AND `D17`**, whose cells read `same` and resolve here by backreference, and it is worded to read correctly from any of the four. **The Help-index half reopens nothing:** a dedicated article appearing would document the section and would not touch the ruling that excludes it **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D15 | Projects | W | GAP | same. One official fact only: the Project URL field is not available for newly-added projects **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D16 | Publications | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D17 | Patents | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D18 | Honors and awards | W | GAP | `/edit/` family ruling. `a563433` documents add/edit/delete but says only *"enter the required information"* -- it does not name the fields **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D19 | Test scores | W | GAP | `/edit/`; name-searched, no dedicated article **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D20 | Languages | W | GAP | `/edit/`; name-searched, every hit is a Page-language or UI-language article **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D21 | Organizations | W | GAP | `/edit/`; name-searched, no dedicated article **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D22 | Volunteer experience | W | GAP | `/edit/`; name-searched, every hit is the Experience section, the Page Volunteer button, or opportunity discovery **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D23 | Causes you care about | W | GAP | `/edit/` family ruling. An **Additional** item in `a540837` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| D24 | Open to volunteering | W | GAP | `a6862361`. Real fields: areas of interest, skills, On-site / Hybrid / Remote, locations, an "Email me matching opportunities" toggle, plus Edit and Delete. Reached from the `Open to` button, one of the three items measured on his account **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, NEEDS-CAPTURE.** It tells nonprofits he is open to volunteering. Its entry is an ANCHOR on his own profile render, to `/in/<member>/opportunities/volunteering/education/`; the `/in/me/` spelling of that address is refused by the read boundary today. Lifted by capture C3 (after that exact address is admitted: one navigation, shapes only, stopping at the first control that leads into the Open-to flow, whose Open To Work sibling fires `saveAndFetchNextStep` on entry). Its live proof content is his to name. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the `Open to` item's page `/in/<me>/opportunities/volunteering/education/`. |
| D25 | "Add profile section" menu contents | R | GAP | **CORRECTED 2026-09-19, WITHIN THE HOUR, BY THE WAVE THAT GOT IT WRONG.** This row was moved to MEASURED-ABSENT earlier today in commit `09d56d4` on the reading below; a follow-up probe OVERTURNED IT and the row is back to GAP. **The control exists. It was RENAMED, and my needle was as old as the row.** Measured: `a:has-text("Add section")` = **3 links**, while `[aria-label*="Add profile section" i]`, `[aria-label*="Add section" i]` and `button:has-text("Add section")` are all **0** -- the string `Add section` occurs 3 times in the document and 0 times inside `<script>` content, so it is rendered markup and not a payload. **The needle that read zero was taken from LinkedIn's HELP ARTICLE wording (`a540837`), not from the page** -- the same error the row itself embodies, repeated by the instrument sent to check it. **THEN MEASURED TO THE END, SAME MORNING, AND THE BLOCKER IS NOT WHAT THE LEDGER SAYS.** A third probe read the three anchors BEFORE pressing anything, because a click does NOT consult `readonly.is_read_url` the way `BROWSER.goto` does -- pressing an anchor with a real href moves his live signed-in session to an address nothing checked. All three read: `rel=same-host-path`, `path_depth=2`, `aria-haspopup=None`, `aria-expanded=None`, `aria-controls=no`, `role=None`. **Zero ARIA disclosure evidence, so they are LINKS and not a menu trigger** -- the add-a-section flow on this render is navigation, not a dropdown, and the gate correctly REFUSED to press. Boundary verdict on their target, as a boolean about the address and never the address: **`is_read_url=False` with `forbidden_tokens_present=[]`.** **So the address is UNDECLARED, NOT FORBIDDEN** -- it trips no `/edit/`, `/add`, `/new`, `/create`, `/delete` or settings token; simply no allowlist pattern names it. **THEREFORE `ADD-SECTION-MENU` IS MIS-COSTED IN SECTION 3 OF THE BLOCKER LEDGER, WHICH FILES IT `boundary: none`. It needs an allowlist +1 or a ruling, and that is a DECIDE item, not a MEASURE one.** NOW OWED: the operator naming that address, after which the supported route is the one every other nav in this package already uses -- read the address off the page, put it through the boundary, then `goto`. Evidence `_audit/_scratch/_probe-small-measures-followup-v2.txt` and `_probe-add-section-menu.txt`. **THE SUPERSEDED READING, kept because a retraction that deletes its own evidence cannot be checked:** on a 235-control render `add_profile_section` = 0 under both accessible-name spellings, detector 10 of 10 on owned markup, page control firing in the same top card (`edit` 6, `open_to` 5, `hiring` 3), corroborated by a second instrument on a separate load. All of that was true and none of it was sufficient, because every needle in it carried the help article's spelling. Prior enumeration kept for the record: **Core** About, Education, Position, Services, Career break, Skills; **Recommended** Featured, Licenses and certifications, Projects, Courses, Recommendations; **Additional** Volunteer experience, Publications, Patents, Honors and awards, Test scores, Languages, Organizations, Causes. Evidence `_audit/_scratch/_probe-profile-modal-presence.txt` |
| D26 | Profile level meter / All-Star | R | GAP | `my_profile` docstring: *"LinkedIn's own profile-strength meter is not exposed here, so this server does not report one. What it reports is derived and labelled as such."* **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.1), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: linkedin_my_profile does not read the strength meter (its docstring says so); whether /in/me/ still draws one is unmeasured. A docstring that declines a field rules nothing out. |
| D27 | Create / delete a secondary-language profile | W | GAP | `a541878`: desktop only; *"Translations aren't done for you"*  **COMPOUND, AND RULED NOT-SPLIT 2026-09-21 under `COMPOUND-ROW-SPLITS-ONLY-ON-STATE` (`_audit/2026-09-21-the-compound-rows.md`).** Both halves are writes and both are GAP, so no cell this census publishes is false of either half and there is nothing a split would fix today. **TRIGGER, and it is the reason this row kept being re-escalated: `R5` (`delete_or_withdraw_anything` -- *"destruction is not a write this design covers, at any confirm level"*) reaches the DELETE half and nothing reaches the CREATE half. IF `R5` IS EVER APPLIED TO THE DELETE HALF, THIS ROW MUST BE SPLIT FIRST** -- delete to EXCLUDED-RULED under `R5`, create staying GAP -- because the state cell would then be false of one half, and the state vocabulary has no both-value: `classify` answers a two-state cell by silently taking the first by textual order, or by dropping the row out of the census entirely. Until `R5` is applied the row stays whole **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** Its editor is an anchor on his profile ('Edit profile language'), to `/in/<member>/edit/secondary-language/`; its `/in/me/` spelling is refused by `/edit/` today and admissible under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED. What blocks it is the undo: a created language profile is removed only by the delete half, which `delete_or_withdraw_anything` refuses, so no live proof can restore it in the same session and a create would be irreversible through this server. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** its editor `/in/<me>/edit/secondary-language/`, drawn as 'Edit profile language'. |
| D28 | View a profile in multiple languages | R | GAP | `a541878`, `a544976` **DRAWN, 2026-09-24 (lane Y2 completeness admission):** a language rendering `/in/<slug>/en/`, linked from a job posting. |
| D29 | Add a LinkedIn Learning certificate of completion to the profile | W | GAP | `a704787`. Lands in a "Certificates of Completion and Skills" section. **Blocked if the profile already has over 100 skills** -- he has 20, so it is available to him **LANE L7, 2026-09-24 -- BROADCAST-BY-NATURE, BLOCKED.** A new section entry -- the kind of change the ruling's own example (a new position) names -- and excluded from live proof by that ruling. Its form is the `/edit/` certifications editor (`D13`, excluded) and its undo is a delete. No build. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |

### E. Skills and endorsements (8)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| E1 | Own skills list | R | COVERED-PROVEN | 20 skill cards off `/in/me/details/skills/`, live 2026-09-03 |
| E2 | Add / remove skills | W | GAP | `/edit/` family ruling; the editor address is measured in this repo's own fixtures as `/in/<member>/details/skills/edit/forms/<n>/` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. Its remove half stays refused by delete_or_withdraw_anything. |
| E3 | Reorder skills / pin top skills | W | GAP | `/edit/` family ruling, plus `drag_to` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. drag_to is also an unsanctioned mutation class. |
| E4 | Endorse another member's skills | W | EXCLUDED-RULED | `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`: *"zero endorse controls across 13 tracked fixtures ... and zero among the 222 controls read live on his own profile on 2026-08-30. You cannot endorse yourself, so the only surface that would carry the control is a THIRD PARTY'S PROFILE -- and loading one leaves them a durable record ... IMPOSSIBLE AS SPECIFIED, not unwanted"* **REOPENER, NAMED 2026-09-20 -- the same one `network.md` **R3** now carries, because this row and `N 111` rest on ONE measurement and it was written down once: a non-zero endorse control count from a re-take on a surface this package may load -- the account's own skills surface, or the tracked fixture corpus -- by `scripts/_probe_endorse_and_follow_lines.py`.** WHO: that instrument. **A control found on a THIRD PARTY'S PROFILE reopens nothing**, because that prohibition is ours and is not a measurement; the quoted key says IMPOSSIBLE AS SPECIFIED for exactly that reason. **INHERITED BY `E5`**, whose cell reads `same key` and resolves here -- `E5` additionally rests on `delete_or_withdraw_anything`, so it needs the operator act as well as the count |
| E5 | Remove an endorsement you gave | W | EXCLUDED-RULED | same key, plus `delete_or_withdraw_anything` |
| E6 | Hide / show an endorsement received | W | GAP | blocker: `"/endorse"` is a forbidden substring. **The written reason does not reach this act** -- `endorse_or_recommend` argues about making a statement about another person; this is housekeeping on his own profile **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** The same capability as network `114`. It targets one member's endorsement, so a live proof needs the endorsement HE names. Blocked twice: his skills page draws no endorsement at all (`E7`, network `118`, measured 2026-09-04), so none is on record to hide; and a per-skill endorsers address carries the forbidden substring `/endorse`. Reopened by the shipped re-take in `dom.read_profile_detail_entries` returning a non-zero count. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| E7 | Endorsements received | R | MEASURED-ABSENT | **BANKED 2026-09-19 BY THE FIELD-LEVEL PASS, and its stated blocker was inherited from a WRITE row it does not apply to.** The cell read *"same blocker, same absence of a reason"* -- pointing at `E6`, where `/endorse` is a forbidden substring. **That substring is about the endorse ACTION. This row is a READ of endorsements already received on HIS OWN profile, and the page that would show them is ADMITTED** -- `readonly.is_read_url("https://www.linkedin.com/in/me/details/skills/")` returns True, verified here. So no boundary blocks it. **THE CAPABILITY IS MEASURED ABSENT, NOT UNBUILT.** `dom.read_profile_detail_entries` emits an `endorsements` field on every call (`dom.py:8772`) carrying `drawn`, `lines` and `looked_at`, and `linkedin_my_profile` lifts it into its payload (`server.py:4221-4227`) -- whose own comment says it plainly: *"Not a parser this tool is missing -- a line LinkedIn does not draw."* **`N 118` IS THE SAME CAPABILITY IN ANOTHER SLICE and is already MEASURED-ABSENT** on a live 2026-09-04 reading of that page: 20 skill cards returned and no endorsement line among them. **WHAT THE INSTRUMENT DID SEE travels with the zero** -- the field reports its own denominator (`looked_at.cards`, `looked_at.card_lines`), so a no here is distinguishable from a page that never loaded. REOPENER: LinkedIn drawing an endorsement line, which that reading is taken live on every call to catch. **E6 and N 114 stay GAP: both are WRITES** (hide/show an endorsement) and a read banks neither |
| E8 | Opt out of endorsements entirely | W | GAP | a settings-family toggle (`a551156`); section 6 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (who may endorse him). |

### F. Recommendations (9)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| F1 | Recommendations received | R | GAP | no tool, no reason |
| F2 | Request a recommendation | W | GAP | **EXCLUDED-RULED 2026-09-19 by PROPAGATION, not a new decision.** Named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`, the same key this slice already cites on `F3`/`F4`/`F5`. Twin `N119` -- *Request a recommendation from a 1st-degree connection* -- is EXCLUDED-RULED under `network.md` **R3**, whose own row list enumerates 119-128. **The control is inside this block:** three of its eight recommendation writes already carried the ruling and five did not, so the split is not a slice-wide difference. R3's basis is a MEASUREMENT rather than a policy -- zero endorse controls across 13 tracked fixtures, zero on his own skills surface, zero among 222 controls read live. Prior note kept: no tool, no reason; it reaches another member **REOPENER, NAMED 2026-09-20, and it is the SHARPER of the two `network.md` **R3** now carries -- ANY count at all of RECOMMENDATION controls.** This cell already states that R3's basis is a measurement of ENDORSE controls; finding 7.4 of this slice states the same defect from the other side (*`endorse_or_recommend` names recommendations and argues only about endorsements*). **No census of recommendation controls has ever been taken**, so the measurement under this row is not merely dated -- it is of a DIFFERENT OBJECT. WHO: a needle that does not exist yet, which is itself the finding. The endorse-count reopener on `E4` also applies **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F3 | Give a recommendation | W | GAP | named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`. **NAMED BUT UNARGUED** -- finding 7.4 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F4 | Revise a recommendation you gave | W | GAP | same key, same caveat **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F5 | Delete a recommendation you sent | W | EXCLUDED-RULED | `delete_or_withdraw_anything`: *"destruction is not a write this design covers, at any confirm level"*; `/delete` is also forbidden |
| F6 | Accept or dismiss a recommendation received | W | GAP | **EXCLUDED-RULED 2026-09-19 by PROPAGATION, not a new decision.** Named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`, the same key this slice already cites on `F3`/`F4`/`F5`. Twin `N121 + N122` -- *Accept / Dismiss a recommendation you received* -- is EXCLUDED-RULED under `network.md` **R3**, whose own row list enumerates 119-128. **The control is inside this block:** three of its eight recommendation writes already carried the ruling and five did not, so the split is not a slice-wide difference. R3's basis is a MEASUREMENT rather than a policy -- zero endorse controls across 13 tracked fixtures, zero on his own skills surface, zero among 222 controls read live. Prior note kept: no tool, no reason **REOPENER, NAMED 2026-09-20: any count at all of RECOMMENDATION controls** -- see `F2` and `network.md` **R3**. The measurement this row inherits counted ENDORSE controls (finding 7.4), so it is of a different object, and no recommendation-control census has ever been taken. WHO: a needle that does not exist yet **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F7 | Ask for a revision of one received | W | GAP | **EXCLUDED-RULED 2026-09-19 by PROPAGATION, not a new decision.** Named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`, the same key this slice already cites on `F3`/`F4`/`F5`. Twin `N123` -- *Ask for a revision of a recommendation you received* -- is EXCLUDED-RULED under `network.md` **R3**, whose own row list enumerates 119-128. **The control is inside this block:** three of its eight recommendation writes already carried the ruling and five did not, so the split is not a slice-wide difference. R3's basis is a MEASUREMENT rather than a policy -- zero endorse controls across 13 tracked fixtures, zero on his own skills surface, zero among 222 controls read live. Prior note kept: no tool, no reason **REOPENER, NAMED 2026-09-20: any count at all of RECOMMENDATION controls** -- see `F2` and `network.md` **R3**. The measurement this row inherits counted ENDORSE controls (finding 7.4), so it is of a different object, and no recommendation-control census has ever been taken. WHO: a needle that does not exist yet **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F8 | Decline a requested recommendation | W | GAP | **EXCLUDED-RULED 2026-09-19 by PROPAGATION, not a new decision.** Named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`, the same key this slice already cites on `F3`/`F4`/`F5`. Twin `N128` -- *Decline a recommendation request someone sent you* -- is EXCLUDED-RULED under `network.md` **R3**, whose own row list enumerates 119-128. **The control is inside this block:** three of its eight recommendation writes already carried the ruling and five did not, so the split is not a slice-wide difference. R3's basis is a MEASUREMENT rather than a policy -- zero endorse controls across 13 tracked fixtures, zero on his own skills surface, zero among 222 controls read live. Prior note kept: no tool, no reason **REOPENER, NAMED 2026-09-20: any count at all of RECOMMENDATION controls** -- see `F2` and `network.md` **R3**. The measurement this row inherits counted ENDORSE controls (finding 7.4), so it is of a different object, and no recommendation-control census has ever been taken. WHO: a needle that does not exist yet **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |
| F9 | Hide / unhide recommendations on the profile | W | GAP | **EXCLUDED-RULED 2026-09-19 by PROPAGATION, not a new decision.** Named in `PERMANENTLY_FORBIDDEN["endorse_or_recommend"]`, the same key this slice already cites on `F3`/`F4`/`F5`. Twin `N126` -- *Hide or unhide a recommendation you received* -- is EXCLUDED-RULED under `network.md` **R3**, whose own row list enumerates 119-128. **The control is inside this block:** three of its eight recommendation writes already carried the ruling and five did not, so the split is not a slice-wide difference. R3's basis is a MEASUREMENT rather than a policy -- zero endorse controls across 13 tracked fixtures, zero on his own skills surface, zero among 222 controls read live. Prior note kept: no tool, no reason **REOPENER, NAMED 2026-09-20: any count at all of RECOMMENDATION controls** -- see `F2` and `network.md` **R3**. The measurement this row inherits counted ENDORSE controls (finding 7.4), so it is of a different object, and no recommendation-control census has ever been taken. WHO: a needle that does not exist yet **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: writes.PERMANENTLY_FORBIDDEN['endorse_or_recommend'] names recommendations, but its surviving ground is a count of ENDORSE controls (profile.md finding 7.4) and the operator dissolved its policy ground on 2026-08-25; no census of recommendation controls has ever been taken. A recommendation is written about or for another member, so a fire goes only at a member he names. The key lives in writes.py, lane L4's file. |

### G. Featured and Activity sections (7)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| G1 | Own activity items | R | COVERED-PROVEN | **WITH A MEASURED RELIABILITY DEFECT** -- the qualifier was in the state cell until 2026-09-05 and made this row invisible to every counter. the rail returned 233 controls on one reading and 67 on another in the same session. The spec's own words: *"A check that answers nothing on some readings it can take is not a verification."* |
| G2 | Activity section default view | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** Which activity type visitors see first on his profile: a display preference, nobody targeted, reversible. Its control is an ANCHOR on his profile render, 'Edit default activity', to `/in/<member>/edit/forms/content-collections-star-pill/new/` -- its `/in/me/` spelling is refused by `/edit/` today and admissible under the ruling PROFILE-EDITOR-ADDRESSES-ALLOWED. Lifted by capture C4 (that exact address admitted; one navigation; the options, which is checked, its Save; no press). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| G3 | Featured section: add work samples | W | GAP | the upload ban -- the package-wide ban on `set_input_files` that the operator opened on 2026-09-04 (NAMED 2026-09-24 by lane L7 on the orchestrator's call: this cell opened 'same ruling', which took its argument by position from whichever substantive row sat above it; record `_audit/2026-09-24-lane-l7-profile-writes.md`, integration) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.1), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: no WriteSpec and the Featured add flow has never been opened; uploads were opened on 2026-09-04. Its 'same ruling' pointed by position at a COVERED row. |
| G4 | Featured: reorder / edit / remove / unpin | W | GAP | `/edit/` family ruling, plus `drag_to` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. drag_to is also an unsanctioned mutation class; its remove half stays refused by delete_or_withdraw_anything. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the section's control 'Featured overflow menu'. |
| G5 | Feature content pulled from other profile sections | W | GAP | `/edit/` family ruling **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the "/edit/" entry on readonly._FORBIDDEN_URL_SUBSTRINGS keeps refusing every profile editor but the intro editor, the one exact-url exemption (readonly.py records that narrowing was refused on 2026-08-31). The operator ruled on 2026-08-31 that the profile editors are allowed, in the /in/me/ spelling only, and the update_profile_field WriteSpec also refuses until a field inside the editor has been observed. Admitting this editor is an exemption or a narrowing in readonly.py, which is lane L1's file. |
| G6 | Per-post analytics | R | COVERED-PROVEN | **FIRED AND BANKED 2026-09-23 22:14 (live lane, session 1), impressions and engagements only.** Fired live by `scripts/_probe_live_lane_session_1.py --only per_post` through the shipped `linkedin_creator_analytics`, attach mode, one page load, nothing pressed. `per_post` came back in its documented shape: `readable` True, `items_read` 2, `impressions` and `engagements` each a list of 2 integers, `engagements_drawn_for` 2. **The fields whose meaning was checked:** `total_impressions` is the sum of `impressions` and `total_engagements` the sum of `engagements`; each list has one entry per item read; `unparsed`, `not_activity_links`, `duplicate_links` and `unreadable_links` all 0; no identifier, title or name in the field. The scope below is unchanged by the fire: the items that page features, not every post. See `_audit/2026-09-23-live-lane-session-1.md` Entry 2. **Lane L1's paragraph, as it stood before the fire:** **COVERED-UNFIRED 2026-09-23 (lane L1), impressions and engagements only.** The headline numbers were never behind the refused per-post page: LinkedIn prints them IN THE TEXT OF THE LINK to it. `/analytics/creator/content/` -- admitted, and already fired by `linkedin_creator_analytics` (`M C40`, COVERED-PROVEN) -- draws one `/analytics/post-summary/urn:li:activity:<id>/` link per featured item reading "<n> impressions <middle dot> <n> engagements", and his profile's activity rail draws the same link reading "<n> impressions"; measured offline on two captures. `linkedin_creator_analytics` now returns `per_post` (`linkedin_server/post_summary_counts.py`): integers, one per distinct item, no identifier, title or name. **UNFIRED** -- nothing has seen the field return live; the live lane fires it. **SCOPE** -- the items that page features, not every post he has published; the reactions/comments/reposts split and the viewer demographics live on the per-post page itself, whose address was admitted the same day and is NEEDS-CAPTURE (`M C38` carries that half). See `_audit/2026-09-23-lane-l1-refused-reads.md`. Prior cell, kept: ~~no tool, no reason. Two impressions links were counted on an item permalink 2026-09-01 and never followed~~ |
| G7 | Profile search appearances | R | **COVERED-PROVEN 2026-09-05** | **"no tool, no reason" IS NOW HALF FALSE, 2026-09-05.** THE REASON EXISTS: this is the reciprocal instrument in the people-search consent question -- it reads the receiving end of a SEARCH the way `linkedin_who_viewed_me` reads the receiving end of a profile view, and `_audit/2026-09-05-search-results-consent.md` names it LOAD A, the one reading that would let 19 reads be ruled on evidence instead of on silence. Boundary opened, `dom.read_search_appearances` built, census key `search_appearances` added, `linkedin_search_appearances` registered. **AND THE PAGE WAS READ, TWICE, 2026-09-05** -- 108 appearances on both loads, four named discovery channels of which `Search` is one at 12%, no redirect, no authwall, nothing spent between loads. **The two metric CAPTIONS came back `<redacted>`**, so the numbers are proven and their labels are not: the ancestor walk ran out of hops and the reader takes the conservative branch. See `_audit/2026-09-05-search-appearances-load-a.md`, which also withdraws an overclaim about what the five member links on the page point at |

### H. Services / "Providing services" (11)

A Service Page is a profile-attached identity surface open to every member --
`Services` is a **Core** item in the Add-profile-section menu. **All eleven are
GAPs: nothing in this repo mentions a Service Page in any capacity.**

| # | capability | R/W | state | blocker |
|---|---|---|---|---|
| H1 | Create a Service Page / add services | W | GAP | one of the three items the `Open to` menu resolves to on his account; never actioned **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, NEEDS-CAPTURE.** A Service Page is a public listing clients can act on; its content is his to name. Its entry is an ANCHOR ('Showcase your services ... Add services', also in the tracked `profile_topcard_hydrated.html`), to `/in/<member>/opportunities/services/education/`; the `/in/me/` spelling of that address is refused by the read boundary today. Lifted by capture C3. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the `Open to` item's page `/in/<me>/opportunities/services/education/`. |
| H2 | Service categories and specific services | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists on this account (`H1`), so there is nothing to edit. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H3 | Service Page About / expertise text | W | GAP | typing -- `fill` IS sanctioned, so this is a surface gap, not a class gap **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H4 | Work location and remote availability | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H5 | Starting rate or contact-for-pricing | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H6 | Edit the Service Page | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H7 | Unpublish the Service Page | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`); and an unpublish withdraws a public page, which wants `delete_or_withdraw_anything` read against it before any build. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H8 | Link / unlink the Service Page to a Company Page | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`); the Company Page link has nothing to hang on. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H9 | Request service reviews (up to 20 invitations) | W | GAP | reaches real people; no tool, no reason |
| H10 | Turn off / manage services reviews | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** No Service Page exists (`H1`), so no reviews to manage. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| H11 | "Providing services" section as rendered on the profile | R | GAP | no tool, no reason |

### I. Open To Work and job-seeking identity (16)

**The block where the repo argues most and can act least.** The state is read; the
editor has never been loaded, and its absence is a measurement rather than an
admission. From `set_open_to_work.reversibility_procedure`:

> *"THE ABSENCE OF A URL IS NOW A MEASUREMENT RATHER THAN AN ADMISSION, taken
> 2026-08-24: 237 distinct urls and 37 payload paths were enumerated across all
> five profile captures and ZERO reach an open-to-work editor, a job-preferences
> page or a career-interests page; the strings 'opentowork' and 'open-to-work'
> occur zero times anywhere. The editor is not url-addressed AT ALL -- its screens
> are addressed by an internal screen id, and its entry control fires a request
> whose own name is saveAndFetchNextStep. So the one click that would first REVEAL
> the editor is also the first click that could CHANGE it, which is why no capture
> of it may be taken except with him watching."*

`opentowork` and `open-to-work` are both forbidden substrings; the spec carries
`url_template=None`; **and no tool is registered for the action at all.**

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| I1 | Open To Work on/off AND its current audience | R | COVERED-PROVEN | `my_profile.open_to_work`, off the topcard at no extra page load, at BOTH hydration states. LinkedIn prints the audience verbatim ("Open to work - Recruiters only"); confirmed against frozen fixtures and live 2026-08-23 |
| I2 | Turn Open To Work on or off | W | GAP | the quote above; no tool exists **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I3 | Change the audience | W | GAP | same, plus the green-frame residue clause. Help `a507508` documents THREE audiences -- All LinkedIn Members, Recruiters only, **Visible only to you** -- against the spec's three, which differ; finding 7.3 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). The All-members audience draws a frame his employer can see: a fire is his to name. |
| I4 | OTW field: job titles | W | GAP | editor never loaded **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I5 | OTW field: locations | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I6 | OTW field: location / workplace types | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I7 | OTW field: employment types | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I8 | OTW field: start date | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I9 | OTW field: notice period (INDIA ONLY -- applies to him) | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I10 | OTW field: expected annual salary (INDIA ONLY -- applies to him) | W | GAP | same **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.3), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the set_open_to_work WriteSpec refuses in its own words (NEVER LOADED): the editor is a modal no capture has held, the single click that would first show it is also the first that could change it, and no tool is registered for it; "opentowork" and "open-to-work" stand on the forbidden tuple. The operator approved Open To Work on 2026-08-23 and the 2026-08-25 audit files it UNMEASURED, to be measured rather than refused. Owed: one capture of the modal taken with him watching (profile-modals 2026-09-05 s7). |
| I11 | Delete the Open To Work card | W | EXCLUDED-RULED | same, plus `delete_or_withdraw_anything` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.5), on the census's own written grounds.** Ground two: deleting the Open To Work card is a deletion, and this cell already cites delete_or_withdraw_anything, which still stands; the NEVER LOADED half no longer holds it. |
| I12 | Job preferences / career-interests page | W | GAP | measured: zero of 237 urls reach one **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: no url among 237 reaches the page (measured, url routes only); the Open To Work editor is the standing case of a click-reached surface with no url, and no click route has been tried. Editing the preferences is a write with no WriteSpec. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** `/jobs/preferences/`, the jobs nav's 'Preferences' link (captured 2026-09-20) -- a drawn address for the page this cell measured zero urls reaching. |
| I13 | Minimum pay preference | W | GAP | on that same unreachable page, and named nowhere in this repo **LANE L7, 2026-09-24 -- SELF-PRIVATE, NEEDS-CAPTURE.** A job-seeker preference used for matching. The page is NOT unreachable by url: a local jobs-alerts capture draws a 'Preferences' anchor to `/jobs/preferences?viewType=SEEKING_PREFERENCES`, and the completeness probe recorded `/jobs/preferences` too; it is refused by the read boundary today. Lifted by capture C5 (that address admitted; one navigation; the minimum-pay control's shape; no press). `I12`'s zero-of-237 was measured over profile captures, which do not draw this anchor -- raised for its owner, not edited here. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| I14 | Signal interest in working for a company | W | COVERED-UNFIRED | ~~no tool, no reason~~ **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BUILT -- COVERED-UNFIRED.** `linkedin_mark_company_interest(job_id)`: the 'I'm interested' control in the posting's About-the-company card, on record in three tracked captures, pressed behind the single-use grant, off by default. The card must open by naming the posting's own employer and exactly one control in it must wear the OFF label; an employer already signalled reads unknown (the ON label was never captured) and is refused. Verified on a fresh render of the posting: the OFF label still drawn is 'not signalled'. It tells THAT employer's recruiters, so its live proof fires only at a posting HE names. Jobs twin `J 86`. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **NO TESTED RESTORE, AND TWO PREREQUISITES TO ITS LIVE FIRE (2026-09-24, integration, on the orchestrator's call).** Its undo is `P I15`, which presses the ON-state control that no capture holds, so no restore through this server exists or is tested; the row stays COVERED-UNFIRED all the same. It fires only once BOTH hold: a company HE names, and `P I15`'s control captured, so the undo exists before the act. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, the live queue. |
| I15 | Remove interest in working for a company | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, NEEDS-CAPTURE.** The removal presses the ON-state control, which no capture holds. Lifted by capture C6: the first supervised `I14` press records it (the receipt's after-press counts and fresh render, then one shape census of that card). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| I16 | Share how you found your job | W | GAP | no tool, no reason **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** LinkedIn asks it after a job change -- adding a new position, the ruling's own broadcast example -- and he has no new job on this account to share. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |

### J. Open To Hiring (4)

| # | capability | R/W | state | blocker |
|---|---|---|---|---|
| J1 | Turn on #Hiring / share that you are hiring | W | GAP | the `Open to` menu item IS observed across five captures; nothing acts on it **LANE L7, 2026-09-24 -- BROADCAST-BY-NATURE, BLOCKED.** Sharing that he is hiring is a share by nature, excluded from live proof by the ruling, and a false public claim for a job seeker. Its entry, the Open-to menu's 'Hiring' item, was measured 2026-09-19 named three times with no activation relation, and the Open-to family's entry is where the Open To Work editor fires `saveAndFetchNextStep` -- no press into it can be shown not to save. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the share box's 'Share that you're hiring'. |
| J2 | #Hiring photo frame add / remove | W | GAP | same **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** The same capability as `B7`; blocked with `J1`. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| J3 | Which job the #Hiring frame links to | W | GAP | same **LANE L7, 2026-09-24 -- VISIBLE-TO-OTHERS, BLOCKED.** Blocked with `J1`, and it needs a job HE posted as a hirer, of which none is on record. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| J4 | Current #Hiring state | R | GAP | `my_profile` parses only the open-to-WORK line off the topcard |

### K. Verification and badges (10)

`/mypreferences/d/verifications` is a page below the settings index, so the
settings-family ruling reaches it -- see section 6, and see finding 7.1 for why
the denylist does not.

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| K1 | Verifications section on the profile | R | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: not a setting: the Verifications section sits on his own profile (/in/me/, admitted), and no reader extracts it. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** the settings index draws the section as a page, `/mypreferences/d/verifications/` ('Verifications', captured 2026-09-23). |
| K2 | Identity verification via DigiLocker (India -- his route) | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: an identity verification that hands a government ID record to LinkedIn through DigiLocker's own domain -- an irreversible disclosure of his data, his to fire; the "verification" word entry, a settings-family backstop added 2026-09-03, refuses verification addresses. **DRAWN, 2026-09-24 (lane Y2 completeness admission):** 'Verify now' on the Premium hub opens `/trust/verification/`. |
| K3 | Workplace verification by work email | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: needs a code from his work inbox, an off-platform step only he can complete; the "verification" word entry refuses verification addresses. |
| K4 | Workplace verification via Microsoft Entra Verified ID | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: completes on Microsoft's domain (Entra Verified ID), an off-platform step only he can complete; the "verification" word entry refuses verification addresses. |
| K5 | Workplace verification via a company LinkedIn Learning licence | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: needs a company LinkedIn Learning licence, which the account is not recorded as holding; the "verification" word entry refuses verification addresses. |
| K6 | Phone number verification | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: needs an SMS code, an off-platform step only he can complete; the "verification" word entry refuses verification addresses. |
| K7 | Remove a verification | W | EXCLUDED-RULED | `delete_or_withdraw_anything`, and `/delete` |
| K8 | Top Voice blue badge on the profile | R | GAP | ~~no tool, no reason~~ **"NO REASON" IS FALSE: THE READING EXISTS AND IT WAS TAKEN AT ALLOWLIST +0. STATE DELIBERATELY UNCHANGED.** `_audit/2026-09-05-profile-rest.md` measured this row live on the already-admitted `/in/me/`, needing no boundary change: `top voice` reads **0 in the text corpus AND 0 in the accessible-names corpus**. **The instrument was shown able to disagree with itself on the same page** -- `premium` read 0 in text and 19 in names, `verified` read 4 and 5 -- so a double zero is a reading rather than a dead needle, which is the discrimination a bare zero cannot make. Its conclusion: *"No Top Voice badge is drawn on this account, in text or in accessible names."* **AND THE TOOL HALF IS TRUE AND SEPARATE:** `Top Voice` and `top_voice` occur ZERO times in `linkedin_server/`, so no tool would report the badge if LinkedIn started drawing one. **WHY NO STATE MOVES HERE.** The obvious move is MEASURED-ABSENT and it is not mine to make: that state is a claim about a surface, the reading is sixteen days old, and this wave was forbidden the browser. It is named as a candidate, with its evidence, so the next holder of the signed-in profile can settle it in one re-read. See `_audit/2026-09-21-the-read-triage.md` |
| K9 | Show / hide the Top Voice badge | W | GAP | `a1577365`; no tool, no reason **LANE L7, 2026-09-24 -- SELF-PRIVATE, BLOCKED.** The same capability as `B8`: no Top Voice badge on this account (`K8`). Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| K10 | Verification badge as it appears on job posts | R | COVERED-PROVEN | **FIRED AND BANKED 2026-09-20.** Fired live by `scripts/_probe_unfired_job_detail_insights.py` through the shipped `linkedin_job_detail` over **11 real postings** harvested via the shipped search tool. **THE TEST IS DISCRIMINATION, NOT PRESENCE:** this field is a boolean, so a reader whose selector had rotted would return False for every posting on earth and be indistinguishable from a correct one over a flat sample. It is therefore tallied three ways and banked only on OBSERVED-BOTH. `verified_job`: **true on 5 postings, false on 6, absent on 0 -> OBSERVED-BOTH.** This row and `jobs.md J 27` are the same capability in two slices and they now agree, on the same fired reading. **THE PRIOR SOURCE-TRACE, KEPT BECAUSE IT IS WHY THE ROW EXISTED:** **BANKED 2026-09-19; SHIPPED CODE ALREADY SURFACES IT AND THE ROW SAID *no tool, no reason*.** `dom.read_job_insight_panels` returns `"verified_job": bool(markers.get("verified"))` (`dom.py:8313`), and `server.py:3665` assigns that WHOLE dict to `linkedin_job_detail`'s `insights`, so the field reaches a tool result. Read off `/jobs/view/<id>` -- an address already admitted and already loaded -- so **no boundary cost and no page load added**. It is a BOOLEAN, which is why it is admissible at all: a badge's presence carries no name. **HOW THIS WAS NEARLY MISSED, because the method transfers:** a grep for `verified_job` in `server.py` and `shape.py` returns ZERO, which reads exactly like a dead field. A field-name grep CANNOT SEE A DICT THAT PASSES THROUGH BY REFERENCE; only tracing the assignment does. **UNFIRED, NOT PROVEN:** no audit records this field's value coming back from a live posting. See `_audit/2026-09-19-read-tail.md` |

### L. Creator tools, followers and analytics (9)

Creator mode itself is RETIRED (section 8); these survived it, and LinkedIn's
Help Center now states all members have access.

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| L1 | Creator analytics / audience analytics | R | GAP | **CAPTURED 2026-09-23 22:47 (live lane, session 1); STATE UNCHANGED.** `/analytics/creator/audience/` SERVED exactly; 39 chart labels, NONE in `chart_labels.KNOWN_METRICS` (33 unrecognised), so the shipped chart reader reads nothing here yet. Capture: `_state/l1-audience.html`. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. no tool, no reason **ADDRESS ADMITTED 2026-09-23 (lane L1); STATE UNCHANGED.** `/analytics/creator/audience/` is drawn on `/analytics/creator/content/` as the analytics nav's "Audience analytics" link and is now on the read allowlist, one anchored line, no query, no sub-path. No tool, because nothing has seen the page: NEEDS-CAPTURE for the live lane (`scripts/_probe_l1_admitted_reads_live.py` key `audience`). The page is follower DEMOGRAPHICS, aggregated other people -- a reader owes `census_shape` plus `census_redact_rare`. See `_audit/2026-09-23-lane-l1-refused-reads.md` |
| L2 | Own follower COUNT | R | MEASURED-ABSENT | **NOT ON THE TOPCARD. Measured live 2026-09-04**, `scripts/_probe_endorse_and_follow_lines.py`: `/in/me/` drew exactly ONE `shape._COUNT_LINE` match and it reads CONNECTIONS -- zero followers lines. So the *"275 followers"* quoted inside `publish_post.residue`, which was this row's own evidence, did NOT come from that page. **SCOPE, and the state claims nothing wider: measured absent FROM THE TOPCARD, and no other surface was searched.** A measurement retires the surface it covered, never the capability's whole name. What the same reading DID recover is the CONNECTIONS count, which `shape.parse_profile_topcard` had recognised since it was written -- only ever to rule it out of the headline -- and thrown away; `linkedin_my_profile` now returns it, with `at_least` carrying the `+` because LinkedIn stops counting out loud at 500 |
| L2b | Own follower LIST | R | GAP | **NOBODY HAS LOOKED.** Split from L2 on 2026-09-04 by team-lead ruling. L2 was compound -- count AND list -- and one half is measured absent while the other was never searched for. Neither state is honest about both: MEASURED-ABSENT would claim evidence this half does not have, and GAP would throw away evidence the COUNT half does have. **Merging the two deletes the vocabulary needed to say what is actually known**, which is how a row comes to be more confident than its evidence -- and this census has already dispatched a wave on one that was. No tool, no reason, and no reading **ADDRESS ADMITTED 2026-09-23 BY A `REVIEW:` COMMIT (lane L1); STATE UNCHANGED.** The write guard on the /follow substring matched a read address -- the connections-list shape. The 2026-09-05 network-tail wave (its section 2) wrote the repair as a spec and did not apply it; lane L1 applied ONE of its two halves (this list; the following list is `N 38`'s): an anchored allowlist line and a pattern exemption excusing that one substring alone, denylist unchanged at 33, with the spec's owed proof as a test (`tests/test_l1_follower_list_carve_out.py`). NAMED only -- no capture draws it. It merges on the operator's look, and that look must weigh a WRITTEN stance pointing the other way: `readonly.py`'s Manage-Pages entry says "the right response to the luck running out is to leave the people list unread, never to shorten the forbidden list" -- written about the FOLLOWING list (`N 38`, EXCLUDED-RULED on it); this admission shortens nothing but does read a people list. Then NEEDS-CAPTURE (live-lane key `followers`). A reader owes counts and relations only. See `_audit/2026-09-23-lane-l1-refused-reads.md` **DRAWN, 2026-09-24 (lane Y2 completeness admission):** a second address for the list, `/feed/followers/` ('<n> followers' on the member's own profile). |
| L3 | Create / edit / delete a newsletter | W | GAP | no tool, no reason  **COMPOUND (three verbs), AND RULED NOT-SPLIT 2026-09-21 under `COMPOUND-ROW-SPLITS-ONLY-ON-STATE` (`_audit/2026-09-21-the-compound-rows.md`).** All three halves are writes and all three are GAP, so no published cell is false of any of them. **TRIGGER: `R5` reaches the DELETE verb and nothing reaches CREATE or EDIT. IF `R5` IS EVER APPLIED TO THE DELETE VERB, THIS ROW MUST BE SPLIT FIRST** -- and note it splits into TWO rows, not three, because create and edit would still share a state and a direction, and the rule splits on state divergence alone. Until then the row stays whole |
| L4 | Newsletter analytics | R | GAP | no tool, no reason |
| L5 | Host a LinkedIn Live | W | EXCLUDED-RULED | he clears the >150-follower gate at 275; no tool, no reason **RETIRED 2026-09-05, `LIVE-BROADCAST` (3.5), on LinkedIn's own sentence fetched this pass:** `a568503` -- you cannot stream directly from LinkedIn, a streaming tool is needed to broadcast LinkedIn Lives. A browser driver cannot supply a video stream and LinkedIn says outright that no native path exists. REOPENER: browser-native go-live with no external encoder. See `_audit/2026-09-05-decide-retire-rulings.md` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.5), on the census's own written grounds.** Ground four: a568503, fetched 2026-09-05 -- 'You cannot stream directly from LinkedIn. A streaming tool is needed to broadcast LinkedIn Lives.' A browser session supplies no video stream. |
| L6 | Audio events | W | GAP | no tool, no reason; the Help article itself 404s. **EVENTS ROOT READ LIVE 2026-09-19 and this row DELIBERATELY STAYS GAP.** `/events/` is already allowlisted (`readonly.py:609`); page control PASS; `Audio event` 0/0, `Audio Event` 0/0, `Audio` 0/0, `Event type` 0/0, `audio` 0 in main / 2 in html uncharacterised. **What IS drawn: "Create an event"** (main=1). **NOT called MEASURED-ABSENT on purpose:** an event's TYPE is chosen inside the create-event flow, not on the root, so a zero here is a fact about which page was opened -- the render-gate error this repo has paid for twice. NEXT ARTIFACT: press "Create an event" and enumerate the Event-type control. **That press needs a consent ruling, not a boundary change** -- it opens a creation composer, which is exactly the "pressing a disclosure control is a different permission from reading a render" question `MATCH-DETAILS-COLLAPSED` was queued for. Evidence `_audit/_scratch/_probe-small-measures-live.txt` |
| L7 | "Ideas for your next post" | R | GAP | no tool, no reason |
| L8 | Analytics and tools hub | R | GAP | **CAPTURED 2026-09-23 22:47 (live lane, session 1); STATE UNCHANGED.** `/dashboard/` SERVED exactly; 19 chart labels, none in the metric vocabulary. Capture: `_state/l1-overview.html`. See `_audit/2026-09-23-live-lane-session-1.md` Entry 5. no tool, no reason **ADDRESS FOUND AND ADMITTED 2026-09-23 (lane L1); STATE UNCHANGED.** The hub is `/dashboard/`, and the tie is LinkedIn's own words: the tracked `tests/fixtures/profile_topcard_hydrated.html` draws it as a link whose accessible name is "Show all analytics", and the analytics nav on `/analytics/creator/content/` draws it as "Overview". Now on the read allowlist, one anchored line; `/analytics/` and `/creator-hub/` stay refused. NEEDS-CAPTURE for the live lane (key `overview`). See `_audit/2026-09-23-lane-l1-refused-reads.md` |

### M. Resumes and application documents (12)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| M1 | Upload a resume from Job Application Settings | W | GAP | `/jobs/application-settings/` contains `"/jobs/application"`, the FIRST entry on the forbidden tuple. **SECOND CLAUSE REPAIRED 2026-09-20; THE STATE IS UNCHANGED AND THE LOAD-BEARING HALF IS UNTOUCHED.** This cell went on to say *"`set_input_files` is **unsanctioned** on top of that"*, which has been false since 2026-09-04: `readonly.SANCTIONED_MUTATIONS` holds it as its seventh entry, at `('linkedin_server/writes.py', 'perform', 'set_input_files')`. **That changes nothing about this row**, and saying so is the point -- the forbidden-substring gate is what excludes it, the upload clause was never doing the work, and a stale clause beside a live one is how a reader comes to doubt both. **The capability is closed on its own terms anyway:** the verb is absent from `writes.PERFORMABLE` (12 actions) and `writes.writes_enabled()` is False. **REOPENER, NAMED, in two independent halves because this row has two reasons: the forbidden-substring entry `"/jobs/application"` being re-ruled by the operator, OR the upload verb entering `writes.PERFORMABLE`. BOTH must fall for the row to reopen**, which is why a reopener on either one alone would be a trigger that cannot fire. WHO: the operator for the first, one import for the second. See `_audit/2026-09-20-the-sanctioned-seventh.md` for the full reading of the sanction **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; the upload verb is not in writes.PERFORMABLE (uploads were opened on 2026-09-04), so a WriteSpec is owed. |
| M2 | Saved resumes list (4 most recent) | R | GAP | same address, same entry **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; no reader. |
| M3 | Delete a saved resume | W | EXCLUDED-RULED | same address, plus `delete_or_withdraw_anything` and `/delete` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.5), on the census's own written grounds.** Ground two: the act is a deletion, and this cell already cites delete_or_withdraw_anything, which still stands. The "/jobs/application" half was withdrawn with ruling (b) and no longer holds it. |
| M4 | Download a saved resume | R | GAP | same address **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; no reader. |
| M5 | View the resume used for a specific application | R | GAP | same address **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; no reader. |
| M6 | Saved screening-question answers | R/W | GAP | same address **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; no reader and no WriteSpec. |
| M7 | Opt out of saving job application data | W | GAP | same address **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.2), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: "/jobs/application" (the read-only server's first commit, no argument recorded) refuses /jobs/application-settings/: a fence for a read-only server whose purpose, the apply cut and the read-only rule, ruling (b) withdrew; no WriteSpec. The same toggle is J 76 in the jobs slice, a settings page awaiting admission by name. |
| M8 | "Share resume data with recruiters" toggle | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (recruiters receive his resume data). |
| M9 | Stored job applicant accounts | R/W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) (`/mypreferences/d/job-application-accounts`); denylist-uncaught, finding 7.1 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a pure read (the view half; its delete half stays refused by delete_or_withdraw_anything). Measured 2026-09-23 through the shipped gate: its address, /mypreferences/d/job-application-accounts, is refused by the "job-application" word entry, a settings-family backstop added 2026-09-03, so an admission by name needs an exemption past that entry too. |
| M10 | Autofill for work emails | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle. |
| M11 | Resume Builder | R/W | GAP | no tool, no reason; entitlement unverified |
| M12 | Resume Tips (AI resume feedback) | R | GAP | no tool, no reason; entitlement unverified |

### N. Account preferences, sign-in, security and account lifecycle (31)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| N1 | Dark mode current state | R | COVERED-PROVEN | six agreeing readings across two days and three builds |
| N2 | Dark mode change | W | **COVERED-PROVEN 2026-09-19** | **FIRED AND VERIFIED BOTH WAYS.** In `PERFORMABLE`; the verification this row specified -- a fresh navigation and a re-read of all three radios -- is the one that was performed. Round trip watched end to end: prior state read off the radio group (3 radios, exactly one checked), one click, page RELOADED, changed state read back with the other two reading unchecked, undo clicked, restoration read identical to the prior state. **The read-back is stronger than re-reading the control that was pressed** -- a control that redrew wrongly would have to report itself checked AND both others report themselves unchecked to pass. Two refusals on the way in, both correct and both naming what they SAW: an unknown state value listed the three known ones, and a no-op change was refused rather than performed. Audience: NOBODY -- broadcast nowhere, no feed, no notification. **This also settles `reversibility_class`, which every preview until today printed as `STILL-UNKNOWN`** and which the tool's own gate said needed exactly ONE ROUND TRIP, WATCHED. Evidence: `_audit/2026-09-19-tier1-fires.md` rung 1 |
| N3 | Which settings sections exist (the index) | R | COVERED-PROVEN | census `settings`, 34 controls, live 2026-08-30, three readings. The six sections are fixed by `a1337839`: Account preferences, Sign in & security, Visibility, Data privacy, Advertising data, Notifications **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the index at `/mypreferences/d/` ('Manage your account and privacy') and five of its six sections as pages in its side nav -- `/mypreferences/d/categories/account/`, `/mypreferences/d/categories/sign-in-and-security/`, `/mypreferences/d/categories/profile-visibility/`, `/mypreferences/d/categories/privacy/`, `/mypreferences/d/categories/ads/`; a notification card's own 'Change notification preferences' routes into the sixth. Each section's settings are this slice's rows (N4-N27, the O block, the P-R block). |
| N4 | Two-step verification on / off | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10). Methods named by `a1381088`: authenticator app, SMS. **Denylist-uncaught -- finding 7.1, and the most consequential of the six** **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: a credential control). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N5 | Change password | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10). `a1379143`: min 8 chars, plus a "Require all devices to sign in with new password" checkbox. Denylist-uncaught **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: a credential control, and the "password" word entry refuses its address). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N6 | Passkeys add / delete | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10). `a1621596`: max 5 per account **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: a credential control; its delete half stays refused by delete_or_withdraw_anything). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N7 | Email addresses -- add / remove / change primary | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10), `/psettings/email` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: account recovery; its remove half stays refused by delete_or_withdraw_anything). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N8 | Phone numbers -- add / remove / choose reset number | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: account recovery; its remove half stays refused by delete_or_withdraw_anything). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N9 | Where you're signed in -- list active sessions | R | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10), `/psettings/sessions`. Shows location, IP, device, browser, last sign-in **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a pure read (SECURITY: the active sessions). |
| N10 | Sign out of one session / all sessions | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10), same page. A distinct act from reading the list **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: signing out all sessions ends this server's own session). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N11 | Devices that remember your password | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (SECURITY: a credential control). **CREDENTIAL-SETTINGS-NEED-THE-OPERATOR (orchestrator, 2026-09-24, delegated; lane R at its merge).** Never live-proven unless the operator names it: it falls on the cannot-be-undone arm of OUTWARD-ACTS-NEED-THE-OPERATOR. |
| N12 | Keep me logged in / auto sign-in | W | GAP | `/uas/login` -- not a settings-index page, caught by no substring, named nowhere in this repo. `a1342645`: unavailable when 2FA is on  **THE `caught by no substring` CLAUSE IS FALSE AND THE ROW MOVES ON THE CORRECTION, 2026-09-20. EXCLUDED-RULED.** `/uas/` entered the forbidden substring tuple in `readonly.py` on 2026-09-03 with its reason written beside it -- *"A LEGACY NAMESPACE. `/uas/` is LinkedIn's old auth tree and the exact sibling of the `/psettings/` three lines up, which is on this list for precisely the stated reason that a legacy address is one LinkedIn can start serving again."* That tuple is checked BEFORE the allowlist. `password` and `two-factor` are on it too and reach the same family. **CO-LOCATION UNDER A RULED ADDRESS, not a judgement that auto sign-in was weighed and declined.** **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: "/uas/", added 2026-09-03 for LinkedIn's legacy auth tree, catches /uas/login. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its row GAP. a1342645: unavailable when two-step verification is on. |
| N13 | Sign-in security prompt / email code / CAPTCHA | R | GAP | an interstitial, not a page. `config.AUTHWALL_MARKERS` already turns a `/checkpoint/` landing into a reported failure, which is adjacent but not this **RETIRED 2026-09-05, `SIGNIN-INTERSTITIAL` (3.8) -- holding it would be the defect.** An authentication challenge exists to test that the actor is the human, and `writes.PERMANENTLY_FORBIDDEN` already carries `any_anti_detection_technique`. The designed response is built: a landing on any `config.AUTHWALL_MARKERS` becomes a REPORTED FAILURE. The row asks for a READ and the server implements a STOP, and the stop is correct. REOPENER: nothing that keeps the shape. See `_audit/2026-09-05-decide-retire-rulings.md` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.5), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: the challenge renders at /checkpoint/, which the boundary does not admit (allowlist silence); config.AUTHWALL_MARKERS turns a landing into a reported failure. Reading which challenge appeared is a diagnostic read; ANSWERING one is refused by any_anti_detection_technique. |
| N14 | Identity verification for account recovery (Persona) | W | GAP | `a1342692`; no path, no tool, no reason **LANE L7, 2026-09-24 -- CREDENTIAL, BLOCKED -- AND NO BUILD IS APPROPRIATE.** An account-recovery identity check reached when he cannot sign in, completed with his government ID and a live selfie through a third party. This server drives his signed-in browser and must never present his documents or his face; the ruling CREDENTIAL-SETTINGS-NEED-THE-OPERATOR makes it his alone. Record: `_audit/2026-09-24-lane-l7-profile-writes.md`, section 6. |
| N15 | Interface language | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10). `a521833` names ONE language control **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/settings/language/` ('Language' on the settings index, captured 2026-09-23). |
| N16 | Autoplay videos | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/settings/autoplay-videos/` ('Autoplay videos' on the settings index, captured 2026-09-23). |
| N17 | Sound effects | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/settings/enable-sounds-desktop/` ('Sound effects' on the settings index, captured 2026-09-23). |
| N18 | Showing other members' profile photos | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/settings/show-profile-photos/` ('Showing profile photos' on the settings index, captured 2026-09-23). |
| N19 | Name / location / industry via settings | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10); also reachable through the intro editor (block A) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (profile fields members see). **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/profile/<id>/` ('Name, location, and industry' on the settings index, captured 2026-09-23). |
| N20 | Calendar sync | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10); mobile app only **REOPENER, NAMED 2026-09-20: the operator admitting this setting BY NAME**, which is the settings-family mechanism and is how dark mode got in -- section 6, and `network.md` **R11**. WHO: the operator. **THE `mobile app only` CLAUSE IS THE CONTINGENT HALF AND IT IS NOT THE LOAD-BEARING ONE, flagged so the two are not confused:** a calendar-sync control appearing on the DESKTOP settings surface would falsify that clause and would leave the row exactly where it is, because the settings family is admitted by name or not at all. WHO for that half: an enumeration of the settings index, which this repo already runs **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (shares his calendar data with LinkedIn). **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mynetwork/settings/manage-syncing/`, drawn as both 'Sync calendar' and 'Sync contacts' (N21) on the settings index (captured 2026-09-23). |
| N21 | Contact / address-book sync | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (uploads other people's contact data). |
| N22 | Subscriptions and payments | R | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a pure read (his subscriptions and payments). **DRAWN, 2026-09-24 (lane Y2 completeness admission):** the purchase's own manage link at `/manage/purchases-payments/purchases/<id>/`; changing the plan is row S9. **DRAWN (lane Y2 re-harvest):** 'View purchase history' at `/manage/purchases-payments/purchases/` and 'Manage Premium account' at `/mypreferences/d/premium-manage-account/` (captured 2026-09-23). |
| N23 | Partners and services / Microsoft account link | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (an account link). |
| N24 | Permitted Services -- third-party app access | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10), `/psettings/permitted-services` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a self-only reversible toggle (third-party app access to his data). |
| N25 | LinkedIn Services (DMA) connect / disconnect | W | GAP | `a6222119`: Jobs, Marketing Solutions, Learning. Nav path only -- no URL, so no address to rule on. **RE-FILED 2026-09-19, `NO-URL-AT-ALL`. NOT a new decision -- the operator's settings ruling already covers it**, and the refusal needs no measurement of LinkedIn at all. Refused from two written directions: `/psettings/` and `/settings/` are on `_FORBIDDEN_URL_SUBSTRINGS`, checked BEFORE the allowlist, and DMA data-sharing controls live under that tree; and `linkedin_server/server.py:7364,7401-7402` -- "the read allowlist admits exactly one page below the settings index, admitted BY NAME on the operator's ruling that a setting is admitted by name or not at all." Same ruling already carries `M37`, `M41`, `M42`, `M46`, `M50`. REOPENER: the operator naming this setting -- that is the mechanism, not a consolation; it is how dark mode got in. **NOTE, and it is load-bearing: five rows cite this ruling as `server.py`, in `linkedin_update_setting` (7401-7402 at 2026-09-19 -- **cite the SYMBOL; 6476-6484 is now `_write_tool`**). That range is now `linkedin_save_job`.** The ruling moved ~900 lines and nothing detected it **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.6), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: a Settings and Privacy data-sharing control reached by nav path only: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: self-only, reversible. |
| N26 | Mentions, tags and collaborations permission | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10), `a522861` **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (who may mention, tag or collaborate with him). |
| N27 | Merge or close duplicate accounts | W | EXCLUDED-RULED | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the census's own written grounds.** Ground two, by act-class and not a new decision: merging or closing a duplicate account closes an account, destruction delete_or_withdraw_anything covers 'at any confirm level'. |
| N28 | Close (delete) account | W | EXCLUDED-RULED | `"/close-accounts"` was added to the forbidden tuple 2026-08-31 precisely because nothing else caught it, plus `delete_or_withdraw_anything` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the census's own written grounds.** Grounds one and two: "/close-accounts" entered the forbidden tuple on 2026-08-31 aimed at this capability, and closing the account is destruction, which delete_or_withdraw_anything covers. It is not a setting awaiting admission. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/close-accounts/` ('Close and delete account' on the settings index, captured 2026-09-23). |
| N29 | Hibernate account | W | EXCLUDED-RULED | `"/hibernate-account"`, added the same day, same reason **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the census's own written grounds.** Ground one: "/hibernate-account" entered the forbidden tuple on 2026-08-31 aimed at this capability, with its argument written beside it -- one of 'the only two on it that are not undoable by re-running the opposite tool'. It is not a setting awaiting admission. **DRAWN, 2026-09-24 (lane Y2 re-harvest):** `/mypreferences/d/hibernate-account/` ('Hibernate account' on the settings index, captured 2026-09-23). |
| N30 | Deceased member -- request account closure | W | EXCLUDED-RULED | `a1380121`, via a Help Center form (`/help/linkedin/ask/ts-rmdmlp`). Not his own act; included because it is an account-lifecycle capability the product offers **RETIRED 2026-09-05, `HELP-CENTER-FORM` (3.3), and it is not his act at all.** This is what somebody ELSE files about HIM, after he dies. A server acting for him cannot perform an act defined as being performed by another person. No measurement needed. REOPENER: LinkedIn making it self-service for a living member. See `_audit/2026-09-05-decide-retire-rulings.md` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.5), on the census's own written grounds.** Ground four, by definition: a1380121 defines this form as filed by another person about a DECEASED member, so a server acting for him while he lives cannot reach it. No measurement is needed, and none is claimed. |
| N31 | Deceased member -- request memorialization | W | EXCLUDED-RULED | same article, different form slug (`/help/linkedin/ask/TS-RDMLP` -- note the case differs) **RETIRED 2026-09-05, `HELP-CENTER-FORM` (3.3), and it is not his act at all.** This is what somebody ELSE files about HIM, after he dies. A server acting for him cannot perform an act defined as being performed by another person. No measurement needed. REOPENER: LinkedIn making it self-service for a living member. See `_audit/2026-09-05-decide-retire-rulings.md` **KEPT OUT OF SCOPE 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s3.5), on the census's own written grounds.** Ground four, by definition: a1380121 defines this form as filed by another person about a DECEASED member, so a server acting for him while he lives cannot reach it. No measurement is needed, and none is claimed. |

### O. Visibility and privacy (23)

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| O1 | Who's Viewed Your Profile list | R | COVERED-PROVEN | 10 rows, 10 distinct names, verified live; 365 days back on Premium Career |
| O2 | De-anonymise a private-mode viewer | R | EXCLUDED-RULED | `PERMANENTLY_FORBIDDEN["deanonymise_a_viewer"]`: *"six of ten profile viewers chose anonymity; the row LinkedIn renders him is the whole of what he is entitled to"* **REOPENER, NAMED 2026-09-20 -- the same one `network.md` **R6** now carries, and it is narrower than it looks.** The PROHIBITION is ours and nothing reopens it: de-anonymising a viewer who chose anonymity stays forbidden. What is contingent is the quoted cell's SECOND clause -- *the row LinkedIn renders is the whole of the entitlement* -- a fact about what LinkedIn draws for this account, of which *six of ten* is a dated reading and not a constant. So: **LinkedIn itself rendering an identity the account is entitled to see reopens this row, because READING WHAT IS DRAWN IS NOT DE-ANONYMISATION.** WHO: a capture of `/analytics/profile-views/`. **An identity recovered by any inference, join or lookup reopens nothing** |
| O3 | WVYP Premium insights and filters | R | COVERED-PROVEN | **FIRED AND BANKED 2026-09-23 23:21 (live lane, session 1).** Fired live by `scripts/_probe_live_lane_session_1.py --only pv_switch,pv_verify` through the shipped `linkedin_who_viewed_me(view_switch="interesting_viewers_verified", show_more_analytics=True)`, attach mode, one page load for the fire and one for the check. **Filters:** the view switch applied one filter, returned the 6 viewers it left (headline 6), and was proven restored -- the view after equals the view before, and a fresh load agreed. **Insights:** the decided "Show more analytics" press was permitted (url unchanged, three counters held), and `more_analytics.revealed_insights` (`linkedin_server/profile_views_more.py`) read both sections it drew -- `highlights` (top location, top industry, top company) and `details` (a "companies" breakdown, 5 entries with their shares) -- with 0 lines unparsed. **The fields whose meaning was checked:** the top company equals the largest entry in the company breakdown, and that entry is unique -- two sections parsed independently, agreeing; every share is a percentage in the range the reader admits; the counters and url held across the press. See `_audit/2026-09-23-live-lane-session-1.md` Entry 8. **The paragraph below was written before the reader existed:** **HALF PROVEN 2026-09-23 23:08 (live lane, session 1); STATE UNCHANGED, AND THE OTHER HALF IS NOW A READER.** One fire of the shipped `linkedin_who_viewed_me(view_switch="interesting_viewers_verified", show_more_analytics=True)`. **The filters half is shown:** the view switch applied one filter, returned the 6 viewers it left, and was proven restored (the view after equals the view before, and a fresh load agreed) -- the same fire that banks `N 134`. **The insights half is REVEALED, NOT READ:** the one decided plain button, "Show more analytics" (`linkedin_server/reveal.py`, on its orchestrator-delegated call), was pressed and PERMITTED -- url unchanged, three counters held -- and it drew two new sections, "Highlights" (7 lines, no numbers) and "Details" (7 lines, 5 of them percentages), 90 more elements in `main`, 49 new text lines. Nothing in the package turns those two sections into data yet, so the row is not banked on them. First past the boundary: a READER for the two sections, built offline from the live capture (a raw page kept only in the lane's gitignored state directory, by the lane's rule; the session document names it), owing a shape and a redaction rule for rare values because it describes other people in aggregate. See `_audit/2026-09-23-live-lane-session-1.md` Entry 7. no tool, no reason **"NO REASON" IS NO LONGER TRUE, AND THE REASON IS THE SAME PRESS `N 134` WAITS ON. STATE UNCHANGED.** Wave `what-is-reachable-now`, 2026-09-21. This row and `N 134` share one blocker (`ANALYTICS-CONTROLS-UNPRESSED`) and one surface, `/analytics/profile-views/`, which `linkedin_who_viewed_me` already loads. The disclosing press on that surface was ruled PERMITTED 2026-09-19 (`DISCLOSING-PRESS-PERMITTED`), three of its four conditions settle offline against the shipped gate (`check_address` admitted, `check_shape('[aria-expanded]')` legal, `check_basis` `structural` and complete), and the press was TAKEN here with the witness that did not exist in September's first run: `"witness": {"disclosed": true, "moved": ["expanded_true", "menus"]}`. **So the panel opens, and this row is no longer blocked on a ruling, on the mechanism, or on whether a press does anything.** It is blocked on ONE unbuilt artifact: a NAME-FREE SHAPER for the disclosed content. That is not a formality on this surface -- the panel is made of OTHER PEOPLE, `linkedin_who_viewed_me` already states the resolution any such shaper must hold (*numbers, filter labels, the chart's own sentence and COUNTS of page regions, and nothing else*), and `textContent` merges an `aria-hidden` visible copy with a `visually-hidden` screen-reader copy that on people surfaces CARRIES THE EMPLOYER NAME. Nothing in `dom.py` or `shape.py` names notable or interesting viewers, `press.disclose` has no caller in the package, and `dom.py`'s `# readonly-ok` waiver budget is AT its cap of 22 with 0 remaining -- so the shaper must reuse an already-declared in-page script or the cap moves and needs its own argument. Preflight `_audit/_census/_slice-analytics-press-preflight.md`; press evidence `_audit/2026-09-21-what-is-reachable-now.md`  **MEASURED LIVE 2026-09-23 AND STILL GAP -- THE 2026-09-21 WITNESS PRESSED THE NAV, AND THE INSIGHTS SIT BEHIND A CONTROL NO SANCTIONED PRESS REACHES.** Wave `readers-four-rows`. A zero-press load of this page (18:57 IST) put `[aria-expanded]` page-wide index 0 in the header's nav, classifying as the account menu, and index 1 as the business menu; the three filter pills sit in `main`. So the press behind *"the panel opens"* (2026-09-21, `index=0` page-wide) landed on page chrome, and that sentence is not evidence about any analytics panel. The reader was then built and fired once (19:10 IST): `linkedin_who_viewed_me(open_filter_menus=True)` opens each pill through `press.disclose` (scope `main`, reading `profile_views_filter_menu`), and all three presses were PERMITTED with closure verified and the witness moving -- but every pill is a FILTER FORM carrying an apply control, so using a filter is a submission the ruling refuses by name, and **"Show more analytics" is drawn carrying neither sanctioned attribute**, so the Premium insights behind it are a shape the ruling cannot reach (condition 2, terminal by this route). What would move it: a ruling on a third sanctioned shape for a plain disclosing button, with its measured blast radius. On the same load the shipped `insights.filters` read 3, after a fix that stops it publishing a closed dialog's two labels. Bucket-3 gate PRESS-PERMITTED -> PRESS. Evidence `_audit/2026-09-23-readers-four-rows.md` sections 2.2 and 3.0.1 |
| O4 | Profile viewing options (his own browsing mode) | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10). `a568195`: "Your name and headline" / "Private profile characteristics" / "Private mode" **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (what members see when he views them). |
| O5 | Public profile badge creation | W | GAP | `/badges/profile/create` -- caught by no substring, on no allowlist pattern, named nowhere in this repo  **ALL THREE CLAUSES ARE FALSE AND THE ROW MOVES ON THE CORRECTION, 2026-09-20. EXCLUDED-RULED.** `/create` entered the forbidden substring tuple in `readonly.py` on 2026-09-03 and THIS ADDRESS IS NAMED VERBATIM in the entry's own comment: *"THE VERB THAT WAS NEVER ON THIS LIST. The four entries above are the destructive verbs and they have been here from the start; the verb that MAKES something has not. `/badges/profile/create` is the member that exposed it, and the class is every creation surface on the account."* **CO-LOCATION UNDER A RULED ADDRESS**, and the sharpest instance in this slice of a blocker cell asserting an absence the shipped boundary contradicts. **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s2.4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: refused by a class filter written for a different purpose: "/create", added 2026-09-03 for every creation surface on the account, catches /badges/profile/create. INCIDENTAL-CAPTURE-IS-NOT-A-RULING (2026-09-19) names this address and rules its row GAP. |
| O6-O20 | fifteen further visibility toggles: contact-info and email visibility, connection-list visibility, who can see members you follow, who can follow you, representing your organization ("Profile information on content"), Page owners exporting your data, discoverability by email, discoverability by phone, profile discovery off LinkedIn, off-LinkedIn visibility, manage active status, share profile updates with your network, visibility of shared posts, sharing public posts on and off LinkedIn, Page visit visibility | W | GAP | settings family; section 6 **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (fifteen visibility toggles). |
| O21 | Block / unblock a member | W | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10); independently an act on another member's relationship **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a toggle that changes what OTHER members see of him or can do toward him (its block half stays refused by delete_or_withdraw_anything, as N 141 is). |
| O22 | See a list of who you blocked | R | GAP | settings family -- ruling: `linkedin_update_setting` (`linkedin_server/server.py`), *"a setting is admitted by name or not at all"*; the SAME ruling as `network.md` **R11** and messaging **MESSAGING-SETTINGS** (3.10) **RETURNED TO GAP 2026-09-23 BY LANE R (2026-09-23-exclusion-returns.md s4), on the orchestrator's delegated call, not a new ruling.** BLOCKER, NAMED: awaiting admission by name: the operator's settings ruling (2026-08-31, ONE-NAMED-SETTINGS-PAGE-AT-A-TIME) admits ONE named settings page at a time and names none of these, and "/mypreferences/d/categories/", "/psettings/", "/settings/" and "settings" hold the family closed until he names one. It admits by name; it rules none out. Class: a pure read. |
| O23 | Whether a photo, banner or headline change notifies the network | R | GAP | the `update_profile_field` docstring states this as an UNMEASURED cost: *"LinkedIn notifies a network about some profile changes, which this server has not measured and would not control."* A named unknown, not a considered exclusion |

### P-R. Data privacy, advertising data, notification settings (45)

Forty-four of the forty-five resolve to a `/psettings/...`,
`/mypreferences/d/categories/...` or `/mypreferences/d/settings/...` address and
are EXCLUDED-RULED by the settings-family ruling (section 6). **LANE R, 2026-09-24: THESE 44 WERE NOT RE-DECIDED, AND THIS SAYS WHY.** Lane R reads the settings-family ruling as admitting a page by name and ruling none out, which is how 34 of the 35 settings rows went back to GAP (`2026-09-23-exclusion-returns.md` s4 and its Integration 2026-09-24 section). These 44 carry no row ids, the bullets below group them rather than list them, and at least one (Delete Data) is a deletion, which ground two keeps by act-class; so they cannot be decided one capability at a time until they are itemized into rows. Until then `scripts/census_completion.py` counts them with `P1`, outside the achievable capability surface. The enumeration is
kept at LinkedIn's own published granularity:

* **The four data rights**, named verbatim by `a1340649`: Delete Data; Access and
  Download Data; Change or Correct Data; Restrict or Object to the use of Data.
* **Download your data, two tiers**: 37 categories "within 10 minutes", 30
  categories "within 48 hours", download available for 72 hours. (`a1339364`
  states both "10 minutes / 48 hours" in the lists and "minutes / 24 hours" in
  its prose; the discrepancy is on the page and is not reconciled here.)
* **The seven notification categories**, named verbatim by `a1341821`:
  Invitations and messages; Jobs and opportunities; Activity in your network;
  Activity that involves you; News and articles; Offers and tips from LinkedIn;
  Updates from events. Plus channel controls (in-app / push / email) and the four
  connection-notification toggles.
* **Eight advertising controls**: profile data, activity and inferred data,
  off-LinkedIn data, ads personalization, demographic data, research invitations,
  the ads category page, cookie preferences.
* Search history, salary data, invitations-to-connect audience, research
  invitations, InMail opt-out, read receipts and typing indicators, message
  nudges, harmful message detection, job-post email notifications.

The one that is not settings-family:

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| P1 | Mark notifications read | W | EXCLUDED-RULED | `PERMANENTLY_FORBIDDEN["mark_notifications_read"]`, on three independent grounds, the third being that *"the read tool ALREADY has the full effect -- opening the page clears the badge -- so a write here could only ever run after its own consequence had landed"* |

(`P1` is counted in the 45; the settings-family share of this block is 44.)

### S. Recovered 2026-09-24 from what LinkedIn drew (10) -- the completeness admission

**ADDED BY LANE Y2 (`_audit/2026-09-24-lane-y2-admission.md`).** Each row is a
capability LinkedIn DREW on a captured page (`_audit/2026-09-23-completeness-probe.md`)
that no row of this slice carried in words or in synonyms. **THE COUNTS IN SECTION 1
ARE NOT REWRITTEN**: rows S1-S10 enter the denominator here, all GAP. Read rows carry
a line in `_audit/_census/read-addresses.tsv`, write rows one in
`_audit/_census/write-classes.tsv`. S1-S5 and S8-S10 come from the 71 captures lane Y
adjudicated (S8-S10 after the lane's cold verification showed its first reading too
lenient, section 10 of its record); S6 and S7 from the lane's one re-harvest, over the
live lane's capture of the settings index on 2026-09-23.

| # | capability | R/W | state | evidence / blocker |
|---|---|---|---|---|
| S1 | Featured section items -- read what the profile features, on its details page | R | GAP | Drawn: `/in/<me>/details/featured/`, the Featured card's 'View featured detail screen' link on the member's own profile (captured 2026-09-20). G3-G5 are the Featured WRITES; no row read it. **New blocker: `FEATURED-DETAILS-SURFACE`** -- the address is refused (no pattern, measured) while its sibling details pages are read, so it is one pattern and a reader. |
| S2 | Rewrite profile sections with LinkedIn's AI assist ("Enhance profile", Premium) | W | GAP | Drawn: the control 'Enhance profile' (the Premium hub and the search-appearances page) and its overlay `/in/<me>/overlay/enhance/` (captured 2026-09-20). A2 and D2 are the plain headline and About edits; no row carried the AI assist. **New blocker: `AI-PROFILE-ASSIST`** -- the overlay is refused (no pattern, measured) and has never been opened, and what it writes lands on the public profile. |
| S3 | See where a skill is used on the profile -- the experiences, education and companies tied to one skill | R | GAP | Drawn: `/in/<me>/skill-insights/<skill>/`, linked from each card on the member's own skills page as '<n> experiences at <company> and <n> other company' (tracked fixture `tests/fixtures/profile_skills.html`). E1 reads the skill list, not this; D6 is the write that ties a skill to a position. **New blocker: `SKILL-INSIGHTS-SURFACE`** -- refused (no pattern, measured); no reader. |
| S4 | Add a custom button to the profile intro (Premium) | W | GAP | Drawn: the control 'Add custom button' on the member's own top card (tracked fixture `tests/fixtures/profile_topcard.html`). No intro row names it, and what the button can offer was not read. **New blocker: `CUSTOM-BUTTON-EDITOR`** -- the top card's page is admitted; the editor the control opens has never been captured, and the write lands on the public profile. |
| S5 | Profile engagement insights: impressions on one experience entry, and recent clicks on a profile section | R | GAP | Drawn on `/analytics/search-appearances/` (captured 2026-09-20) as two cards, 'You have <n> impressions on your experience at <X>' and 'Someone recently clicked on your <X>' (the section it named was the intro), each with a 'View' link. G7 reads that page's search appearances; no row carried these per-section numbers. **Blocker `PARSER-ON-A-LOADED-PAGE`**: the page is admitted and `linkedin_search_appearances` already loads it, so this is a parser for two cards -- and the company the first card names is an employer, which a reader must not publish. |
| S6 | Personal demographic information, a page of the account settings | W | GAP | Drawn: `/mypreferences/d/demographic-info-copy/`, 'Personal demographic info' on the settings index (the live lane's capture of `/mypreferences/d/`, 2026-09-23). Block N names the account settings one by one and not this one; what the page asks was not read. **Blocker: a settings page with no pattern** (refused, measured); the settings-family ruling would reach it if applied, and until then `EXCLUDED-RULED-ADMISSION` keeps the row GAP with its blocker named. |
| S7 | Choose the content language, a settings page apart from the interface language | W | GAP | Drawn: `/mypreferences/d/language-for-translation/`, 'Content language' on the settings index (captured 2026-09-23); N15 is the interface language, a separate page on the same index. **Blocker: a settings page with no pattern** (refused, measured), as S6. |
| S8 | Choose key skills so recruiters find the profile (Premium) | W | GAP | Drawn: `/premium/profile-key-skills/`, a card on the search-appearances page reading 'Add key skills ... helps you appear in recruiter searches' (captured 2026-09-20). E2 adds and removes skills in the profile editor; whether this Premium page is that act or a separate key-skills list was never read, and the lane's cold verifier read it as separate. **New blocker: `KEY-SKILLS-EDITOR`** -- the address is admitted (premium-four, 2026-09-20) and has never been loaded; no tool writes it, and the write lands on the public profile. |
| S9 | Change the Premium plan | W | GAP | Drawn: 'Change plan' at `/premium/switcher/` on the Premium hub (captured 2026-09-20). N22 is the subscriptions and payments settings, a read; changing the plan is a purchase. **New blocker: `PREMIUM-PLAN-SWITCHER`** -- refused (no pattern, measured), and the act spends money, so a live proof would need the operator. |
| S10 | Post a job as a hirer (the free job-posting flow) | W | GAP | Drawn: 'Post a job' and 'Post a free job' at `/talent/job-posting-redirect/`, in the jobs nav and a posting's aside (four surfaces). J1 is the #Hiring signal; the jobs slice's section 3 puts the hirer side out of its own scope by design -- a scope call, not a registered ruling -- so no row held it. **New blocker: `JOB-POSTING-FLOW`** -- refused (no pattern, measured), and a posting is public and names an employer. |

---

## 5. WHAT THE 79 GAPS WOULD TAKE

Reversibility matters more than difficulty, because the asset is his professional
identity. Counts sum to 79, and every one was counted off the table above rather
than asserted.

**5.1 -- Controls inside the ONE editor this server can already open (9).**
`A9, A14, A15, A22, A25-A29`.
`/in/me/edit/intro/` is admitted; these controls were simply not among the 17 read
on 2026-09-02. Shape: open the panel (`Edit contact info` is itself one of the 17)
and re-read. **No boundary moves and no new permission is needed** -- this is the
one group blocked by a missing observation rather than a standing refusal. Write,
with a read as its precondition; reversible only if `profile_editor_values` is
extended to the panel first.

**5.2 -- `/public-profile/settings` and its neighbours (6).** `B4, C2-C6`.
Not below the settings index, so the settings ruling does not reach it; caught by
no denylist entry; named nowhere in this repo. Shape: one allowlist pattern. Write.
**C5 is irreversible in audience** -- once a profile is indexed by a search engine,
un-indexing it does not un-cache it.

**5.3 -- Surfaces nobody in this repo has ever named (64).**
`A23, A24, B7-B10, C8, D24, D25, D27-D29, E6, E7, F1, F2, F6-F9, G2, G6, G7,
H1-H11, I13-I16, J1-J4, K8-K10, L1-L8, M11, M12, N12-N14, N25, N30, N31, O3, O5,
O23`.
Shape: a surface census first, then a spec, then a gate -- the full path every
existing write took. Mixed read and write. The irreversible-in-audience ones, named
rather than lumped: **F2 and H9 send a request to a real person**; **H1-H8 publish
a public Service Page under his name**; **J1-J3 tell a network he is hiring**;
**L3 and L5 broadcast**; **I14 tells a company's recruiters he wants to work
there**; **N30 and N31 act on somebody's account after their death.** The safely
reversible ones are all reads: `C8, D25, D28, E7, F1, G6, G7, H11, J4, K8, K10, L1,
L2, L4, L7, L8, M12, N13, O3, O23`.

**The cheapest work in this census is not in this section at all.** `D4` and `D10`
-- reading his own Experience and Education entries -- are COVERED-CANNOT-DELIVER,
because `my_profile` already declares both fields, already hands back both urls,
and both urls are **already on the read allowlist**. No boundary moves, no new
permission, one harvest function of the same shape as the skills read that works
today. That is the single highest-value buildable item this slice found.

## 6. THREE FAMILY RULINGS PRODUCE 123 OF THE 150 EXCLUSIONS

**The settings family -- 93 rows.** Every settings-index page in `C7, E8, K1-K6,
M8-M10, N4-N11, N15-N24, N26-N27, O4, O6-O22` and 44 of the 45 P-R rows. From
`linkedin_update_setting`:

> *"ONE SETTING IS WRITABLE, AND ASKING ABOUT ANY OTHER LOADS NOTHING. The read
> allowlist admits exactly one page below the settings index, admitted BY NAME on
> the operator's ruling. ... READ THIS BEFORE ASKING FOR THE FAMILY TO BE OPENED.
> `Close and delete account` and `Hibernate account` are addresses in it. A
> permission written for the FAMILY would carry those with it, which is why a
> setting is admitted by name or not at all -- and it is why this tool shipping
> does NOT mean the next setting is a small step."*

**The `/edit/` family -- 23 rows.** Every profile-section editor: `D2, D5-D9,
D11-D23, E2, E3, G4, G5`. Those addresses are measured in this repo's own fixtures
(`/in/<member>/details/skills/edit/forms/<n>/`) and the argument is written beside
the one exemption that was granted:

> *"Narrowing was refused: `/edit/` must keep refusing the whole rest of that
> family, on his own profile and on everybody else's, and buying one page by
> weakening a standing refusal is the trade this module exists to make somebody
> argue for."*

**`/jobs/application` -- 7 rows.** `M1-M7`, the whole Job Application Settings
page, refused by the first entry on the forbidden tuple.

All three rulings are real, argued and correct, and this census disputes none of
them. What it insists on is the arithmetic: **not one of those 123 capabilities has
been considered on its own merits.** A family ruling says the family stays shut; it
does not say that "who can see your connections", "read my verification badges" or
"add a certification to my profile" was weighed and declined. Asked tomorrow which
of them this server should reach, the honest answer is that nobody has an opinion
about any of the 123. Exactly three addresses have ever been let out of these
families, each admitted BY NAME on the operator's own ruling: dark mode, the intro
editor, and the skills details page. **SINCE 2026-09-23 THE THREE FAMILIES ARE BLOCKERS NAMED ON EACH ROW, NOT EXCLUSIONS** (lane R, `2026-09-23-exclusion-returns.md`): each returned row's own cell names its family's entry as the blocker, under INCIDENTAL-CAPTURE-IS-NOT-A-RULING and the settings ruling's admit-by-name reading. Measured on the merged tree, 2026-09-24: of the 35 settings rows named above, 34 are GAP; all 23 `/edit/` rows are GAP; 6 of the 7 `/jobs/application` rows are GAP; `N27` and `M3` stay EXCLUDED-RULED on ground two, each a deletion by act-class. This slice holds 15 EXCLUDED-RULED rows in all. The split below is the census as it stood when this section was written.

Split of the 150:

    settings-family ruling                                           93
    /edit/ family ruling                                             23
    set_open_to_work spec, per field (I2-I12, B6)                    12
    PERMANENTLY_FORBIDDEN named keys
      (E4, E5, F3, F4, F5, K7, N28, N29, O2, P1)                     10
    /jobs/application forbidden entry (M1-M7)                         7
    set_input_files, test-enforced (B2, B3, B5, G3)                    4
      ^ 2026-09-20: the ban is NOT package-wide -- one sanctioned call site
        since 2026-09-04. The four rows are unchanged and still correct; see
        B2 and finding 7.11. AND THE MEMBERSHIP IS SOFTER THAN THIS LINE
        READS: resolved through the pointer graph, only B3 inherits B2's
        argument. B5 backreferences B4 and G3 backreferences G1, so two of
        these four rest on a DIFFERENT cell than this roll-up attributes to
        them. Recorded rather than re-pointed -- moving a backreference is a
        change to what a row means, not a typo fix.
    my_profile's own declaration (D26)                                1
    ---------------------------------------------------------------
                                                                    150

## 7. FINDINGS

### 7.1 -- SIX live `/mypreferences/d/<name>` addresses slip past the denylist, and one of them is the password-change page

`readonly._FORBIDDEN_URL_SUBSTRINGS` gained `"/close-accounts"` and
`"/hibernate-account"` on 2026-08-31, with this reasoning in the source:

> *"The settings audit assumed 'Close and delete account' and 'Hibernate account'
> ... were covered by the `/mypreferences/d/categories/` entry three lines up.
> THEY ARE NOT. ... The only thing that had ever refused them was the anchored
> allowlist. ... for the two worst addresses on the account there was no second
> gate at all."*

The Help Center names **six more addresses of exactly that shape**. Each was run
through all 23 forbidden substrings programmatically; none matches:

    /mypreferences/d/change-password                (a1379143)
    /mypreferences/d/two-factor-authentication      (a1381088)
    /mypreferences/d/verifications                  (a1359065)
    /mypreferences/d/job-application-accounts       (a507642)
    /mypreferences/d/member-cookies                 (a1336669)
    /mypreferences/d/profile-visibility-for-partners (a518980)

Four more settings-shaped addresses are also uncaught: `/public-profile/settings`,
`/uas/login`, `/badges/profile/create`, and **`/mwlite/settings` -- an entire
parallel mobile-web settings tree the denylist does not touch at all.**

**AND ONE SETTING HAS TWO SPELLINGS, ONE CAUGHT AND ONE NOT.** LinkedIn's own Help
corpus prints `profile-visibility-for-partners` both ways, for what the articles
describe as the same setting:

    /mypreferences/d/profile-visibility-for-partners           (a518980)  UNCAUGHT
    /mypreferences/d/settings/profile-visibility-for-partners  (a548106)  caught by "/settings/"

This repo has already written the rule that covers this, about the messaging
composer: *"A ruling that one spelling cannot express is not a ruling, it is a
spelling filter."* The same shape has recurred on the settings family.

**This changes no state in this census.** The capability-level ruling in
`update_setting` excludes these regardless of spelling. What is missing is the
second, independent gate the denylist is documented to be -- and the 2026-08-31
fix was per-address where the class has at least ten more members.

### 7.2 -- The comment on `"/settings/"` says it catches nothing current. It catches five live addresses.

The source says:

> *"LinkedIn moved its settings to `/mypreferences/d/`, and the legacy address is
> `/psettings/` ... The only address the old entry ever caught is a `/settings/`
> LinkedIn no longer serves."*

The measurement behind that sentence was taken on the two INDEX urls. Current Help
articles name five per-setting addresses in a third shape the entry does catch:
`discover-me-by-email-address`, `discover-me-by-phone-number`,
`profile-visibility-for-partners`, `data-export-by-page-admins`,
`data-for-ai-improvement`, all under `/mypreferences/d/settings/`. `"/settings/"`
is doing real work today; only the sentence needs changing.
EVIDENCE CLASS: DERIVED -- from Help Center reads (`a548106` read in full,
per-string existence probes), not from a live LinkedIn page load, which this slice
was forbidden.

### 7.3 -- The Open To Work spec and LinkedIn agree on a count and disagree on a member

`set_open_to_work.audiences` enumerates `recruiters only`, `all linkedin members`
and `off`; the spec states that only ONE has ever been observed on this account.
Help `a507508` names three too -- **All LinkedIn Members, Recruiters only, and
"Visible only to you"** -- with no "off". So the reader's third string may never
appear and a fourth it has never met may. The design already handles this
correctly (*"the reader recognises the audience string it has met and refuses to
interpret one it has not"*), so it cannot act wrongly; knowing the name is cheaper
than meeting it.

### 7.4 -- `endorse_or_recommend` names recommendations and argues only about endorsements

The key covers two acts. The reason under it, rewritten 2026-08-30, is entirely a
measurement of ENDORSE controls -- *"zero endorse controls across 13 tracked
fixtures ... zero among the 222 controls read live"* -- and says nothing about
recommendations, whose controls have never been counted. `F3` and `F4` are
excluded by a key that names them and a reason that does not reach them. The old
reason that DID reach them (*"a statement ABOUT ANOTHER PERSON, which is not his
to automate"*) was retired as policy on 2026-08-25.

### 7.5 -- The best-verified write in the package has never successfully written

`linkedin_update_profile_field` is described, in the live server's own MCP
instructions, as *"the only one here that can verify its own outcome by reading the
field back"*. Its complete firing record: shipped 2026-09-02 in `a540461`, driven
end to end, `NAVIGATIONS ATTEMPTED: []`, repaired the same day in `ea5354d`, never
driven again.

**The 2026-09-02 ship-and-repair is still not written into `_audit/` at all.** It
lives in a commit message, a test docstring
(`tests/test_a_performable_action_can_reach_its_control.py:25`), and the untracked
`_TEAM_LEAD_SUCCESSOR_BRIEF.md`. Anyone reading `_audit/` alone finds only the
pre-ship refusal at `_audit/2026-08-31-linkedin-perform.md:3436`, which says the
action REFUSES -- true when written, false since.

### 7.6 -- The intro editor's control set changed completely in two days

23 controls on 2026-08-31, 17 on 2026-09-02, and *"a different set"*: `First name*`
and `Last name*` lost their accessible names entirely, `School*` and `Month`
vanished, `Pronouns` and `Education` appeared, and six new controls including
`Write with AI` arrived. **Every COVERED-UNFIRED and COVERED-CANNOT-DELIVER row in
block A rests on a reading that is now a day old on a surface with a measured
two-day half-life.**

### 7.7 -- Two of this slice's proven reads LAND outside the read boundary

Recorded by `_audit/2026-08-31-linkedin-finish.md:275-279` rather than found here,
and repeated because both landings are in this slice:

* `my_profile` and census `profile` request `/in/me/` and land on
  `/in/<member>/?isSelfProfile=true`, which **FAILS the allowlist**;
* census `settings` requests `/mypreferences/d/` and lands on
  `/mypreferences/d/categories/account`, which **HITS a forbidden substring**.

`assert_read_url` gates the REQUESTED url only; the landed url is never
re-checked. The finish audit measures the settings-page exposure as NIL. So the
`categories/` entry that helps exclude 92 rows is, on one surface, a door this
server is already standing behind.

### 7.8 -- The repo's measurement of the "Open to" menu is CONFIRMED by LinkedIn's documentation

A census of all five profile captures measured the `Open to` button's menu
resolving to exactly three items -- Hiring, Providing services, Finding volunteer
opportunities -- and concluded the entry LinkedIn would have used for job-seeking
*"is absent precisely BECAUSE the setting is already on"*. LinkedIn documents four
items (`a547248` names three: finding a new job, hiring, providing services;
`a6862361` adds finding volunteer opportunities). **Four documented minus the one
already enabled is exactly the three measured.** The repo's inference was right and
is now corroborated from outside.

### 7.9 -- The contact-info panel has six fields and no address field

`a565128` enumerates exactly six: LinkedIn profile link, email address, phone
number, website, instant messenger accounts, birthday. **No address field and no
Twitter/X field.** Revision 1 of this census listed "address" as a contact-info
capability; it is removed. Per-field visibility is documented only for email,
website, birth date and last name (`a545600`) -- **phone and instant messenger have
no documented visibility rule at all.**

### 7.10 -- Eight profile sections are undocumented by LinkedIn itself

Courses, Projects, Publications, Patents, Test scores, Languages, Organizations and
Volunteer experience were each searched BY PRODUCT NAME and each is absent from a
Profile topic index that rendered 55 articles. LinkedIn documents them only as menu
items in `a540837` and one-line descriptions in `a564064`. **The form-field
granularity does not exist on either side of the ledger**, so rows D14-D22 are
counted at section granularity and their sub-fields are uncounted for a reason that
is not this census's fault and cannot be fixed by more searching.

---

### 7.11 -- SOME OF THIS REPOSITORY'S SHARPEST RULINGS LIVE IN TEST DOCSTRINGS, AND THIS CENSUS ALMOST MISSED ONE

Revision 1 scored profile-photo and banner upload as GAP -- "nothing in this repo
has ever considered photo upload". **That was wrong**, and the correction came from
the lead rather than from this census's own method.
`tests/test_readonly.py` carries a complete, argued, test-enforced ban in
`test_exactly_one_place_in_this_package_can_reach_a_file_input`: it scans every
module for `set_input_files`, PLANTS a mutation to prove the pattern still bites,
and pins the kind's membership of `SANCTIONED_MUTATIONS` by name. Its docstring
states the ground:

**TWO REPAIRS TO THE SENTENCE ABOVE, 2026-09-20, and the second is the one that
matters.** (1) The citation was `tests/test_readonly.py:306-341`, which today
lands inside `test_the_partition_conserves_every_hit` -- a different test about
a different property. A stale line range does not dangle; it hands the reader a
PLAUSIBLE WRONG ANSWER and stops them looking. It is now cited by SYMBOL.
(2) The sentence read *"asserts the kind ABSENT from `SANCTIONED_MUTATIONS` by
name"*, and **since 2026-09-04 the test asserts the exact inverse** -- that the
kind IS on the list, exactly once. The test's own docstring says so: *"the
literal inverse of the line this test used to end on."*

**THE BAN IS NARROWER THAN THIS SECTION SAID, AND THE ROWS STILL DO NOT MOVE.**
`readonly.SANCTIONED_MUTATIONS` holds 7 entries, the seventh being
`('linkedin_server/writes.py', 'perform', 'set_input_files')`. What keeps the
capability closed is elsewhere and was measured by import: the upload verb is
absent from `writes.PERFORMABLE` (12 actions), so no caller can name it, and
`writes.writes_enabled()` is False. **The verb is permitted at exactly one
switched-off call site while the capability stays closed.**

**AND THE OPEN QUESTION BELOW IS STILL OPEN.** The docstring's ground is that
the operator *"has never been asked"*. **No operator answer is recorded anywhere
in this corpus**, so the 2026-09-04 sanction is a dated fact about the code and
must not be read as an answer to it. Full reading:
`_audit/2026-09-20-the-sanctioned-seventh.md`.

> *"UPLOADING IS A DIFFERENT CAPABILITY FROM TYPING. A fill puts his words in a
> box; a file input puts a FILE from this machine into somebody else's inbox,
> chosen by a path string. Nothing in this package should be one edit away from
> that, and the operator has never been asked about it."*

That is EXCLUDED-RULED holding an explicit open question, which is a different
thing from a GAP: somebody decided, wrote it down, made it fail-fast, and named
what remains unanswered.

**THE METHOD DEFECT IS THE FINDING.** This census read `_audit/` (54 files) and the
seven `linkedin_server/` modules. It did NOT read `tests/` -- **74 files, 55,185
lines** -- and a ruling that lives only in a test docstring scores as
never-considered. So **GAP is over-counted and EXCLUDED-RULED under-counted, by an
amount nobody has measured, and in the OPPOSITE DIRECTION to the topic-page
hazard.** The two error sources push apart, which is worse than either alone,
because they do not cancel and neither is bounded.

**WHAT WAS DONE ABOUT IT, and what was not.** After the correction, `tests/` was
swept for ruling-shaped phrases (*"never been asked"*, *"must never"*, *"may not"*,
*"deliberately not"*, *"not a capability"*) and for every subject noun in this
slice. That sweep found the upload ban and found **no test-level ruling** for
recommendations, newsletters, verifications, Service Pages, `/public-profile/`,
or the `details/experience` and `details/education` reads -- zero keyword hits for
each, so those GAPs and D4/D10's CANNOT-DELIVER stand. **A keyword sweep is not a
read of 55,185 lines.** The residual over-count is real, unmeasured, and named here
rather than quietly absorbed.

## 8. THE SET-ASIDE BUCKETS, ITEMISED

**NOT-ENTITLED (13)** -- Premium Career, in India. He cannot do these on LinkedIn
directly either: custom cover image (USA only); cover-image slideshow (Premium
Business / Sales Nav / Recruiter); custom profile CTA button (same); "increase
button visibility" (same); website link in the intro section (Premium Business);
media on a Service Page (Premium Business); Services Showcase with ratings (Premium
Business); "Request services" button across surfaces (Premium); identity
verification via CLEAR (US / Canada / Mexico); workplace verification via an active
Recruiter licence; voluntary self-identification demographic data (US only);
LinkedIn Page notification settings (Page admin); manage Page visit visibility
settings (Page admin).

**RETIRED OR PAUSED BY LINKEDIN (7)** -- measured, not assumed: creator mode toggle
(removed March 2024, `a5999182`; old articles `a522537` and `a524035` both 404);
profile hashtags / topics (removed February 2024); skill assessments (badges removed
from profiles during 2024, `a1690529`); Community Top Voice gold badge (retired
8 October 2024, `a6245087`); Professional sources (removed, data deleted);
educational-institution verification (paused for new applicants); Profinder Pro.

**OWNED BY A SIBLING CENSUS SLICE (14)** -- follow / unfollow a company; follow /
unfollow a person; connections and invitations; the messaging composer and InMail
send; the InMail credit balance as a spend gate; reading the notifications list; job
alerts; job tracker; saved jobs; the apply flow; groups followed; newsletters
subscribed; schools followed; influencers followed. The profile-rendered Interests
section is a genuine boundary between this slice and the network slice and neither
claims it.

**HELP-CENTER DOCUMENTATION (101)** -- troubleshooting pages, FAQs and "how X works"
explainers across the three primary walks. Dropped before mapping so the denominator
counts things he can DO.

**RECOVERY-PASS CONFIRMATIONS (40)** -- of the 60 rows the two product-name passes
returned, 20 were capabilities not already in the denominator and are in the table
above; the other 40 confirmed a row already counted, added field-level detail to one,
or were documentation.

---

## 9. WHAT REVISION 2 CHANGED

| | rev 1 | rev 2 |
|---|---|---|
| mapped capabilities | 240 | 260 |
| COVERED-PROVEN | 19 | 19 |
| COVERED-UNFIRED | 7 | 7 |
| COVERED-CANNOT-DELIVER | -- | 5 |
| EXCLUDED-RULED | 105 | 150 |
| GAP | 109 | 79 |
| uncaught denylist addresses (finding 7.1) | 3 | 6, plus 4 more shapes |
| sources read for rulings | `_audit/` + modules | + a keyword sweep of `tests/` |

Moves: `A2, A4, A6` EXCLUDED-RULED to COVERED-CANNOT-DELIVER (a tool exists and an
instrument fired live and measured it unable to aim). `D4, D10` GAP to
COVERED-CANNOT-DELIVER (`my_profile` declares the field, fires, and returns `None`
by construction). `K1-K6, M9, N4` GAP to EXCLUDED-RULED, on the ruling that the
settings-family argument is capability-level and reaches every page below the
settings index whatever its URL spelling -- a correction to rev 1, which had
assigned those by denylist reachability instead. **Open To Work stays
EXCLUDED-RULED and was NOT promoted to the fifth state**: the fifth state needs a
tool that exists and has fired, and `set_open_to_work` has no tool registered at
all (section 3). `B2, B3, B5, G3` GAP to EXCLUDED-RULED on the test-enforced `set_input_files` ban
(finding 7.11). `D2, D5-D9, D11-D23, E2, E3, G4, G5` -- 23 rows -- GAP to
EXCLUDED-RULED on the `/edit/` family ruling, applied for consistency with the
settings family: rev 1 had treated one argued family refusal as a decision and the
other as a mere mechanism. Twenty new capabilities from the recovery passes.

**Net: GAP fell from 109 to 79, and every row that left it left because a WRITTEN
DECISION was found, never because a capability was.** The actionable list got
shorter and sharper; the server did not get one bit more capable.

The rev-2 split was reconciled against the table row by row rather than carried
forward: an earlier draft of this revision said 149/80 where the rows say 150/79.
The table is the authority and the summary was corrected to it.

---

## 10. WHICH HELP CENTER AREAS WERE WALKED, AND WHICH WERE NOT

**THE REPORTED HAZARD DID NOT REPRODUCE IN THIS SLICE.** Every topic index relied
on here rendered a full article list, checked directly: `a64` Your Profile 55,
`a51` Basics 26, `a153003` Search and Apply for Jobs 49, `a149001` Settings 18,
`a65` Data and Privacy 40. The `0 articles` failure looks specific to Events
(`a150003`) and LinkedIn Live (`a151003`). The recovery passes still added 20
capabilities -- but from products that have **no topic home at all** (Sign in &
security, Visibility, Notifications and Advertising data have no standalone tree),
not from an index that rendered empty. Both failure modes undercount; only the
second is invisible, and this slice did not hit it.

**WALKED** -- five passes, 267 tool calls in the primary walks plus 111 in recovery,
public `linkedin.com/help/linkedin/...` pages only, no login, no account-bearing
page: topic `a64` plus hub `a564064` and `a540837`; topics `a149001`, `a65`,
`a151002`; and product-name searches across the profile sections, the four
index-less settings sections, data rights, and the job-seeking identity surfaces.

**HOLES CLOSED SINCE REVISION 1:**

* the seven notification category names -- found in `a1341821`, an article neither
  earlier-read article linked to. **The names hypothesised in the brief ("Searching
  for a job", "Network catch-up", "Groups", "Pages", "Events") appear only in
  third-party blogs, never in LinkedIn Help.** The earlier refusal to invent them
  was correct.
* "Representing your organization and interests" -- `a566358`, control named
  "Profile information on content".
* "Content language" -- **positively established as NOT EXISTING.** `a521833` names
  one language control and says member content displays in the language it was
  written in, offering only per-item translation.
* the contact-info field list -- `a565128`, a different article from the `a570132`
  the first walk checked.
* the full "Add profile section" menu -- 19 items in three groups.
* the eight undocumented profile sections -- now a MEASURED ABSENCE rather than an
  unwalked area (finding 7.10).

**STILL NOT REACHED -- holes in the denominator, not zeroes:**

1. **`a567169` "Manage Licenses & certifications" is HTTP 404 on all three
   verticals tried**, and no replacement exists in the 55-article Profile index. Its
   content survives only as a cached search snippet, so D13's form fields are
   UNVERIFIED.
2. **Pronouns has no locatable Help Center article, and was never name-searched** --
   the search budget capped on that exact query. Absent from `a64` (55), `a51` (26),
   `a547248` and `a545784`. Status: no article located, **not proven absent.** A18
   and A19 are enumerated from the server's own live control list.
3. **Profile video / Cover Story: my two recovery passes DISAGREE.** One concluded it
   is retired (no member-recorded video documented anywhere; the only cover-* articles
   are cover IMAGE). The other refused that conclusion, noting LinkedIn does publish
   retirement notices -- `a10421005` for Professional sources -- and none exists for
   Cover Story. **Unresolved, and reported as unresolved rather than picked.**
4. **"Message suggestions" and "focused inbox"** -- never reached; the WebSearch
   budget refused those calls. Cannot confirm they exist as settings.
5. **"Social, economic, and workplace research"** -- the literal string is absent
   from `a1340649` and `a1336543`, the two best-matched articles. A settings path was
   harvested for it in the first walk and no article names it.
6. **"Data log" / "Manage data permissions"** -- my two walks disagree: the first
   cites `a1341842` and `/psettings/data-log`; the recovery pass probed `a1340649` and
   found both strings absent. Both can be true (different articles); recorded as a
   disagreement rather than resolved.
7. **Search history view-and-delete** -- no dedicated control documented. "Search
   Queries" exists only as a data-export category.
8. **Close vs delete: what survives** -- `a1347782` covers only the enterprise-account
   precondition and states nothing about what is deleted or any reopen window.
9. **Security keys** -- searched; LinkedIn Help documents passkeys and 2FA and no
   separate hardware-key control. A measured negative.
10. **Audio events** -- three URLs 404'd (`a759884` twice, `a597255`, hub `a173033`).
    L6 rests on a search snippet.
11. **No Help Center page in the profile subtree names a `/in/<vanity>/details/...`
    path.** Probed across seven articles. D4 and D10 are grounded in this repo's
    allowlist and the live skills read, not in LinkedIn's documentation.
12. **Postal code / "location within this area"** -- `a547248` was fetched and names
    only "Location" and "Country/Region". `a548419` "Updated location data available
    to members" exists in `a64` and was NOT fetched; it is the best remaining lead.
13. **About 60 of the 145 first-walk settings rows were read from a topic listing or
    the help search index rather than from the article body.** URLs and titles are
    real; descriptions are snippets. None changed a state assignment, because in every
    case the state was decided by the address family rather than the description.
14. **Mobile-only surfaces were not separated systematically.** A23 and N20 are
    flagged; there are probably others, and a browser-driven server cannot reach any
    of them. `/mwlite/settings` (finding 7.1) is the mobile-web settings tree and
    nothing in this repo has ever named it.

**TWO INSTRUMENT LIMITS THAT BOUND EVERYTHING ABOVE, and they are session-wide
rather than mine:**

* **The WebSearch budget hit its cap at 200/200 mid-recovery**, consumed largely by
  sibling agents in this session. Both recovery passes lost queries to it and both
  kept "NOT REACHED (budget)" strictly separate from "searched and found nothing".
  Items 2, 3 and 4 above are budget casualties, not measured negatives.
* **LinkedIn's own Help search endpoint is dead** -- `/help/linkedin/search?query=`
  returns HTTP 400 and `/help/linkedin/solutions?query=` returns HTTP 404. There is
  no in-house fallback when an external search engine is unavailable, and DuckDuckGo
  and Mojeek returned CAPTCHA / HTTP 403 when tried as substitutes.
* **A method caveat worth carrying to every other slice:** WebFetch summarisation is
  lossy in BOTH directions. The same cached page yielded six settings paths under an
  open prompt and only one under a prompt demanding strict verbatim quoting -- the
  stricter-sounding prompt silently dropped markdown link targets. Every load-bearing
  path in finding 7.1 was confirmed by a per-string existence probe, not by a
  summarising fetch.

---

## 11. WHAT THIS CENSUS DID NOT ESTABLISH

1. **Whether the six aimable intro fields are still aimable.** The reading is from
   2026-09-02, on a surface that changed completely in the two days before it.
   Settling it costs one `linkedin_profile_editor_fields` call and zero writes.
2. **Whether the ten uncaught addresses in finding 7.1 should be denied.** They are
   refused by the allowlist today and excluded by the family ruling, so this is a
   defence-in-depth decision rather than a measurement, and taking it requires no
   page load.
3. **Whether Cover Story exists.** Two independent passes reached opposite
   conclusions; see item 3 of section 10.
