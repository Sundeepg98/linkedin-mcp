# job-list DOM shape -- two captures, measured offline

Slice: premium-four / jobshape. Generated by scripts/_probe_job_list_shape.py, no dependency added beyond the stdlib html.parser (lxml/bs4 not installed in this venv).

HEADLINE: a LinkedIn job list is two-tiered. Every job posting gets a list slot (`<li data-occludable-job-id>`); only some slots are hydrated with an actual content card (`<div class="job-card-container" data-job-id>`, nested one bare <div> deeper). The rest are empty placeholder `<li>` elements -- LinkedIn's own class name for them is `jobs-search-results__job-card-search--generic-occludable-area`. See section 2 per capture for the exact hydrated/placeholder split.

## 0. Captures

| capture | sha256 | bytes | mtime (UTC) |
|---|---|---|---|
| recommended (cap-jobs-recommended.html) | 979aadcf0f24e2f51ea8ebd64d442c40a5f634895029d50adf40b19895b96fa3 | 1702493 | 2026-09-20 04:31:49 UTC |
| search (cap-jobs-search.html) | 1804f0b660b5759121ab364341066a3b4370324251798f62b4704a1ac4ac2561 | 1555561 | 2026-09-20 04:32:36 UTC |

## 1. Control -- the extractor, shown able to fail, before its readings are trusted

A synthetic document with a known ground truth: 9 list slots (6 hydrated, 3 empty placeholders), one of the hydrated slots placed OUTSIDE `<main>` as a decoy, landmark ancestors at known depths, and a needle planted inside a `<script>` block to prove stripping. Both outcomes below are the literal, unedited stdout of running scripts/_probe_job_list_shape.py --control and --control --break-extractor, captured at report-generation time -- not retyped, not summarized.

### 1a. good path -- extractor pointed at the real selectors

```
=== CONTROL (good path): known-ground-truth synthetic document ===
  doc-wide slots (tier 1)                                    got=9        want=9        PASS
  inside-<main> slots                                        got=8        want=8        PASS
  doc-wide hydrated cards (tier 2)                           got=6        want=6        PASS
  inside-<main> hydrated cards                               got=5        want=5        PASS
  hydrated slot count                                        got=6        want=6        PASS
  placeholder slot count                                     got=3        want=3        PASS
  hydrated + placeholder accounts for every slot             got=9        want=9        PASS
  list-parent tag is <ul> for all in-main slots              got=True     want=True     PASS
  card 1001 anchor count                                     got=1        want=1        PASS
  card 1003 anchor count (job + company)                     got=2        want=2        PASS
  card 1003 distinct route shapes                            got=2        want=2        PASS
  card 1005 button count                                     got=2        want=2        PASS
  card 1001 aria-label element count                         got=1        want=1        PASS
  card 1001 id mechanism                                     got='data-job-id' want='data-job-id' PASS
  card 1001 id digit length                                  got=4        want=4        PASS
  card 1004 href digit-run length (cross-check)              got=10       want=10       PASS
  cross-tier id equality holds for all hydrated slots        got=6        want=6        PASS
  'something went wrong' rendered count (inside <script>)    got=0        want=0        PASS
  'something went wrong' raw count (inside <script>)         got=1        want=1        PASS
  'we could not' rendered count (inside <p>)                 got=1        want=1        PASS

CONTROL (good path): ALL PASS
```

process exit code: 0

### 1b. deliberately broken path -- same document, wrong class token

```
=== CONTROL (deliberately broken path): same document, wrong selector ===
  selector used: 'job-card-container-TYPO-DOES-NOT-EXIST'
  cards found: 0   (ground truth on this fixture is 6)
  FAIL, as designed: a wrong selector returns the wrong count on a document with a KNOWN answer, so the good-path PASS above is not vacuous.
```

process exit code: 1

## recommended

### recommended -- document frame

| metric | value |
|---|---|
| raw byte size (on disk) | 1702493 |
| raw char length (decoded) | 1699332 |
| rendered text length | 5993 |
| rendered as pct of raw char length | 0.35 |
| `<main>` count | 1 |
| rendered text inside `<main>` | 5225 |
| that as pct of the page's rendered text | 87.19 |
| `<nav>` count | 1 |
| `<header>` count | 4 |
| `<footer>` count | 1 |
| `<aside>` count | 2 |
| `<section>` count | 5 |

### recommended -- the job-card container (TWO TIERS)

tier 1, list slot: any element carrying `data-occludable-job-id` -- one per job posting LinkedIn has placed in this list window, hydrated or not.
tier 2, hydrated card: exact class token `job-card-container` -- only for slots actually rendered with content.

slots found (tier 1): 24
  hydrated (has a nested tier-2 card): 7
  placeholder (`jobs-search-results__job-card-search--generic-occludable-area`, zero children): 17
  neither hydrated nor recognized-placeholder: 0
hydrated cards found (tier 2, independent class-token search): 7
  hydration cross-check: every tier-2 card sits inside a tier-1 slot? yes (7 == 7)

EXACT CLASS TOKEN `job-card-container` -- doc-wide vs inside-`<main>` (reported whichever way it comes out, not smoothed toward the tidier answer):
  doc-wide: 7
  inside-<main>: 7
  equal: True

tier-1 slot tag: li x24
tier-1 list-parent tag: ul x24
structural path from slot to hydrated card: <slot> > <div (0 attrs)> > <card class=job-card-container>

attribute names on the tier-1 slot (name only, never value):

| attribute | slots carrying it | of 24 |
|---|---|---|
| `id` | 24 | 100 |
| `class` | 24 | 100 |
| `data-occludable-job-id` | 24 | 100 |

selector candidates at the slot tier (doc-wide = anywhere in the document, any tag, not just slots):

| attribute | on every slot | doc-wide count | inside-`<main>` count | doc-wide == inside-main |
|---|---|---|---|---|
| `class` | yes | 733 | 524 | NO -- 209 outside main |
| `data-occludable-job-id` | yes | 24 | 24 | yes |
| `id` | yes | 249 | 148 | NO -- 101 outside main |

chosen slot selector: `data-occludable-job-id` -- on every slot (24 of 24) and doc-wide count equals the slot count exactly (24 == 24).

attribute names on the tier-2 hydrated card (name only, never value):

| attribute | cards carrying it | of 7 |
|---|---|---|
| `data-job-id` | 7 | 100 |
| `class` | 7 | 100 |
| `aria-current` | 1 | 14 |

cross-tier id equality (slot's `data-occludable-job-id` vs hydrated card's `data-job-id`, values never printed): 7 equal, 0 mismatched, 0 card-side attribute absent

landmark stack enclosing the list (outermost first, sampled from slot 1's parent chain): main

### recommended -- what one HYDRATED card contains

scope: the 7 hydrated tier-2 cards only. A placeholder slot has zero children by construction and would silently zero out these stats if folded in, so it is not.

| element | min | median | max |
|---|---|---|---|
| `<a>` anchors | 1 | 1 | 1 |
| distinct anchor route shapes | 1 | 1 | 1 |
| `<img>` | 1 | 1 | 3 |
| `<button>` | 1 | 1 | 1 |
| heading elements (h1-h6) | 0 | 0 | 0 |
| `<span>` | 8 | 10 | 12 |
| `<p>` | 0 | 0 | 0 |
| elements with `aria-label` | 2 | 2 | 3 |

distinct anchor route shapes observed, aggregated across all 7 hydrated cards (via the shipped, control-tested shape_path() -- never a raw href):

| route shape | cards linking to it | of 7 |
|---|---|---|
| `/jobs/view/<opaque>` | 7 | 100 |

### recommended -- the job id

list slots exposing an id: 24 of 24 (via `data-occludable-job-id`, which is what defines slot membership -- not an independent finding, the discovery mechanism)
  slot-level id digit length: min 10, max 10

of the hydrated subset, cards independently exposing an id via their own tier-2 attributes/href: 7 of 7

| mechanism | cards |
|---|---|
| `data-job-id` | 7 |

hydrated-card id digit length: min 10, max 10 (id values themselves are never printed)

### recommended -- landmark/empty-state vocabulary

| needle | in source | rendered |
|---|---|---|
| no results | 0 | 0 |
| nothing here | 0 | 0 |
| try again | 1 | 0 |
| not available | 0 | 0 |
| no longer | 0 | 0 |
| sorry | 1 | 0 |
| page not found | 0 | 0 |
| coming soon | 0 | 0 |
| check back | 0 | 0 |
| we could not | 0 | 0 |
| something went wrong | 1 | 0 |
| no jobs | 0 | 0 |

at least one needle hit -- see table above for which, and whether it survived stripping (rendered) or was source-only.

## search

### search -- document frame

| metric | value |
|---|---|
| raw byte size (on disk) | 1555561 |
| raw char length (decoded) | 1551460 |
| rendered text length | 7199 |
| rendered as pct of raw char length | 0.46 |
| `<main>` count | 1 |
| rendered text inside `<main>` | 5224 |
| that as pct of the page's rendered text | 72.57 |
| `<nav>` count | 1 |
| `<header>` count | 4 |
| `<footer>` count | 1 |
| `<aside>` count | 2 |
| `<section>` count | 7 |

### search -- the job-card container (TWO TIERS)

tier 1, list slot: any element carrying `data-occludable-job-id` -- one per job posting LinkedIn has placed in this list window, hydrated or not.
tier 2, hydrated card: exact class token `job-card-container` -- only for slots actually rendered with content.

slots found (tier 1): 25
  hydrated (has a nested tier-2 card): 7
  placeholder (`jobs-search-results__job-card-search--generic-occludable-area`, zero children): 18
  neither hydrated nor recognized-placeholder: 0
hydrated cards found (tier 2, independent class-token search): 7
  hydration cross-check: every tier-2 card sits inside a tier-1 slot? yes (7 == 7)

EXACT CLASS TOKEN `job-card-container` -- doc-wide vs inside-`<main>` (reported whichever way it comes out, not smoothed toward the tidier answer):
  doc-wide: 7
  inside-<main>: 7
  equal: True

tier-1 slot tag: li x25
tier-1 list-parent tag: ul x25
structural path from slot to hydrated card: <slot> > <div (0 attrs)> > <card class=job-card-container>

attribute names on the tier-1 slot (name only, never value):

| attribute | slots carrying it | of 25 |
|---|---|---|
| `id` | 25 | 100 |
| `class` | 25 | 100 |
| `data-occludable-job-id` | 25 | 100 |

selector candidates at the slot tier (doc-wide = anywhere in the document, any tag, not just slots):

| attribute | on every slot | doc-wide count | inside-`<main>` count | doc-wide == inside-main |
|---|---|---|---|---|
| `class` | yes | 1067 | 557 | NO -- 510 outside main |
| `data-occludable-job-id` | yes | 25 | 25 | yes |
| `id` | yes | 350 | 160 | NO -- 190 outside main |

chosen slot selector: `data-occludable-job-id` -- on every slot (25 of 25) and doc-wide count equals the slot count exactly (25 == 25).

attribute names on the tier-2 hydrated card (name only, never value):

| attribute | cards carrying it | of 7 |
|---|---|---|
| `data-job-id` | 7 | 100 |
| `class` | 7 | 100 |
| `aria-current` | 1 | 14 |

cross-tier id equality (slot's `data-occludable-job-id` vs hydrated card's `data-job-id`, values never printed): 7 equal, 0 mismatched, 0 card-side attribute absent

landmark stack enclosing the list (outermost first, sampled from slot 1's parent chain): main

### search -- what one HYDRATED card contains

scope: the 7 hydrated tier-2 cards only. A placeholder slot has zero children by construction and would silently zero out these stats if folded in, so it is not.

| element | min | median | max |
|---|---|---|---|
| `<a>` anchors | 1 | 1 | 1 |
| distinct anchor route shapes | 1 | 1 | 1 |
| `<img>` | 1 | 1 | 1 |
| `<button>` | 1 | 1 | 1 |
| heading elements (h1-h6) | 0 | 0 | 0 |
| `<span>` | 8 | 9 | 11 |
| `<p>` | 0 | 0 | 0 |
| elements with `aria-label` | 2 | 2 | 3 |

distinct anchor route shapes observed, aggregated across all 7 hydrated cards (via the shipped, control-tested shape_path() -- never a raw href):

| route shape | cards linking to it | of 7 |
|---|---|---|
| `/jobs/view/<opaque>` | 7 | 100 |

### search -- the job id

list slots exposing an id: 25 of 25 (via `data-occludable-job-id`, which is what defines slot membership -- not an independent finding, the discovery mechanism)
  slot-level id digit length: min 10, max 10

of the hydrated subset, cards independently exposing an id via their own tier-2 attributes/href: 7 of 7

| mechanism | cards |
|---|---|
| `data-job-id` | 7 |

hydrated-card id digit length: min 10, max 10 (id values themselves are never printed)

### search -- landmark/empty-state vocabulary

| needle | in source | rendered |
|---|---|---|
| no results | 0 | 0 |
| nothing here | 0 | 0 |
| try again | 1 | 0 |
| not available | 0 | 0 |
| no longer | 0 | 0 |
| sorry | 1 | 0 |
| page not found | 0 | 0 |
| coming soon | 0 | 0 |
| check back | 0 | 0 |
| we could not | 0 | 0 |
| something went wrong | 1 | 0 |
| no jobs | 0 | 0 |

at least one needle hit -- see table above for which, and whether it survived stripping (rendered) or was source-only.

## differences between the two surfaces

| metric | recommended | search |
|---|---|---|
| list slots (tier 1) | 24 | 25 |
| hydrated cards (tier 2) | 7 | 7 |
| placeholder slots | 17 | 18 |
| unclassified slots | 0 | 0 |
| hydration ratio | 7/24 | 7/25 |
| selector chosen (tier 1) | data-occludable-job-id | data-occludable-job-id |
| `job-card-container` token: doc-wide | 7 | 7 |
| `job-card-container` token: inside-`<main>` | 7 | 7 |
| `job-card-container` token: doc-wide == inside-main | True | True |
| `<main>` count | 1 | 1 |
| `<nav>` count | 1 | 1 |
| `<section>` count | 5 | 7 |
| rendered text length | 5993 | 7199 |
| hydrated cards exposing an id (tier 2) | 7 | 7 |
| hydrated-card id digit length (min-max) | 10-10 | 10-10 |
| anchors per hydrated card (median) | 1 | 1 |
| images per hydrated card (median) | 1 | 1 |

one slot shape AND one hydrated-card shape describes both surfaces: YES
caveat: slot COUNT differs between the two surfaces (24 vs 25) even though the SHAPE (attributes, nesting, placeholder class) is identical -- a fixture should treat slot count as a per-surface parameter, not a constant.

