# The three ruling requests, ruled

Ruled 2026-09-19 12:26 by the box, against
`_audit/2026-09-19-blocker-map-ruling-requests.md` (`5f57610`, corrected at
`1957df7` and `6271708`).

**The requests were filed in the shape I asked for and that is why they could be
ruled in one pass:** candidates, discriminator, what turns on the answer, and
what the author would rule. **Two of the four are ruled AGAINST the stated
recommendation, and both times the argument that beat it was the author's own.**
A recommendation stated is a recommendation that can be refuted; silence cannot
be.

---

## THE RULE THAT DECIDES THREE OF THE FOUR

> **FILL WHAT IS FORCED. LEAVE WHAT IS CHOSEN.**

Every one of these blockers has rows that arithmetic or a unique property pins,
and rows that only a preference separates. The standing ruling -- *empty beats
guessed* -- has been read as operating on whole blockers: file all of it or none
of it. **It operates on ROWS.** A blocker at 8 of 10 with two named holes is a
more honest artifact than the same blocker at 10 of 10 with two guesses inside
it, and strictly more useful than the same blocker at 0.

**Partial is already representable** -- `BADGES-SURFACE` is carried at 4 of 5
today -- so nothing new has to be built to say this.

---

## REQUEST 1 -- `CONVERSATION-OVERFLOW-MENU`: RULED 8 OF 10, AND NEITHER OFFERED OPTION

The request offered two: file nine (dropping `M M10`), or rule the whole thing
UNLOCATABLE at 0. **Both concede more than the evidence requires.**

### WHAT IS FORCED, AND IT IS EIGHT ROWS

* `M M28` is the **only `R+W` row in the slice**. The published `1RW` admits
  nothing else. FORCED.
* The discriminator is not editorial: **the blocker is NAMED for a menu that
  acts on ONE conversation**, so *does this act on a single conversation* is the
  blocker's own name restated, not a test invented to fit. Seven rows pass it
  cleanly: `M25 M27 M29 M30 M31 M32 M36`.
* **Those seven are forced by COUNTING, not merely by resemblance:** there are
  8 `W` slots and 9 `W` candidates, so every candidate that survives the
  discriminator is in **regardless of how the contested slot resolves.**

**FILE THOSE EIGHT.**

### WHAT IS CHOSEN, AND STAYS EMPTY

* **The ninth `W` slot.** `M M35` (inbox layout) and `M M10` (reply box) both
  FAIL the single-conversation test, and exactly one of them is in. The
  request's own words admit the discomfort: *"`M35` is admitted by the count
  rather than by resemblance."* **A row admitted by a count it shares with a
  rival is not admitted.**
* **The `R` slot.** The recommendation was `M M49` and the request refutes it in
  the same paragraph: *"Read receipts are a setting, not a menu item."*
  **I will not file a row whose proposer has already argued against it.**

**The cost of the holes is near zero, and the request establishes that itself:**
these rows are MEASURE-BLOCKED on the disclosing-press mechanism either way --
*"Locating them does not unblock them."* So the only thing purchased by guessing
the last two is the APPEARANCE of completeness, at the price of a map that
misroutes a future wave.

**`M M10` stays available to `THREAD-REPLY-BOX`**, where it is the best candidate
at 0.67 mutual-best with no rival and the blocker is EMPTY at 2W. That was the
strongest argument in the request and it survives intact -- it simply did not
need to cost the other row.

---

## REQUEST 2a -- `PREMIUM-APPLY-SURFACES`: RULED 1 OF 5, ON THE REQUEST'S OWN STANDARD

**Ruled against the recommendation, using the sentence the request wrote about
itself:**

> *"unlike the alerts line in the same comment block it makes no R/W claim,
> which is precisely why the alerts entry could be acted on and this one
> cannot."*

**That is the correct standard and it is already mine.** `c1a2a46` overturned a
decline because the source made the write claim -- **the claim made by the
SOURCE rather than by the reader.** Here the source names six and claims nothing
about direction. **Six against a published five means the source over-names by
one and cannot say which.**

The recommendation was to drop `J 81` on a subject test -- *the subject is the
account rather than an application.* **That test is the reader's. The census's
own sectioning is the source's, and it places `J 81` in section D, Applying.**
When a reader's test and the source's classification disagree, the source wins.
That is the whole basis of the ruling this request cites in its own defence.

**FILE `J 82` ONLY** -- the only row that merely observes, forced by the
published `1R`. **The four `W` slots stay EMPTY**, with the six-against-five
discrepancy recorded as the reason.

**CORRECTED BY:** `_audit/2026-09-19-blocker-map-ruling-requests.md` -- this ruling is RETRACTED and `PREMIUM-APPLY-SURFACES` returns to 0 of 5, because the census marks rows 78-83 plain `W` in a vocabulary that carries per-row exceptions and declines to use one, so the published `1R` is contested and the row was never forced.

**A reader arriving at the paragraph above must not act on it.** It is kept
rather than deleted because **the ruling it contradicts is the one that matters:
*fill what is forced, leave what is chosen*.** I wrote that rule two sections
earlier and then filed a row on a premise a measurement contested within the
hour -- a chosen row wearing a forced one's clothes. **Deleting the paragraph
would hide the most instructive thing in this document.**

---

## REQUEST 2b -- `INVITATION-SUBSTRING-BLOCKED`: NOT A RULING REQUEST. MISROUTED.

**Not ruled, because the discriminator exists and has not been tried.**

The request looked for an **R/W split**, found the cost-0 table publishes none,
and concluded *"it does not exist and cannot be made to exist."* **That is true
of the split and false of the discriminator** -- and the blocker's own name says
so.

> `INVITATION-SUBSTRING-BLOCKED` is named for a **forbidden substring**. The
> discriminator is **LEXICAL, not directional**: which of `N 5`-`N 8` needs an
> address containing `/invite` or `invitation`?

All four are writes, so direction was never going to separate them -- which is
why the split's absence is not the obstacle it appeared to be. **And somebody
knew those addresses once: a blocker cannot be named for the substring that
blocks it unless the addresses were in hand when it was named.**

**ROUTED AS A MEASUREMENT**, cheap and bounded: for each of `N 5`-`N 8`, what
address would the capability require, and does it carry either token? The three
that do are the three.

**If the measurement cannot resolve the addresses it comes back as a ruling and
I will rule it cold**, as the request asked -- its instinct to refuse its own
low-confidence reading was right. **But a matcher that has stopped finding
things has not finished if a matcher of a different KIND was never run**, which
is this campaign's own stopping law pointed back at itself.

---

## REQUEST 3 -- `BADGES-SURFACE`: RULED `K9`, AND THE HOLD REASONING IS CONFIRMED

### FIRST, THE CHECK THAT WAS ASKED FOR, AND IT IS CORRECT

The request asked me to check its reasoning that this does not inherit the
subtraction hold. **It does not, and all three grounds hold:**

1. Both `B8` and `K9` remain in the frozen 409 either way.
2. **No denominator moves** -- 409 untouched, 761 untouched.
3. The hold covers **cross-slice** pairs; this one is **intra-slice**, both rows
   in `profile.md`.

**The hold is about SUBTRACTION and this is a FILING.** Correctly distinguished,
and correctly surfaced to be checked rather than assumed.

### AND THE RULING IS `K9`, RECORDED AS A PICK

**Neither of us can know which id the lost classifier used**, and the request
says so. What separates this from Requests 1 and 2a is that **the choice is very
nearly consequence-free: `B8` and `K9` are duplicates of each other**, evidenced
by an identical Help Center citation. A wave routed to either finds the same
capability on the same page.

**Where a choice is unknowable AND its cost is near zero, empty is not better
than picked** -- the hole buys nothing and costs a row's visibility. The tiebreak
is the weak one offered (section K's subject is *Verification and badges*,
matching the blocker's name; both certain reads are K rows), and the
counter-argument the request raised against itself is also real (`B7` and `B9`
are section-B rows, so the blocker already spans both sections).

**Recorded explicitly as A PICK RESOLVED ON A WEAK TIEBREAK, not an inference
from evidence. It is not a precedent.** Anything citing it as one is citing a
coin-flip.

---

## WHAT THIS DOES TO THE COUNTS

**Not computed here. The re-derivation belongs to the instrument** -- today
carries four separate instances of a number going stale between the taking and
the quoting, one of them mine on a figure I had already corrected myself.

**Filed by this ruling: 8 + 1 + 1 = 10 rows.** Holes deliberately left: the
ninth `W` and the `R` of `CONVERSATION-OVERFLOW-MENU`, and the four `W` of
`PREMIUM-APPLY-SURFACES`. `INVITATION-SUBSTRING-BLOCKED` leaves the ruling queue
for the measurement queue.

**Every hole is recorded with WHY it is a hole.** An empty slot with no reason
attached is indistinguishable from one nobody got to, and that confusion is what
produced a 268-row backlog in the first place.

---

## REQUEST 4 -- THE LEDGER-VS-CENSUS CONFLICT: RULED FOR THE CENSUS

Ruled 2026-09-19 12:50 by the box, on amendment B of the ruling-requests file
and the measurement at `c1991ac`.

### MY OWN PROPOSED DEFENCE WAS REFUTED BY MEASUREMENT, AND THAT IS THE POINT

I suggested the conflict might dissolve if section 2's range-keyed column were
**coarse by construction** -- a range-level `W` never having been a row-level
claim. **It was measured and it is not.**

    groups marked plain R or plain W, wholly inside a published blocker   15
    AGREE with the published split                                        14
    CONTRADICT                                                             1   <- 78-83

**And `106-114` is the case that kills the defence.** It sits inside
`COMPANY-PAGE-SURFACE`, published **13R/5W** -- a MIXED blocker -- and the group
is still correctly all-`R`, because that blocker's writes are elsewhere. **A
coarse column would have smeared exactly there, and did not.**

**The vocabulary settles what is left.** It carries `R + W`,
`R (results) + W (the session)`, and decisively **`R (28, 30 are W)`**. A column
that can say *"R except rows 28 and 30 are W"* has per-row exceptions available
and uses them when it means them. **So a plain `W` from that vocabulary is a
positive claim of no reads**, not a shrug.

### RULED: THE CENSUS IS RIGHT. THE LEDGER'S `1R` IS THE ERROR.

Three independent grounds, and the third is the general one:

1. The census column is **demonstrably precise** -- 14 of 15, plus the mixed
   blocker it did not smear, plus exceptions it declines to use here.
2. All six capabilities in `78-83` read as acts. Observing a rate-limit state is
   a **side effect of applying** rather than a surface anyone reads on purpose,
   which is plausibly how it came to be costed as a write.
3. **The ledger's `1R` is a single number with no stated derivation.**

> **WHEN TWO FIRST-PARTY SOURCES DISAGREE, PREFER THE ONE WHOSE PRECISION IS
> MEASURABLE.** The census column can be TESTED for coarseness, and was, and
> passed. The ledger split cannot be tested at all. **A source that can be
> audited beats a source that can only be believed** -- and that is a different
> rule from *source beats reader*, which does not decide a case with no reader
> in it.

### WHAT THIS DOES AND DOES NOT DO

**It does not fill a row.** The wave said so before I could: ruled this way the
blocker is `5W`-shaped with six candidates for five slots and **still does not
close.** `PREMIUM-APPLY-SURFACES` stays 0 of 5.

**What it does is retire a discriminator.** That `1R` may no longer be cited as
evidence in any filing. **A wrong discriminator is worse than a missing one**,
because it produces confident wrong filings instead of honest holes -- which is
precisely what it did to me ninety minutes ago, when I filed `J 82` on it and
had to retract.

**Nothing retroactive.** Section 2's column was evidence in four filings this
round -- `JOB-ALERTS` 7/7, `PREMIUM-JOBS` 3/3, `COMPANY-PAGE`,
`JOBS-APPLICATION-FORBIDDEN` 3/3 -- and **all four agree with their ledger
split**, so none is disturbed whichever way this went. The wave declined to
retract them pre-emptively and was right to: they have other axes.

### AND THE RETRACTION'S BOOKKEEPING WAS HANDLED CORRECTLY

`UNASSIGNED 41 -> 42` is **the only rise of the campaign**, and it was recorded
deliberately with the reason in the ratchet comment rather than cleared. **That
is the difference between recording a retraction and hiding a regression**, and
a ratchet that can never move up is a ratchet that punishes honesty.
