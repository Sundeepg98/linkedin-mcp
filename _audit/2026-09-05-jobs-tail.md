# The job-surface tail: five of six blockers are mis-queued, and the mechanism they need is already shipped and empty

Wave `jobs-tail`, 2026-09-05. Six blockers off the ranked table in
`_audit/2026-09-03-linkedin-gap-blockers.md` section 3, gating 19 census rows on
the JOB surfaces:

    36 JOB-ALERTS-SURFACE      7W   ledger: allowlist +1, denylist x1, WriteSpec, BUILD
    61 PREMIUM-APPLY-SURFACES  5    ledger: allowlist +2, WriteSpec, BUILD
    62 TRACKER-ROW-MENU        3W   ledger: denylist x1, WriteSpec, BLOCKED
    65 JOBCARD-OVERFLOW-MENU   2W   ledger: WriteSpec, MEASURE
    70 EASY-APPLY-MULTISTEP    1W   ledger: ruling needed, DECIDE
    81 FOUND-A-JOB-FLOW        1W   ledger: allowlist +1, WriteSpec, BUILD

**No write was fired. No application was submitted. No browser was opened, no
page was loaded, and no LinkedIn session was touched at any point in this wave.**
Every measurement below is taken against code and against the shipped predicates.

---

## 1. THE FINDING: THE LEDGER BROKE ITS OWN ASSIGNMENT RULE ON FIVE OF THESE SIX

The ranked table states its own rule: *"one blocker per row, the EARLIEST
binding constraint. A row that looks blocked by a missing write but has no way
to find its target is filed against the missing surface, not the missing
write."*

Measured against `writes.WriteSpec`, five of these six rows are filed against a
missing write when the earliest binding constraint is a page nobody has opened.

`WriteSpec.url_template` is `Optional[str]`, and the field's own contract says
why:

> `None` where the surface has NEVER BEEN LOADED. A gate may not name a target
> surface nobody has opened, for the same reason it may not print an unmeasured
> reversibility claim.

So for every one of these blockers, a WriteSpec cannot be completed until the
surface is photographed. **The `3*W` the cost model charges for "a new WriteSpec
+ gate + consent text" cannot be spent first.** It is not the earliest
constraint; it is not even a reachable one.

    ledger queue        measured earliest constraint
    36  BUILD       ->  MEASURE  (and a boundary ruling, see section 3)
    61  BUILD       ->  MEASURE
    62  BLOCKED     ->  MEASURE  (and it is NOT blocked on a boundary, see 3)
    65  MEASURE     ->  MEASURE  (confirmed, and it is the cheapest of the six)
    70  DECIDE      ->  DECIDE   (confirmed, and the decision changes, see 5)
    81  BUILD       ->  MEASURE

**This is not a criticism of the classifier's reasoning; it is a consequence of
its inputs.** The ledger was built from the census row TEXT. The constraint that
actually binds lives in a dataclass field in `writes.py`. Nothing in the row text
could have surfaced it.

---

## 2. THE MECHANISM THESE SIX ROWS NEED IS ALREADY BUILT, COSTS NO BOUNDARY, AND IS CURRENTLY EMPTY

`writes._NINE_REFUSALS` is a registry mapping an action to a refusal sentence
that names its own blocker. Its header defines exactly two blocker shapes and
forbids either standing in for the other:

    NO CONTROL  -- the thing that would be clicked has never been observed.
                   A capture lifts it.
    NO SURFACE  -- the address the action would act on is refused by
                   readonly._FORBIDDEN_URL_SUBSTRINGS, which is checked before
                   the allowlist and is NOT shortened for a write.

Those are precisely the two shapes my six blockers occupy, and the surrounding
comment records that building the original nine widened no boundary at all:
*"the four frozen denylists are byte-identical across this change, and that is
checkable rather than asserted."*

**MEASURED AT THIS TREE: `len(_NINE_REFUSALS) == 0`.** Every one of the original
nine has left the table, each departure documented in place as a comment. The
consumer at `writes.py:5548` -- `if spec.action in _NINE_REFUSALS:` -- therefore
cannot fire in the current tree.

Two things must be said about that, and they are different claims:

1. **The empty state is CORRECT.** The table emptied because the work got done:
   actions left by shipping, or by having their blocker ruled on. Nothing here
   is a defect and nothing needs reverting.
2. **A pointer to it has gone stale.** `writes.py:971` reads *"The blockers are
   itemised per action in `_NINE_REFUSALS`."* At this tree it itemises nothing.
   That is a standing instruction rather than a dated note -- this wave followed
   it and reached an empty dict -- and it is the class this repository has
   already recorded as the one the correction machinery cannot bind.

**I DID NOT FIX THAT SENTENCE.** `git log --oneline -3 -- linkedin_server/writes.py`
names `0e98882`, `1a3527f`, `7d785e5` -- the uploads, latency and comment waves.
It is not my artifact and today's rule is that a neighbour's lines are not mine
to sweep. It is recorded here for whoever holds that file.

**The consequence for the roadmap is the useful half:** the honest first
deliverable for the write rows in these six blockers is not six WriteSpecs. It is
six `_NINE_REFUSALS` entries, each naming NO CONTROL or NO SURFACE and the exact
capture or ruling that lifts it -- a shape this package has shipped before, at
zero boundary cost, where `mint` refuses a grant at ISSUE so the operator reads a
warning rather than an offer he cannot act on.

---

## 3. THE BOUNDARY, MEASURED RATHER THAN DERIVED

New instrument, committed with this document:

    scripts/_probe_jobs_tail_boundary.py

It opens nothing. It calls the shipped `readonly.assert_read_url` on candidate
address strings that are literals typed into the file, and reports, per address,
the RELATION plus the forbidden substring that bit -- because a refusal naming
only what it did NOT match is half a measurement.

**IT WAS SHOWN FAILING BEFORE IT WAS BELIEVED, and it failed on its first run
against its own author.** The control block asserts one known-ALLOW and one
known-REFUSE address before any measurement is taken. On the first run it printed

    CONTROL FAILED: control-allow: the job search surface this server reads today
      expected ALLOW, predicate said REFUSE
      reason: raised AttributeError

because I had called a predicate name that does not exist in this package. The
probe refused to print a single measurement. Had the control not been there, all
19 candidates would have come back REFUSE and I would have reported the boundary
as far tighter than it is.

**RESULT, recomputed at freeze time rather than re-read:**

    allowlist patterns   29
    forbidden substrings 33
    candidates           19
      ALLOW                          6
      REFUSE by forbidden substring  5
      REFUSE no pattern matches      8

### Three ledger entries are wrong, and one is wrong in the expensive direction

**62 TRACKER-ROW-MENU -- the ledger charges `denylist x1`. There is no URL
denylist cost at all.** Both tracker stages the row menu is drawn on are ALREADY
ADMITTED:

    ALLOW  https://www.linkedin.com/jobs-tracker/?stage=saved
    ALLOW  https://www.linkedin.com/jobs-tracker/?stage=applied

The census prose the charge came from (`_audit/_census/jobs.md`, the rows 54-56
note) says `"update"`, `"set"`, `"add"` are on the **mutation-verb denylist**.
That is a different list from `readonly._FORBIDDEN_URL_SUBSTRINGS`, and the
ranked table's column is headed `boundary`, which reads as the URL boundary.
**Two different lists were collapsed into one column.** Row 62's boundary cost is
zero and its queue is MEASURE, not BLOCKED.

**36 JOB-ALERTS-SURFACE -- the ledger charges `denylist x1`. Measured, three
distinct forbidden substrings bite across the seven rows, and one address carries
two at once:**

    REFUSE  an alert delete verb           forbidden substring: '/delete'
    REFUSE  an alert create verb           forbidden substring: '/create'
    REFUSE  alert frequency/channel        forbidden substring: '/settings/', 'settings'

The bare token `settings` is on the forbidden tuple, which is checked BEFORE the
allowlist. **So an allowlist addition alone does not open the frequency and
channel rows** (census J35, J36) -- it cannot, because those rows never reach the
allowlist. Exemptions in this package are exact-url tables compared with `==`,
never as a shape, so the real cost is one exemption per exact address rather than
one denylist edit. **This is the entry that is wrong in the expensive direction:
the ledger costs `A=1, D=1` where the measured floor is higher.**

**SHARPENED IN SECTION 9, and read that before quoting this paragraph.** The
statement above is true and incomplete: the shipped refusal distinguishes an
address the substring ALONE refuses from one BOTH gates refuse, and for these
three it says both. The consequence is that the alerts surface splits into a
cheap read half and an expensive write half, which is the actionable form.

There is one piece of good news in the same block:

    ALLOW  https://www.linkedin.com/jobs/search/?f_AL=true

The alerts panel reached as a query on the job-search page is already admitted.
Whether LinkedIn actually draws the alert controls there is UNMEASURED and this
document does not claim it does -- the address being open is not the panel being
present. It is named because it is the one route into this blocker that needs no
boundary ruling to try.

**61 PREMIUM-APPLY-SURFACES -- the ledger charges `allowlist +2`. One of the two
is already on the list:**

    ALLOW  https://www.linkedin.com/premium/my-premium/

And a stale sentence rides on that. `_audit/_census/jobs.md` row 127 (the InMail
credit balance) cites a 2026-08-31 audit for *"the boundary entry and reader are
NOT built"*. The boundary entry IS built and is one of the 29 patterns at this
tree. The READER is still not built, so half that sentence is current and half of
it has been overtaken -- which is the shape that has cost this repository three
routing errors today. I have not edited the census file.

**65 JOBCARD-OVERFLOW-MENU -- the ledger charges nothing on the boundary, and
that is CONFIRMED.** Both pages the card menu is drawn on are admitted:

    ALLOW  https://www.linkedin.com/jobs/search/?keywords=node.js
    ALLOW  https://www.linkedin.com/jobs/view/<numeric id>/

**This is the cheapest of my six by a distance: zero boundary cost, and the
capture is a click on a page this server already loads.**

**81 FOUND-A-JOB-FLOW -- `allowlist +1` is the right SHAPE, and the spelling is a
guess.** Neither candidate carries a forbidden substring; both refuse for want of
a pattern. **Nobody has opened this flow, so its address is not known.** The
probe labels both candidates as unconfirmed spellings in the file itself, because
"the predicate refuses this string" is only interesting when the string is one
LinkedIn would actually serve.

---

## 4. THE PAGE-SIZE DEFECT: WHAT I CHECKED AND WHAT I DID NOT

The brief carried a defect measured today: `linkedin_search_jobs` returns a
window of **SEVEN**, not 25, and `start` offsets by **ONES** -- seventeen live
loads, seven every time, against a LinkedIn-reported count of 2915. The docstring
is fixed for that one tool.

A census of every other page-size, pagination and offset-stride assumption in the
tracked package was commissioned as a separate read-only slice, with the two
classes kept apart deliberately: a LISTING-WINDOW assumption is a claim about
LinkedIn and can be wrong; a caller-side cap this server applies to its own output
is a server choice and is not wrong. **See section 7 for its disposition. I do
not report its numbers here, and no result of it is asserted anywhere in this
document.**

What I can say from my own reading: **none of my six blockers' rows carries a
page-size assumption of its own**, because none of them has a reader at all.
Every one is a write or an unopened surface. The inheritance risk in this slice
is zero, and that is a statement about the absence of readers rather than about
the correctness of any number.

---

## 5. EASY-APPLY-MULTISTEP: THE RULING IS REAL, AND IT IS NOT THE ONE THE LEDGER FRAMES

The ledger files row 70 as DECIDE with "ruling needed". Measured, the row is not
unbuilt and the boundary is not what stops it.

Both address spellings refuse at the substring gate:

    REFUSE  .../jobs/easyapply/<id>/    forbidden substring: 'easyapply'
    REFUSE  .../jobs/easy-apply/<id>/   forbidden substring: 'easy-apply'

**But the shipped `apply_job` does not use either address.** It clicks a control
on `/jobs/view/<id>/`, which is admitted, and LinkedIn draws the flow as a modal
over the posting. So the forbidden substrings are not the blocker.

The blocker is a deliberate, documented refusal. `linkedin_apply_job` submits
only if all of: the modal rendered; exactly one control carries LinkedIn's own
submit hook; that control is visible and enabled; its name corroborates the hook;
and **zero advance controls are present**. Its docstring states the reason
plainly: *"filling in steps that have never been seen, to reach a submit that
cannot be withdrawn, is the one guess this server does not make."*

**THE DECISION THE OPERATOR ACTUALLY FACES IS NOT "SHOULD MULTISTEP APPLY BE
BUILT". IT IS "MAY A DRAFT BE SPENT TO PHOTOGRAPH THE MODAL".** Here is why, and
it is the part the ledger's framing hides:

* the multi-step modal has **never been observed on this account** -- the
  docstring records exactly one apply flow ever seen, a single screen with one
  enabled submit and no Next;
* the modal is drawn only **inside the confirmed path**. There is no
  modal-preview mode: calling `apply_job` without a token reads the posting, not
  the modal;
* and `writes.py:7967` records the cost of opening it: *"the first click opened a
  modal and may leave a draft in his job tracker."*

So the capture that would unblock this row costs a DRAFT ROW, not an application.
That is a materially smaller and more decidable question than the one the ledger
poses, and it is decidable partly because the draft is observable: the tracker's
`draft` stage is one of the three admitted stages and this repo already holds a
capture of it.

**A design that follows from this, NOT built and NOT proposed as a write:** a
read-only apply-modal reader -- open the control, read the modal, close it,
submit nothing -- would photograph the multi-step flow, and would ALSO let him
see a posting's screening questions before committing to an application that
nobody has established LinkedIn lets him withdraw. That is directly useful to a
Node/TypeScript hunt where screening questions decide whether an application is
worth the credit of his time. **It is not free and this document does not pretend
it is: it leaves a draft, which is a state change on his account, and it is
therefore the operator's ruling and not mine.**

---

## 6. WHAT WOULD MOVE THESE 19 ROWS, RANKED BY WHAT IT COSTS HIM

Ordered by cost to the operator, not by row count.

1. **65 JOBCARD-OVERFLOW-MENU (2 rows) -- zero boundary, zero ruling.** Press the
   overflow control on a job card on a page this server already loads, capture,
   read the item labels. This is the company wave's pattern: a reading taken off
   a page already admitted and already loaded.
2. **62 TRACKER-ROW-MENU (3 rows) -- zero boundary, zero ruling.**
   **CORRECTED IN SECTION 13, and the correction is mine against myself: NOT
   `?stage=saved`, which is measured empty. Use `?stage=draft`,** which is
   admitted and is the one tracked capture carrying a populated row. The
   ledger's BLOCKED label rests on the Applied tab reading zero, and Applied is
   a third tab again.
3. **36 JOB-ALERTS-SURFACE (7 rows) -- needs a boundary ruling I did not take.**
   The largest and the most product-relevant: alerts are how LinkedIn's own
   matcher reaches him, and the `linkedin-jobs` skill already parses what they
   deliver. Two of the seven rows cannot be opened by an allowlist entry at all,
   because `settings` refuses before the allowlist is consulted.
4. **61 PREMIUM-APPLY-SURFACES (5 rows) -- one address already open.** Note for
   whoever takes it: census row 79, marking a job "Top Choice", **spends a
   non-refunding monthly credit, 3 per month.** A preview that cannot say how
   many remain is not a preview, so that row's spec needs a balance read before
   it needs a gate.
5. **70 EASY-APPLY-MULTISTEP (1 row) -- his ruling, restated in section 5.**
6. **81 FOUND-A-JOB-FLOW (1 row) -- nobody knows the address.** The cheapest
   honest step is to find the spelling, not to design the write.

---

## 7. WHAT I DID NOT DO

Stated plainly, because a wave that reports only its wins is reporting a fact
about its own queue.

* **I fired no write and submitted no application.** No confirm token was minted.
* **I opened no browser and loaded no page.** Every measurement here is against
  code and shipped predicates. Chrome pid 1252 was not touched, so there is no
  page of mine to close.
* **I built no reader and no WriteSpec**, for the reason in section 1: a WriteSpec
  may not name a surface nobody has opened, and none of these six surfaces has
  been opened.
* **I did not take the alerts boundary ruling.** Widening the allowlist and adding
  a forbidden-substring exemption is a contested, frozen-list edit and the digest
  chain has one head. It is not a thing to do unilaterally at the end of a wave.
* **I did not edit `linkedin_server/writes.py`** to correct the stale
  `_NINE_REFUSALS` pointer at line 971, nor `_audit/_census/jobs.md` to correct
  the stale boundary sentence at row 127. Both belong to other waves by
  `git log`, and both are recorded above instead.
* **I did not re-count the census.** The 19 rows are the ledger's own figures. If
  the row-by-row retirement edit lands, these counts move with it.
* **Two commissioned slices did not land inside my window** -- a page-size
  assumption census and a fixture census of the tracker and job-card overflow
  menus. Neither produced a number that appears anywhere in this document.
  Section 4's page-size statement and section 6's items 1 and 2 are written from
  my own reading only, and each says so where it matters. Their scratch paths, if
  they completed after this freeze, are
  `_audit/_scratch/_jobs-tail-pagesize-census.md` and
  `_audit/_scratch/_jobs-tail-menu-census.md`, which are gitignored and do not
  survive a clone.
  **PARTIALLY SUPERSEDED BY SECTION 13:** with 25 minutes left I took the single
  tracker fact sections 6 and 11 rest on, myself, narrowly -- and it corrected
  two of my own claims. **The MENU half is still unmeasured**, which is the half
  row 62 actually turns on. The page-size census remains entirely untaken by me.
* **I did not measure whether any tracker or job-card overflow menu exists in a
  capture, or whether it holds items.** Section 13 establishes a populated ROW
  and stops there, deliberately.
* **I did not open the alerts page, the premium apply surfaces, the found-a-job
  flow, or an apply modal.** Every one of my six blockers still has an unopened
  surface at the bottom of it, and that is the finding rather than an omission --
  but it is also, precisely, what this wave did not do.

## 8. GUARD STATE AT FREEZE, AND A RED THAT IS NOT MINE

    pytest tests/test_navigation_is_never_derived.py \
           tests/test_page_text_is_never_printed.py -q -p no:randomly --tb=line
    8 failed, 226 passed in 6.03s

**Nothing red is mine, and I proved the guards SAW my file rather than assuming
a missing name meant a pass** -- `--collect-only` shows the new probe in 2
collected parameters, and the sink test named for it passes on its own.

The 8 reds trace to a **SyntaxError at `scripts/_probe_comment_identifier.py`
line 318** -- a `try` with no `except` or `finally` yet. That is another wave
mid-edit in the shared tree, not a defect in the guards: the AST parse of the
corpus dies and takes the corpus-wide assertions down with it. **This is a
reading dated by the TREE and not by the SHA, and it will very likely be gone by
the time anyone reads this.** Nobody should inherit "8 reds" from this line.

---

## 9. THE ALERTS SURFACE SPLITS IN TWO, AND THE SHIPPED REFUSAL SAYS SO ITSELF

Section 3 said three forbidden substrings bite across the alerts rows. True, and
not yet actionable. **This repository already ships the instrument that makes it
actionable and I did not need to build one.**

`readonly.assert_read_url` composes its refusal from two clauses, pinned by
`tests/test_refusal_names_both_gates.py`. When a substring bites it also reports
whether an allowlist pattern would have admitted the address anyway:

    "A READ PATTERN DOES ADMIT THIS ADDRESS, so this substring is the ONLY
     thing refusing it"                                      -> exemption alone opens it
    "AND NO READ PATTERN ADMITS THIS ADDRESS EITHER ... Both gates refuse it,
     and the substring is merely the first"                  -> exemption alone does NOT

Asked of the four alerts addresses, the shipped refusal answers:

    census row        address shape                  substring gate   verdict
    J31 create        /jobs/alerts/create            FIRED            both gates refuse
    J34 delete        /jobs/alerts/delete/<id>       FIRED            both gates refuse
    J35 J36 freq/chan /jobs/alerts/settings/         FIRED            both gates refuse
    J37 manage page   /jobs/alerts/                  did NOT fire     no pattern only

**THAT IS THE SPLIT, AND IT IS THE MOST USEFUL LINE IN THIS DOCUMENT.**

* **The READ half is cheap.** The alerts manage page trips no forbidden
  substring at all. It costs exactly ONE allowlist pattern and no exemption, no
  denylist edit, and no write machinery of any kind.
* **The WRITE half is expensive, and more expensive than the ledger says.** Each
  of create, delete and frequency/channel needs an allowlist pattern AND a
  forbidden-substring exemption, because both gates refuse independently.
  "allowlist +1, denylist x1" charges one of the two, once, for a set of
  addresses that each need both.

**Why this is the recommendation to act on, for a Node/TypeScript hunt in
<a term from his own denied-terms list> or remote-India specifically.** His job alerts are how LinkedIn's own
matcher decides what to push at him daily, and the `linkedin-jobs` skill already
parses what those alerts deliver into his inbox. What the skill cannot do is say
whether the alerts are TUNED -- it reads what arrived, not what was configured.
A read of the manage page answers, in one page load and with no write anywhere
near it: how many alerts exist, what each one searches for, and which geography
each is pinned to. **If an alert is aimed wrong, every downstream email is wrong,
and nothing currently in this system can tell him that.**

That read is one allowlist pattern. It is still a boundary ruling and it is still
not mine to take unilaterally, which is why this is a recommendation and not a
commit. **But it is the smallest, cheapest, least reversible-risk item in all six
of my blockers, and it is attached to the one that gates the most rows.**

**WHAT THIS SECTION DOES NOT CLAIM.** That `/jobs/alerts/` is the address
LinkedIn serves -- nobody here has opened it, the spelling is a candidate, and
the probe labels it as one. A refusal measured against a guessed address tells
you about the gate, never about the page. **The gate's answer is solid; the
address is a hypothesis, and the first page load is what settles it.**

---

## 10. THE HEADLINE CLAIM, TESTED WHERE IT COULD HAVE BROKEN

Section 1 rests on one branch: that a spec with no `url_template` cannot be
acted on. I took that from the field's own docstring, and this repository's
standing rule is that a docstring is a standing instruction which can be stale.
So it was measured, and the measurement is a clean split rather than an argument:

    writes.grant_is_possible(spec):
        return spec.action in PERFORMABLE and spec.url_template is not None

    SANCTIONED_WRITES        13 specs
      url_template set       12   grant_is_possible True   12
      url_template None       1   grant_is_possible True    0

**The split falls exactly on the `url_template` boundary, 12 against 1, with no
exceptions in either direction.** A second, independent fact corroborates it
without sharing the same instrument: the one spec with `url_template=None` is
`linkedin_set_open_to_work`, and the server exposes **twelve** write tools, not
thirteen. It is designed, registered, and unable to act -- which is precisely
the state a WriteSpec for any of my six blockers would be born into.

### The stronger test was attempted and its CONTROL FAILED, so it is void

I also tried to show `mint` refusing a grant for the no-url spec. Both the
subject and the control came back with the same refusal:

    set_open_to_work  REFUSED  writes are disabled: set LINKEDIN_ENABLE_WRITES=1
    save_job          REFUSED  writes are disabled: set LINKEDIN_ENABLE_WRITES=1  <- CONTROL

The control was supposed to succeed and did not. **Both refused for a reason
that has nothing to do with `url_template`, so that reading cannot distinguish
the two causes and I am making no claim from it.** Had the control been absent I
would have read "set_open_to_work refused" as confirmation and reported a result
the experiment did not contain.

**I DID NOT ENABLE WRITES TO COMPLETE IT.** Setting `LINKEDIN_ENABLE_WRITES=1`
is a step toward firing, this wave was told not to fire, and the `grant_is_possible`
split already carries the claim without it. **A measurement I do not need is not
worth a flag I was told not to set** -- and the twelve shipped writes' bar is not
mine to loosen for the convenience of my own evidence.

---

## 11. ROW 62 IS BLOCKED ON A SOUND PREMISE AND A CONCLUSION THAT DOES NOT FOLLOW

The census note behind `TRACKER-ROW-MENU` reads: a capture of a populated
tracker row, *"which the repo has never had, because the Applied tab reads
zero."*

**The premise is solid, and I checked whether it was an instrument artifact
before saying so.** It is not: `linkedin_saved_jobs`'s contract is that *"an
empty result says so explicitly and carries LinkedIn's own count for the tab, so
an empty list can never be mistaken for a read that failed."* That is the same
corroborator whose ABSENCE let `linkedin_search_jobs` report seven rows against a
LinkedIn-reported 2915 without anything noticing. **The tracker reader has the
guard the search reader lacked, so a zero from it is a zero.**

**The conclusion does not follow from it.** Applied reading zero blocks a capture
of an APPLIED row. The tracker has more than one readable tab, and this server
already reads two others:

    /jobs-tracker/?stage=saved    linkedin_saved_jobs
    /jobs-tracker/?stage=draft    linkedin_draft_applications  (the In Progress tab)
    /jobs-tracker/?stage=applied  reads zero on this account

All three addresses are on the allowlist (measured in section 3). **A populated
row on Saved or In Progress is a populated tracker row, and the per-row overflow
menu is drawn on it the same way.** Whether either is non-empty is a
measurement, and it is one nobody has to open a page to take, because this repo
already holds tracker captures on disk.

So row 62's queue is MEASURE, its boundary cost is zero, and the thing that
would move it is a click on a page this server loads today. **The word doing the
damage in that census note is "because".**

**WHAT I DID NOT ESTABLISH:** whether the Saved or In Progress tabs actually
carry rows on this account. I commissioned that measurement against the tracked
captures and it did not land inside my window (section 7). **I am not asserting
they are populated -- I am asserting that "Applied reads zero" is not evidence
that they are not, which is a different and much weaker claim, and the only one
the premise supports.**

---

## 12. THE ALERTS READ PATTERN, MEASURED BEFORE IT IS PROPOSED AND NOT APPLIED

Section 9 recommends one allowlist pattern for the alerts manage page. A
recommendation naming no pattern is a wish, so here is the exact candidate and
what it was measured to do. **It is NOT in `readonly.py`. I did not edit the
allowlist, did not recompute the digest, and did not touch the freeze chain.**

    re.compile(r"^https://www\.linkedin\.com/jobs/alerts/?$")

Tested in memory against the shipped forbidden tuple:

    match  substring gate      address
    YES    none                the manage page, with trailing slash
    YES    none                the manage page, without
    no     '/create'           an alert create verb
    no     '/delete'           an alert delete verb
    no     '/settings/','settings'   alert frequency and channel
    no     none                an alert DETAIL page, /jobs/alerts/<id>/
    no     none                the manage page carrying any query

**It admits the read and nothing else.** The three write addresses do not match
it, and they remain independently refused by the substring gate, so this pattern
cannot become a back door to them even if a later reading of the shape is wrong.

**It takes NO query group**, deliberately, following the discipline the
search-appearances entry states about itself: the older profile-views entries
carry a query group because they predate the rule, and entries written under it
do not. Nothing here needs a query, so nothing here gets one.

**BLAST RADIUS: zero.** Six already-pinned addresses across jobs, tracker, feed,
profile and messaging were evaluated before and after; **6 unchanged, 0 changed.**

**WHY IT IS STILL NOT APPLIED, and this is not timidity.** The boundary-freeze
chain has ONE head, and this tree has had a wave's committed digest line deleted
by a neighbour computing `new` from the same `old` earlier today. Adding a
pattern means recomputing the digest against the tree as it then stands and
verifying that the tree minus your own line hashes to the previously pinned
value. **That is a several-minute operation whose correctness depends on nothing
else landing in the middle of it, and I am inside the last half hour of a wave
with two commissioned slices still outstanding.** Starting it now is how the
deletion happened to somebody else.

**So this is handed over rather than half-done.** The pattern is measured, its
reach is bounded, its blast radius is zero, and whoever holds the boundary can
apply it in one step with the evidence already taken.

**AND THE ADDRESS IS STILL A HYPOTHESIS.** Everything above measures the GATE.
Not one byte of it says LinkedIn serves that page at that spelling, because
nobody has opened it. **If the first load 404s, the correct response is to
change the pattern, not to conclude he has no alerts** -- the `linkedin-jobs`
skill's inventory says he has five.

---

## 13. I TOOK THE TRACKER MEASUREMENT MYSELF, AND IT CORRECTS TWO OF MY OWN CLAIMS

The commissioned slice had not landed with 25 minutes left, so I took the one
fact sections 6 and 11 depend on, narrowly: **is there a populated tracker row
in the captures this repo already holds?** Counts only; no row content, no
identifier, no title of any posting is reproduced here or was read out.

Two independent methods over the four tracked tracker captures. Method A counted
distinct `/jobs/view/<numeric id>` paths. Method B counted job references of any
shape (`currentJobId`, a job-posting urn, a view path) and, separately, the page
`<title>` and any sign-in markers.

    capture                      bytes    A: distinct ids   B: job refs   title
    _probe-tracker-saved.html     49448          0               0        Job tracker | LinkedIn
    _probe-tracker-saved-hyd.html 73242          0               0        Job tracker | LinkedIn
    _probe-tracker-draft.html     63268          1               2        Job tracker | LinkedIn
    _probe-tracker-applied.html  438464          0               0        LinkedIn

No sign-in or authwall markers in any of the four.

### Correction 1: I was wrong to suggest the Saved tab might be populated

Section 6 item 2 proposed capturing the row menu on `?stage=saved`. **The Saved
capture renders correctly -- right title, no authwall -- and carries zero job
references by both methods.** That tab is empty. My suggestion rested on the
Saved tab being a different tab from Applied, which is true, and on it being
non-empty, which I never checked and which is false in these captures.

### Correction 2: the APPLIED capture is not a Job tracker page

`_probe-tracker-applied.html` is the largest of the four at 438 KB and its
`<title>` is **`LinkedIn`, not `Job tracker | LinkedIn`** -- alone among the
four. It also carries 146 list elements against the siblings' 11. **Whatever
that capture is, it is not the tracker page rendering the Applied tab**, so it
cannot support the census note's "the Applied tab reads zero".

**That does NOT overturn the note.** Section 11 established the live reader
carries LinkedIn's own count for the tab, and that is a different and better
instrument than this fixture. **The narrow claim is only that the ARTIFACT does
not support the sentence, which is worth knowing for anyone who reaches for it
as evidence.** This is the render-gate law in its usual clothes: a zero taken
from a page that did not render is a fact about the capture.

### And the thing that actually helps: the populated row exists, on Draft

**`_probe-tracker-draft.html` is the only one of the four carrying a job
reference** -- 1 distinct posting id, referenced twice, which is consistent
rather than contradictory (a link and a urn for the same posting). The page
rendered correctly, same title as its siblings.

`/jobs-tracker/?stage=draft` is on the allowlist, measured in section 3. **So the
populated tracker row that row 62 needs already exists in a tracked fixture, on
the Draft tab, at zero boundary cost and zero page loads.**

**WHAT IS STILL NOT ANSWERED, and it is the whole of row 62:** whether that
capture contains the per-row OVERFLOW MENU, and whether it contains the menu's
ITEMS or only its trigger. Under the render gate those are three different
outcomes and a capture taken without pressing the control holds none of the
items. **I did not measure that**, and it is exactly what the commissioned slice
was asked for. **Nothing here should be read as "the tracker row menu is
capturable from a fixture" -- only that the ROW is.**
